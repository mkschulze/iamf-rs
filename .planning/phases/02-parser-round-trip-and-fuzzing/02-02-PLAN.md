---
phase: 02-parser-round-trip-and-fuzzing
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/obu/param_definition.rs
  - src/obu/audio_element.rs
  - src/obu/mix_presentation.rs
  - src/model/mod.rs
  - src/sequence.rs
  - src/dump.rs
  - tests/descriptors.rs
  - tests/temporal.rs
  - tests/profile.rs
  - tests/conformance.rs
  - tests/support/fixture.rs
  - tests/support/test_000003.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "All nine reserved bit groups currently discarded by known readers are owned and re-emitted"
    - "Every existing struct literal is migrated explicitly; encoder/test constructors choose zero rather than relying on normalization"
    - "Non-zero foreign reserved values parse and byte-round-trip while validation reports findings"
  prohibitions:
    - statement: "MUST NOT combine unrelated reserved groups, hide known bits in trailing Raw bytes, or reject them structurally."
  artifacts:
    - path: src/obu/audio_element.rs
      provides: "Owned Audio Element, Demixing-default, scalable-layout, layer, and output-gain reserved fields"
    - path: src/obu/mix_presentation.rs
      provides: "Owned rendering-config and layout reserved fields"
    - path: src/obu/param_definition.rs
      provides: "Owned shared seven-bit parameter-definition field"
---

<objective>
Remove lossy zero-normalization from every known reader before sequence byte-fidelity claims are
made. Output summary: `.planning/phases/02-parser-round-trip-and-fuzzing/02-02-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parser-round-trip-and-fuzzing/02-CONTEXT.md
@src/obu/param_definition.rs
@src/obu/audio_element.rs
@src/obu/mix_presentation.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Give every discarded reserved group a width-accurate owner</name>
  <read_first>
    - src/obu/param_definition.rs — discarded 7-bit field
    - src/obu/audio_element.rs — Audio Element(5), Demixing default(5+4), scalable layout(5), channel layer(2), OutputGain(2)
    - src/obu/mix_presentation.rs — RenderingConfig(6), layout post-field(2 for Sound System or 6 otherwise)
  </read_first>
  <files>src/obu/param_definition.rs, src/obu/audio_element.rs, src/obu/mix_presentation.rs, tests/descriptors.rs, tests/temporal.rs</files>
  <action>
Write non-zero hand-computed vectors before editing models. Add fields at the nearest exact wire
structure: `ParamDefinition.reserved`; `AudioElement.reserved`; two Demixing default reserved
fields; `ScalableChannelLayoutConfig.reserved`; `ChannelAudioLayerConfig.reserved`; `OutputGain.reserved`;
`RenderingConfig.reserved`; and `LayoutWithLoudness.reserved` (width conditional on layout type).
Readers store and writers emit each original value. Constructors set zero. `validate()` reports
non-zero values but read/write remain faithful and permissive.
  </action>
  <verify>
    <automated>cargo test --locked --test descriptors --test temporal &amp;&amp; ! rg -n 'let _reserved|let reserved.*Preserved as zero' src/obu</automated>
    <fails_when>non-zero exit; any enumerated group remains discarded; literal-zero writing replaces stored data; or a non-zero vector is rejected</fails_when>
  </verify>
  <done>Nine reserved groups are represented, validated separately, and byte-faithful.</done>
  <commit>git add src/obu/param_definition.rs src/obu/audio_element.rs src/obu/mix_presentation.rs tests/descriptors.rs tests/temporal.rs &amp;&amp; git commit -m "feat(parser): preserve reserved fields"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate every existing struct literal and presentation path</name>
  <read_first>
    - Output of `rg -l 'ParamDefinition \{|AudioElement \{|AudioElementParam::Demixing \{|ScalableChannelLayoutConfig \{|ChannelAudioLayerConfig \{|OutputGain \{|RenderingConfig \{|LayoutWithLoudness \{' src tests --glob '*.rs'`
    - Each listed file — preserve its intended zero or explicitly tested non-zero value
  </read_first>
  <files>src/model/mod.rs, src/sequence.rs, src/dump.rs, tests/profile.rs, tests/conformance.rs, tests/support/fixture.rs, tests/support/test_000003.rs</files>
  <action>
Update every literal in the live census, including `src/model/mod.rs`, `src/sequence.rs`,
`src/dump.rs`, `tests/descriptors.rs`, `tests/temporal.rs`, `tests/profile.rs`,
`tests/conformance.rs`, `tests/support/fixture.rs`, and `tests/support/test_000003.rs`. Use explicit
zero at encoder fixtures and the intended non-zero values only in fidelity vectors. Update dump
output so reserved values are visible. Re-run the census after edits; any newly reported file is in
scope and must be migrated before commit.
  </action>
  <verify>
    <automated>cargo test --locked &amp;&amp; cargo clippy --locked --all-targets -- -D warnings</automated>
    <fails_when>non-zero exit; an initializer is missed; golden fixture bytes change; or dump output hides the new values</fails_when>
  </verify>
  <done>Every current initializer compiles with deliberate reserved values and Phase 1 zero-byte output is unchanged.</done>
  <commit>git add src/model/mod.rs src/sequence.rs src/dump.rs tests/descriptors.rs tests/temporal.rs tests/profile.rs tests/conformance.rs tests/support/fixture.rs tests/support/test_000003.rs &amp;&amp; git commit -m "refactor(model): migrate reserved field literals"</commit>
</task>
</tasks>

<final_verification>
- The reserved-discard census is empty and the struct-literal census names no unmodified file.
- Full tests, golden reproduction, strict Clippy, and `git diff --check` pass.
</final_verification>

<success_criteria>
- Known foreign syntax is representable and faithful, removing a prerequisite gap for PARSE-04.
</success_criteria>
