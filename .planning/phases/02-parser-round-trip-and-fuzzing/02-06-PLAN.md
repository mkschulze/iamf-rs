---
phase: 02-parser-round-trip-and-fuzzing
plan: 06
type: tdd
wave: 4
depends_on: ["02-03"]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .gitignore
  - src/lib.rs
  - src/fuzzing.rs
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/deny.toml
  - fuzz/fuzz_targets/parse_sequence.rs
  - fuzz/fuzz_targets/obu_roundtrip.rs
  - tests/fuzz_regression.rs
autonomous: true
requirements: [FUZZ-01, FUZZ-02, FUZZ-03]
must_haves:
  truths:
    - "Excluded fuzz workspace has its own lock/deny policy; libfuzzer-sys/NCSA never enters root lock or policy"
    - "Exactly two bins exist: hostile-byte parse_sequence and structure-aware obu_roundtrip"
    - "One bounded canonical generator is shared by roundtrip fuzzing and stable replay"
    - "Feature wiring and every command explicitly enable root fuzzing APIs only where required"
  prohibitions:
    - statement: "MUST NOT add a third target, custom mutator, root NCSA allowance, or disable production bounds under fuzz cfg."
  artifacts:
    - path: fuzz/Cargo.toml
      provides: "Independent exact two-bin package and explicit feature wiring"
    - path: src/fuzzing.rs
      provides: "Bounded canonical model generator"
    - path: fuzz/fuzz_targets/obu_roundtrip.rs
      provides: "Structural and byte equality oracle"
---

<objective>
Build the isolated two-target fuzz harness and shared bounded model generator. Output summary:
`.planning/phases/02-parser-round-trip-and-fuzzing/02-06-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parser-round-trip-and-fuzzing/02-03-SUMMARY.md
@.planning/research/STACK.md
@Cargo.toml
@deny.toml
</context>

<feature_contract>
Root: optional `arbitrary = 1.4.2` behind `fuzzing`; explicit `[[test]] fuzz_regression` with
`required-features = ["fuzzing"]`. Fuzz package: `default = []`,
`roundtrip-model = ["iamf/fuzzing"]`; only `obu_roundtrip` has
`required-features = ["roundtrip-model"]`. Therefore parse builds/runs without features, while every
roundtrip cargo-fuzz command passes `--features roundtrip-model`; stable replay passes root
`--features fuzzing`.
</feature_contract>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create isolated workspace, scoped license policy, and exact feature graph</name>
  <read_first>
    - .planning/research/STACK.md — fuzz and cargo-deny sections
    - Cargo.toml, Cargo.lock, deny.toml, .gitignore — root graph promises
  </read_first>
  <files>Cargo.toml, Cargo.lock, .gitignore, fuzz/Cargo.toml, fuzz/Cargo.lock, fuzz/deny.toml</files>
  <action>
Make root an explicit workspace with member `.` and excluded `fuzz`. Add optional arbitrary with its
license and the exact feature/test contract above. Create independent fuzz package with pinned
`libfuzzer-sys 0.4.13` and its own lock. Copy root cargo-deny policy into `fuzz/deny.toml`, add NCSA
only there with rationale. Ignore fuzz target/transient artifacts while committing lock/corpus and
intentional minimized regressions. Root lock and default linked graph must remain libfuzzer-free.
  </action>
  <verify>
    <automated>cargo metadata --locked --manifest-path fuzz/Cargo.toml --no-deps --format-version 1 &gt;/dev/null &amp;&amp; ! rg -n '^name = "libfuzzer-sys"$' Cargo.lock &amp;&amp; rg -n '^name = "libfuzzer-sys"$' fuzz/Cargo.lock</automated>
    <fails_when>non-zero exit; fuzz workspace resolves under root; root lock contains libfuzzer; or scoped policy omits NCSA</fails_when>
  </verify>
  <done>Fuzz dependencies and NCSA are isolated with explicit non-unified features.</done>
  <commit>git add Cargo.toml Cargo.lock .gitignore fuzz/Cargo.toml fuzz/Cargo.lock fuzz/deny.toml &amp;&amp; git commit -m "build(fuzz): isolate fuzz workspace"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared generator and exactly two targets</name>
  <read_first>
    - src/sequence.rs — production APIs from 02-03
    - tests/sequence_parse.rs — canonical shape rules
    - fuzz/Cargo.toml — required feature contract from Task 1
  </read_first>
  <files>src/lib.rs, src/fuzzing.rs, fuzz/fuzz_targets/parse_sequence.rs, fuzz/fuzz_targets/obu_roundtrip.rs, tests/fuzz_regression.rs</files>
  <action>
Expose doc-hidden `fuzzing` module only under root feature. Implement
`sequence_from_fuzz_bytes(&mut arbitrary::Unstructured) -> arbitrary::Result<ParsedSequence>` with
small capped vectors/payloads and every canonical shape: empty, descriptors-only, temporal variants,
all parameter contexts, redundant descriptors, reserved/trailing values, and unknown placements.
`parse_sequence` target calls production parser for every byteslice. `obu_roundtrip` uses the shared
generator, serializes, parses, asserts derived model equality, reserializes, and asserts byte
equality. It may reject only insufficient arbitrary input, never writer-valid models. Add stable
smoke oracle reuse. Exactly two `[[bin]]` entries exist.
  </action>
  <verify>
    <automated>cargo +nightly-2026-09-01 fuzz build parse_sequence &amp;&amp; cargo +nightly-2026-09-01 fuzz build obu_roundtrip --features roundtrip-model &amp;&amp; cargo test --locked --features fuzzing --test fuzz_regression</automated>
    <fails_when>non-zero exit; required feature is missing; more/fewer than two bins exist; or stable and fuzz paths use different generators</fails_when>
  </verify>
  <done>Both required targets build with commands matching manifests and share one bounded structural oracle.</done>
  <commit>git add src/lib.rs src/fuzzing.rs fuzz/fuzz_targets tests/fuzz_regression.rs &amp;&amp; git commit -m "feat(fuzz): add parser fuzz targets"</commit>
</task>
</tasks>

<final_verification>
- Both exact cargo-fuzz build commands and stable feature-gated smoke test pass.
- Root linked graph remains `iamf` plus `thiserror`; both cargo-deny configs pass independently.
- Exactly two fuzz bins exist and `git diff --check` passes.
</final_verification>

<success_criteria>
- FUZZ-01..FUZZ-03 provide isolated hostile-byte and structure-aware fuzzing without shipping-graph contamination.
</success_criteria>
