---
phase: 02-parser-round-trip-and-fuzzing
plan: 04
type: tdd
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/sequence.rs
  - tests/round_trip.rs
  - tests/support/sequence_cases.rs
autonomous: true
requirements: [PARSE-03, PARSE-04, PARSE-05, PARSE-06]
must_haves:
  truths:
    - "Canonical valid models survive serialize/parse by derived PartialEq"
    - "Both crate writer paths survive parse/serialize byte-identically"
    - "Foreign non-minimal ULEB widths may canonicalize and are documented/tested as the sole width caveat"
    - "Unknown OBUs and bounded unknown parameter data preserve bytes and exact positions"
    - "Non-zero known reserved fields and all Parameter Data contexts participate in properties"
  prohibitions:
    - statement: "MUST NOT use normalized equality, promise universal foreign-byte identity, or use Raw as recovery for known corruption."
  artifacts:
    - path: tests/support/sequence_cases.rs
      provides: "Bounded canonical strategies covering every Phase 2 shape"
    - path: tests/round_trip.rs
      provides: "Both directions and explicit unknown-position sentinels"
---

<objective>
Prove both promised round-trip strengths and exact unknown-data placement. Output summary:
`.planning/phases/02-parser-round-trip-and-fuzzing/02-04-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parser-round-trip-and-fuzzing/02-03-SUMMARY.md
@src/sequence.rs
@src/obu/parameter_block.rs
@tests/support/fixture.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove structural model round trips over canonical strategies</name>
  <read_first>
    - src/sequence.rs — exact flat model and writer/parser APIs
    - tests/descriptors.rs and tests/temporal.rs — constructible domains and edge vectors
    - Cargo.toml — existing proptest 1.11.0
  </read_first>
  <files>src/sequence.rs, tests/support/sequence_cases.rs, tests/round_trip.rs</files>
  <action>
Write a failing 256-case property, then build bounded compositional strategies (vectors 0..=4,
payloads 0..=64) for empty/descriptors-only, delimiter/no-delimiter, multi-substream, redundant
descriptor, every non-zero reserved field, Mix Gain/Demixing/Recon Gain/Reserved data, known
trailing, and unknown OBU placements. All blocks have earlier governing definitions and canonical
grouping; do not discard cases with broad `prop_assume!`. Add
`ParsedSequence::from_parts(&DescriptorSet, &[TemporalUnit]) -> Result<Self>` as an ordering-only
adapter, not a serializer. Assert derived `PartialEq` exactly.
  </action>
  <verify>
    <automated>PROPTEST_CASES=256 cargo test --locked --test round_trip model_round_trip</automated>
    <fails_when>non-zero exit; custom equality is used; or invalid generation dominates</fails_when>
  </verify>
  <done>Every canonical encoder-representable shape has exact structural round-trip evidence.</done>
  <commit>git add src/sequence.rs tests/support/sequence_cases.rs tests/round_trip.rs &amp;&amp; git commit -m "test(parser): prove model round trips"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove own-byte identity, caveat foreign widths, and lock unknown offsets</name>
  <read_first>
    - src/bits/leb128.rs and src/obu/header.rs — minimal/fixed widths and re-emission
    - src/obu/mod.rs — known trailing precedence
    - src/obu/boundaries.rs — exact offset oracle
  </read_first>
  <files>src/sequence.rs, tests/support/sequence_cases.rs, tests/round_trip.rs</files>
  <action>
Add full-vector byte properties for `write_parsed_sequence` and streaming `write_sequence`. Add a
legal fixed-width foreign `obu_size` vector that parses then canonicalizes, and put the precise
own-output/foreign-width caveat on public rustdoc. Add an explicit sentinel sequence: known OBU with
trailing bytes, unknown OBU with flags/extension/indivisible payload between known sentinels, raw
extension definition, multi-subblock Raw Parameter Data, and final frame. Assert variant indices,
raw slices, and every `find_obu_boundaries` offset before/after. Add truncation/corrupt-known
negative tests. Minimized property failures become named regressions before fixes.
  </action>
  <verify>
    <automated>PROPTEST_CASES=256 cargo test --locked --test round_trip -- --nocapture</automated>
    <fails_when>non-zero exit; own bytes differ; unknown bytes survive but move; foreign width is overpromised; or known corruption becomes Raw</fails_when>
  </verify>
  <done>Own-output byte identity and exact unknown placement are executable, with an honest foreign-width boundary.</done>
  <commit>git add src/sequence.rs tests/support/sequence_cases.rs tests/round_trip.rs &amp;&amp; git commit -m "test(parser): prove byte and unknown-data fidelity"</commit>
</task>
</tasks>

<final_verification>
- 256-case round-trip suite, full root tests, strict Clippy, and `git diff --check` pass.
- Equality is derived and explicit sentinel assertions include vector positions plus absolute offsets.
</final_verification>

<success_criteria>
- PARSE-03..PARSE-06 hold at their documented strengths without normalizing foreign syntax.
</success_criteria>
