---
phase: 02-parser-round-trip-and-fuzzing
plan: 05
type: tdd
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/sequence.rs
  - tests/sequence_parse.rs
  - tests/round_trip.rs
  - tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md
  - tests/fixtures/MANIFEST.md
  - tests/support/reference_expectations.rs
  - tests/parse_reference.rs
autonomous: true
requirements: [PARSE-07]
must_haves:
  truths:
    - "All 37 positive fixtures have explicit complete field expectations; both named negatives are separate"
    - "Four unpaired iamf-tools files have committed byte-offset hand-decodes tied to pinned git blobs before constants are written"
    - "test_000015 remains positive through explicit raw ungoverned-Parameter-Block preservation"
    - "test_000059 remains positive and asserts Demixing plus every Recon Gain block/layer/flag/gain"
    - "Assertions call flat `ParsedSequence::validate()` and never sort/rebuild DescriptorSet"
    - "FLAC/Opus/AAC config remains Raw with exact FourCC, position, length, and SHA-256"
  prohibitions:
    - statement: "MUST NOT use parse-success alone, parser-generated expectations, manifest filename summaries, or Phase 3 codec interpretation."
  artifacts:
    - path: tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md
      provides: "Pinned independent hand-decodes for four unpaired files"
    - path: tests/support/reference_expectations.rs
      provides: "37 positive field rows and two named negative dispositions"
    - path: tests/parse_reference.rs
      provides: "Inventory bijection and full semantic assertions"
---

<objective>
Prove the committed foreign corpus is understood, with independent expectation provenance and no
positive exclusions. Output summary:
`.planning/phases/02-parser-round-trip-and-fuzzing/02-05-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parser-round-trip-and-fuzzing/02-03-SUMMARY.md
@tests/fixtures/MANIFEST.md
@tests/refcorpus.rs
@tests/fixtures_cap.rs
@REFERENCES.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Derive complete independent expectations for four unpaired iamf-tools files</name>
  <read_first>
    - REFERENCES.md — exact pinned iamf-tools SHA
    - Pinned `.reference/iamf-tools` README, four git blobs, and OBU reader sources via `git show`
    - tests/fixtures/MANIFEST.md — current short summaries, explicitly insufficient alone
  </read_first>
  <files>tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md, tests/fixtures/MANIFEST.md</files>
  <action>
Before Rust constants, create one complete table per unpaired file. Record commit
`848c6ff4968ff8cc6f728259892ab4f90cb83256`, upstream path, blob ID, local SHA-256/length, and every
OBU offset/bit field needed by the expectation schema: order/headers, profiles, IDs/references,
layouts/layers, definitions/data, grouping/trims, reserved/trailing, raw codec offset/length/digest,
and findings. Derive semantic values manually from `xxd -g1` plus pinned field-layout sources.
Cross-check boundaries by manual size arithmetic and the Phase 1 walker only; never use
`parse_sequence`/crate dump as semantic oracle. Link the artifact from MANIFEST and document that
upstream provides no paired configurations for these four.
  </action>
  <verify>
    <automated>for n in noise_1024samp_5p1_opus noise_1024samp_stereo_flac noise_3s_stereo_opus tones_100ms_3OA_stereo_opus; do rg -q "$n" tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md || exit 1; done &amp;&amp; rg -q '848c6ff4968ff8cc6f728259892ab4f90cb83256' tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md &amp;&amp; ! rg -n 'parse_sequence|iamf::' tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md</automated>
    <fails_when>non-zero exit; any table/blob/digest is incomplete; or parser output supplied semantics</fails_when>
  </verify>
  <done>All four unpaired files have reviewable pinned-byte truth before expectations exist.</done>
  <commit>git add tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md tests/fixtures/MANIFEST.md &amp;&amp; git commit -m "docs(fixtures): hand-decode unpaired references"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the evidence-backed 37-positive/two-negative expectation bijection</name>
  <read_first>
    - 34 paired `tests/fixtures/reference/*.textproto` files
    - tests/fixtures/PHASE2-EXPECTATION-PROVENANCE.md — remaining four independent sources
    - tests/refcorpus.rs — deterministic discovery
  </read_first>
  <files>tests/support/reference_expectations.rs, tests/parse_reference.rs, tests/fixtures/MANIFEST.md</files>
  <action>
Define plain Rust expectation structs with exact, non-optional checks for OBU kinds; profiles;
codec IDs/frame sizes/roll/raw digest or LPCM fields; element IDs/types/substreams/parameters/layers;
mix IDs/references/layouts; temporal units/delimiters/block IDs/data/frame IDs/trims;
reserved/trailing bytes; and ordered findings. Add exactly 37 positive rows: 33 valid root IAMFs
plus all four iamf-tools IAMFs. Add separate negative rows for structurally truncated
`test_000129.iamf` and `negative/tones_256samp_5p1_pcm.iamf` with zero frame size. Write a
filesystem bijection test that
rejects missing, duplicate, unexpected, or empty inventories. Preserve cap/provenance rules.
  </action>
  <verify>
    <automated>cargo test --locked --test parse_reference expectation_inventory_is_a_bijection</automated>
    <fails_when>non-zero exit; counts differ from 37+2; or either set difference is non-empty</fails_when>
  </verify>
  <done>Every committed file has exactly one correctly classified explicit disposition.</done>
  <commit>git add tests/support/reference_expectations.rs tests/parse_reference.rs tests/fixtures/MANIFEST.md &amp;&amp; git commit -m "test(parser): inventory reference expectations"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert every positive field and both named negative dispositions</name>
  <read_first>
    - tests/support/reference_expectations.rs — complete ledger
    - src/sequence.rs — flat validation/grouping interface
    - src/obu/codec_config.rs — Raw codec boundary
  </read_first>
  <files>tests/parse_reference.rs, tests/support/reference_expectations.rs</files>
  <action>
Implement assertion helpers whose failures name fixture, OBU index, and field. Compare every ordered
field, never sorting. Call `ParsedSequence::validate()` directly and compare exact ordered
`(Location, message)` findings. Require non-LPCM configs to remain Raw and hash their bytes. Give
test_000015 an explicit raw ungoverned Parameter Block disposition and test_000059 explicit
Demixing and all Recon Gain layer/flag/gain assertions. In separate negative tests, assert
test_000129's exact structural truncation/offset, then parse the zero-frame-size file, assert stored
zero, and assert the exact validation finding; never run either through the positive helper.
  </action>
  <verify>
    <automated>cargo test --locked --test parse_reference -- --nocapture &amp;&amp; cargo test --locked --test fixtures_cap --test refcorpus --test reference_manifest</automated>
    <fails_when>non-zero exit; a positive stops at is_ok; test_000059 is skipped; validation rebuilds DescriptorSet; or negative is promoted</fails_when>
  </verify>
  <done>All 37 positives are field-understood and both negatives have exact named dispositions.</done>
  <commit>git add tests/parse_reference.rs tests/support/reference_expectations.rs &amp;&amp; git commit -m "test(parser): assert reference corpus semantics"</commit>
</task>
</tasks>

<final_verification>
- Corpus suite reports 37 positives and two separately tested negatives; cap/manifest/pin tests pass.
- Provenance contains no parser-derived semantics; raw codecs remain opaque; test_000059 is fully asserted.
- Strict Clippy and `git diff --check` pass.
</final_verification>

<success_criteria>
- PARSE-07 is satisfied for every committed positive with independent field-level evidence.
</success_criteria>
