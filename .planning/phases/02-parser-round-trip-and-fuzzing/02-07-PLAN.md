---
phase: 02-parser-round-trip-and-fuzzing
plan: 07
type: tdd
wave: 5
depends_on: ["02-06"]
files_modified:
  - fuzz/corpus/parse_sequence/
  - fuzz/corpus/obu_roundtrip/
  - fuzz/CORPUS.md
  - tests/fuzz_regression.rs
  - .github/workflows/ci.yml
  - .github/workflows/fuzz.yml
autonomous: true
requirements: [FUZZ-04, FUZZ-05]
must_haves:
  truths:
    - "Parse corpus contains byte-identical copies of four pinned positive iamf-tools fixtures"
    - "Roundtrip corpus decodes to every canonical Phase 2 model/unknown-placement shape"
    - "Stable replay executes every seed and committed minimized artifact on all four target paths"
    - "Coverage fuzzing is separate Linux/nightly bounded CI and uploads failures"
  prohibitions:
    - statement: "MUST NOT use symlinks for cross-platform corpus, skip unreadable seeds, or put nightly/sanitizers in the PR matrix."
  artifacts:
    - path: fuzz/CORPUS.md
      provides: "Seed provenance/shape map and minimization policy"
    - path: tests/fuzz_regression.rs
      provides: "Stable all-file replay"
    - path: .github/workflows/fuzz.yml
      provides: "Bounded Linux nightly discovery fuzzing"
---

<objective>
Seed both fuzzers and make every discovered regression permanent across four stable targets. Output
summary: `.planning/phases/02-parser-round-trip-and-fuzzing/02-07-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-parser-round-trip-and-fuzzing/02-06-SUMMARY.md
@tests/fixtures/MANIFEST.md
@.github/workflows/ci.yml
@.github/workflows/reference.yml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Seed both corpora and make stable replay exhaustive</name>
  <read_first>
    - tests/fixtures/reference/iamf-tools/ — four pinned positive IAMFs
    - src/fuzzing.rs — exact Unstructured grammar
    - .planning/phases/02-parser-round-trip-and-fuzzing/02-CONTEXT.md — minimize/commit policy
  </read_first>
  <files>fuzz/corpus/parse_sequence/, fuzz/corpus/obu_roundtrip/, fuzz/CORPUS.md, tests/fuzz_regression.rs</files>
  <action>
Copy, do not symlink, all four positive iamf-tools IAMFs and assert SHA-256 equality with originals.
Create deterministic roundtrip seeds for empty, descriptors-only, delimiter/no-delimiter,
multi-substream, all parameter contexts, redundant descriptor, known trailing/reserved values,
unknown before/between/after, and bounded raw parameter data. Document every seed's source/shape.
Stable replay recursively enumerates both corpora plus committed artifact directories, sorts paths,
fails on empty/unreadable/skipped inputs, calls production parse for hostile seeds, and calls the
exact shared structural+byte oracle for model seeds. Document `cargo fuzz tmin`, minimized commit,
original CI artifact, and focused named regression when an invariant is known.
  </action>
  <verify>
    <automated>cargo test --locked --features fuzzing --test fuzz_regression -- --nocapture</automated>
    <fails_when>non-zero exit; corpus empty; digest differs; model seed cannot construct; artifact is skipped; or either equality is omitted</fails_when>
  </verify>
  <done>Both committed corpora are meaningful and every retained input replays under stable Rust.</done>
  <commit>git add fuzz/corpus fuzz/CORPUS.md tests/fuzz_regression.rs &amp;&amp; git commit -m "test(fuzz): seed permanent corpora"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire four-target stable replay and separate bounded nightly fuzzing</name>
  <read_first>
    - .github/workflows/ci.yml — exact target/Rosetta matrix and guardrails
    - .github/workflows/reference.yml — pinned action and artifact-upload patterns
    - fuzz/Cargo.toml — exact `roundtrip-model` feature contract
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/fuzz.yml, tests/fuzz_regression.rs</files>
  <action>
Add `cargo test --locked --features fuzzing --target ${{ matrix.target }} --test fuzz_regression` to
the existing matrix, preserving Rosetta. Guardrails run root cargo-deny, fuzz manifest with
`--config fuzz/deny.toml`, and assert root lock/default linked graph excludes libfuzzer. Create
schedule+workflow_dispatch-only Linux workflow using checkout/upload SHAs already pinned, dated
`nightly-2026-09-01`, and cargo-fuzz 0.13.2. Build/run `parse_sequence` normally and
`obu_roundtrip --features roundtrip-model`, each for `-max_total_time=300`; propagate failures and
upload `fuzz/artifacts/**` plus logs.
  </action>
  <verify>
    <automated>cargo test --locked --features fuzzing --test fuzz_regression &amp;&amp; cargo deny --all-features check &amp;&amp; cargo deny --manifest-path fuzz/Cargo.toml --config fuzz/deny.toml --all-features check</automated>
    <fails_when>non-zero exit; stable replay misses a target/input; NCSA reaches root; or fuzz job swallows failures</fails_when>
  </verify>
  <done>Known regressions are four-target stable gates and new discovery is isolated/bounded nightly work.</done>
  <commit>git add .github/workflows/ci.yml .github/workflows/fuzz.yml tests/fuzz_regression.rs &amp;&amp; git commit -m "ci(fuzz): replay corpus across four targets"</commit>
</task>
</tasks>

<final_verification>
- Stable replay covers all corpus/artifact files and both 10,000-run cargo-fuzz commands pass with the exact feature contract.
- Workflow inspection confirms all four stable paths and no PR trigger for nightly fuzzing.
- Root/fuzz license and lock separation, strict Clippy, full tests, and `git diff --check` pass.
</final_verification>

<success_criteria>
- FUZZ-04 and FUZZ-05 provide real pinned seeds, permanent cross-target replay, and separate bounded discovery fuzzing.
</success_criteria>
