---
phase: 01-conformant-lpcm-bitstream
plan: 09
type: execute
wave: 9
depends_on: ["01-08"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - BITS-01
  - BITS-07
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
  - CONF-11
  - DEC-04
gap_closure: true

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "ROADMAP success criterion 2 describes the implemented split: sample identity is proved by the non-silent, per-channel-distinguishable, trimmed 5.1 fixture, while descriptor-order observability is proved by a separate structure fixture with at least six OBUs and two Audio Elements"
    - "ROADMAP success criterion 3 names `iamf-tools@v2.1.0` and its pinned SHA as the tool release whose tree implements IAMF v1.1.0; it no longer asks for a nonexistent v1.x tool tag"
    - "BITS-01 and BITS-07 describe the accepted D-01 implementation: shipping `BitCursor`/`BitWriter` are hand-rolled and `bitstream-io` is a dev-only differential oracle"
    - "CONF-11 describes the executed research correction 3 fixture supply: a size-capped corpus is vendored from the pinned `libiamf` git tree with a manifest, while Bazel remains confined to the separate custom-output oracle path"
    - "All amended requirement IDs, checked completion marks, Phase 1 mappings and traceability rows remain present exactly once"
  prohibitions:
    - statement: "MUST NOT weaken sample identity, strict-parser acceptance, byte-diff exactness, descriptor-order observability, fixture provenance, or reference pinning merely to make the planning documents agree with the implementation. This plan reconciles stale premises to already-recorded decisions and evidence; it does not redefine the Core Value."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Authoritative Phase 1 success criteria aligned with the executed three-fixture conformance design and the exact iamf-tools pin"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Stable BITS-01, BITS-07 and CONF-11 IDs with descriptions matching the accepted implementation and unchanged traceability"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "CONFORMANCE-GATE.md"
      via: "criteria 2 and 3 summarize the recorded Experiment 2 fixture split and the exact oracle verdicts without contradicting them"
      pattern: "two Audio Elements|v2.1.0"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md"
      via: "BITS-01/BITS-07 carry D-01 and CONF-11 carries the documented research correction 3 replacement"
      pattern: "BitCursor|differential oracle|pinned"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "the preserved IDs remain in the Phase 1 requirement list and traceability table"
      pattern: "BITS-01|BITS-07|CONF-11"
---

<objective>
Reconcile the authoritative Phase 1 contract with the implementation and evidence already accepted
during plans 01-02, 01-03 and 01-08.

Purpose: Phase verification found working conformance behavior hidden behind stale wording. The
ROADMAP still describes the rejected one-fixture/two-Codec-Config design and a nonexistent v1.x
`iamf-tools` tag; REQUIREMENTS still prescribes two implementation mechanisms superseded by D-01
and research correction 3. Correct those descriptions while preserving requirement identity,
traceability, completion status and the strength of every observable gate.

Output: an internally consistent `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` ready for a
fresh goal-backward Phase 1 verification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md
@.planning/phases/01-conformant-lpcm-bitstream/01-RESEARCH.md
@.planning/phases/01-conformant-lpcm-bitstream/01-02-SUMMARY.md
@.planning/phases/01-conformant-lpcm-bitstream/01-03-SUMMARY.md
@.planning/phases/01-conformant-lpcm-bitstream/01-08-SUMMARY.md
@.planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md
@CONFORMANCE-GATE.md
@REFERENCES.md
@tests/fixtures/MANIFEST.md
@src/bits/mod.rs
@Cargo.toml
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ROADMAP criteria 2 and 3 to the recorded conformance design</name>
  <read_first>
    - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md — Observable Truths 2 and 3 and Critical Gap 1
    - CONFORMANCE-GATE.md — Experiment 2, the three-fixture table, and the Phase 1 exit verdicts
    - REFERENCES.md — the pin table and "version-number caveat"
    - .planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md — D-17 and the two amendments to D-18
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit only Phase 1 success criteria 2 and 3.

Criterion 2 must retain every substantive assertion: the pinned `libiamf` SHA, decoded sample count,
exact PCM identity, non-silent and per-channel-distinguishable input, non-multiple frame length and
positive final trim. Replace only the stale single-fixture/two-Codec-Config coupling. State that the
sample-identity fixture carries those audio properties, while a separate structure fixture has at
least six OBUs and two Audio Elements so repeated-descriptor ordering is observable. Use the
CONF-04 wording already adopted in REQUIREMENTS (two Codec Configs **or** two Audio Elements), and
do not imply that `libiamf` sample-identity is asserted on the structure-only fixture.

Criterion 3 must retain strict-parser acceptance and the exact-or-exhaustively-ledgered byte diff.
Replace "pinned v1.x tag" with the exact factual identity: `iamf-tools@v2.1.0`, pinned at
`848c6ff4968ff8cc6f728259892ab4f90cb83256`, is the tool release whose model passes the four
IAMF-v1.1.0 discriminators. Preserve that version number belongs to the tool and does not change
`SPEC_VERSION = "1.1.0"`. Keep the relation to the sample-identity file explicit.

Do not edit the historical descriptions of completed plans. They remain provenance for what was
planned and amended; the authoritative criteria are the scope of this task.
  </action>
  <verify>
    <automated>node /Users/cell/.codex/get-shit-done/bin/gsd-tools.cjs roadmap get-phase 1</automated>
    <fails_when>non-zero exit; criterion 2 still says the sample-identity fixture contains two Codec Configs; criterion 3 still says `v1.x`; or the output omits `two Audio Elements`, `v2.1.0`, the full pinned SHA, sample identity, strict-parser acceptance, or the exhaustive byte-diff condition</fails_when>
    <automated>git diff --check -- .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit or whitespace errors</fails_when>
  </verify>
  <done>The two ROADMAP truths that previously failed as written now describe the executed, user-approved fixture split and exact pinned tool release without weakening either oracle.</done>
</task>

<task type="auto">
  <name>Task 2: Amend BITS-01, BITS-07 and CONF-11 while preserving IDs and traceability</name>
  <read_first>
    - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md — Requirements Coverage rows for BITS-01, BITS-07 and CONF-11
    - .planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md — D-01 in full
    - .planning/phases/01-conformant-lpcm-bitstream/01-RESEARCH.md — research correction 3 and the fixture-supply findings
    - .planning/phases/01-conformant-lpcm-bitstream/01-02-SUMMARY.md — executed corpus import and cap
    - src/bits/mod.rs and Cargo.toml — actual shipping boundary and dev-only oracle
    - tests/fixtures/MANIFEST.md — corpus provenance, selection and regeneration procedure
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Rewrite the descriptions of exactly three checked requirements; keep their IDs, order, `[x]` marks,
Phase 1 mappings and traceability statuses unchanged.

- **BITS-01:** require the implemented hand-rolled `BitCursor&lt;'a&gt;` over `&amp;[u8]` and `BitWriter`,
  including `is_byte_aligned()`, `bits_remaining()` and a bounded sub-reader. Name native located
  errors as the reason the cursor owns the byte offset.
- **BITS-07:** require that only `src/bits` owns shipping bit offsets and primitives, no
  `std::io::Error` escapes that boundary, and `bitstream-io` is dev-only as the randomized
  differential oracle. Do not claim shipping code wraps or imports it.
- **CONF-11:** require the executed pinned-source corpus path: select the committed `libiamf@v1.1.0`
  `.iamf` fixtures, vendor the D-14 size-capped subset using pinned `git show`, and record selected,
  skipped and regeneration information in `tests/fixtures/MANIFEST.md`. Clarify that the
  digest-pinned Bazel container remains the generator for CONF-07's matching custom fixture, not the
  source of the offline reference corpus.

Do not mint replacement IDs, append duplicate requirements, or change Phase 2–4 mappings.
  </action>
  <verify>
    <automated>test "$(rg -c '^-[[:space:]]+\[x\][[:space:]]+\*\*BITS-01\*\*' .planning/REQUIREMENTS.md)" -eq 1 &amp;&amp; test "$(rg -c '^-[[:space:]]+\[x\][[:space:]]+\*\*BITS-07\*\*' .planning/REQUIREMENTS.md)" -eq 1 &amp;&amp; test "$(rg -c '^-[[:space:]]+\[x\][[:space:]]+\*\*CONF-11\*\*' .planning/REQUIREMENTS.md)" -eq 1</automated>
    <fails_when>non-zero exit because an amended ID is absent, duplicated, or no longer marked complete</fails_when>
    <automated>rg -n 'BitCursor|dev-only|differential oracle|git show|tests/fixtures/MANIFEST.md|CONF-07' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or the three amended descriptions do not collectively name every required implementation fact</fails_when>
    <automated>git diff --check -- .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit or whitespace errors</fails_when>
  </verify>
  <done>BITS-01, BITS-07 and CONF-11 retain their stable identities and completed Phase 1 mappings while accurately specifying the evidence-backed implementation that satisfies them.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the reconciled contract is consistent with code, evidence and traceability</name>
  <read_first>
    - .planning/ROADMAP.md — amended Phase 1 criteria and full requirement list
    - .planning/REQUIREMENTS.md — amended requirements and Traceability section
    - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md — the three contract-drift findings this plan must retire
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Run consistency checks after both edits. Confirm the Phase 1 ROADMAP list still contains all 63
original requirement IDs, each amended ID appears once in the requirements definitions and once in
traceability, and no checked status changed. Compare each new sentence directly to executable or
committed evidence: `tests/bits_oracle.rs`, the linked dependency graph, the corpus manifest,
`tests/conformance.rs`, `tests/support/fixture.rs`, `CONFORMANCE-GATE.md` and `REFERENCES.md`.

Run the focused tests that anchor the new wording. This is a documentation-reconciliation plan, so
do not modify Rust or fixtures to make the prose true; any mismatch means the prose must be corrected
to the already-approved evidence or the plan must stop and report a new gap.
  </action>
  <verify>
    <automated>ruby -EUTF-8:UTF-8 -e 'r=File.read(".planning/ROADMAP.md",encoding:"UTF-8"); q=File.read(".planning/REQUIREMENTS.md",encoding:"UTF-8"); expected=(1..7).map{|n|"BITS-%02d"%n}+(1..8).map{|n|"OBU-%02d"%n}+(1..9).map{|n|"DESC-%02d"%n}+(1..5).map{|n|"TIME-%02d"%n}+(1..3).map{|n|"SEQ-%02d"%n}+(1..3).map{|n|"PROF-%02d"%n}+(1..11).map{|n|"CONF-%02d"%n}+(1..13).map{|n|"GUARD-%02d"%n}+["DEC-01","DEC-02","DEC-04","DEC-05"]; phase=r[/### Phase 1:.*?^\*\*Requirements\*\*: ([^\n]+)/m,1].scan(/[A-Z]+-\d+/); definitions=q.scan(/^- \[([ x])\] \*\*([A-Z]+-\d+)\*\*:/); trace=q.scan(/^\| ([A-Z]+-\d+) \| Phase 1 \| ([^|]+) \|$/); count=lambda{|xs|xs.each_with_object(Hash.new(0)){|x,h|h[x]+=1}}; want=count.call(expected); abort "ROADMAP Phase 1 IDs differ" unless phase.length==63 &amp;&amp; count.call(phase)==want; abort "definitions not unique and checked" unless expected.all?{|id|definitions.select{|row|row[1]==id}==[["x",id]]}; abort "traceability not unique and Complete" unless trace.length==63 &amp;&amp; expected.all?{|id|trace.select{|row|row[0]==id}==[[id,"Complete"]]}; puts "Phase 1 requirement contract: 63 exact IDs, unique checked definitions, unique Complete traceability"'</automated>
    <fails_when>non-zero exit because the ROADMAP set differs from the exact 63-ID Phase 1 contract, an ID is missing or duplicated, any definition is not `[x]`, or any Phase 1 traceability row is not uniquely `Complete`</fails_when>
    <automated>cargo test --locked --test bits_oracle --test refcorpus --test reference_manifest --test golden --test fixture</automated>
    <fails_when>non-zero exit or any named test target reports zero tests</fails_when>
    <automated>test "$(cargo tree -e normal,no-proc-macro --prefix none | awk 'NF {print $1}' | sort -u)" = "$(printf 'iamf\nthiserror')"</automated>
    <fails_when>non-zero exit because `bitstream-io` or any other crate appears in the linked shipping graph</fails_when>
    <automated>node /Users/cell/.codex/get-shit-done/bin/gsd-tools.cjs verify references .planning/phases/01-conformant-lpcm-bitstream/01-09-PLAN.md</automated>
    <fails_when>non-zero exit or any referenced evidence path is absent</fails_when>
    <automated>git diff --check -- .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit or whitespace errors</fails_when>
  </verify>
  <done>The authoritative contract, stable requirement ledger and executable evidence say the same thing; verification failures caused solely by stale criteria and stale implementation premises are ready to be retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| completed implementation → authoritative planning contract | Editing success criteria after implementation could hide a defect by weakening the required truth. |
| historical evidence → present wording | A paraphrase can accidentally merge fixtures, versions or oracle roles that the evidence deliberately separates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-52 | Tampering | ROADMAP success criteria | high | mitigate | Preserve every substantive oracle assertion and change only the two disproven premises; verify against `CONFORMANCE-GATE.md` and `REFERENCES.md`. |
| T-01-53 | Repudiation | Requirement identity and traceability | medium | mitigate | Preserve IDs, checked marks, ordering and Phase 1 rows; exact-count checks reject deletion or duplication. |
| T-01-54 | Tampering | CONF-11 provenance | medium | mitigate | Name the pinned git source, cap, manifest and separate Bazel role so a self-generated corpus cannot be misrepresented as independent evidence. |
| T-01-SC | Tampering | Package-manager installs | low | accept | This plan installs no package and changes no dependency manifest. |
</threat_model>

<final_verification>
- `node /Users/cell/.codex/get-shit-done/bin/gsd-tools.cjs roadmap get-phase 1` prints the amended criteria without `v1.x` or a two-Codec-Config requirement on the sample-identity fixture.
- The deterministic Ruby contract check reports exactly 63 expected Phase 1 IDs, one checked definition per ID, and one `Complete` Phase 1 traceability row per ID.
- `cargo test --locked --test bits_oracle --test refcorpus --test reference_manifest --test golden --test fixture` passes.
- The linked normal dependency graph remains exactly `iamf` plus `thiserror`.
- `git diff --check -- .planning/ROADMAP.md .planning/REQUIREMENTS.md` passes.
- A direct diff shows only the five intended descriptions changed; no IDs, statuses, mappings or historical plan records moved.
</final_verification>

<success_criteria>
- Verification can mark ROADMAP truths 2 and 3 satisfied from the evidence already recorded.
- Verification can mark BITS-01, BITS-07 and CONF-11 satisfied exactly rather than as contract drift.
- The Core Value and all oracle strength remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-conformant-lpcm-bitstream/01-09-SUMMARY.md` recording
the five reconciled clauses, the evidence source for each, the focused test results, and confirmation
that requirement IDs and traceability were preserved.
</output>
