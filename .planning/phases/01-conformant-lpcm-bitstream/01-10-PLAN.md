---
phase: 01-conformant-lpcm-bitstream
plan: 10
type: execute
wave: 10
depends_on: ["01-09"]
files_modified:
  - .github/workflows/ci.yml
  - CONFORMANCE-GATE.md
  - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md
autonomous: false
requirements:
  - CONF-10
  - GUARD-09
  - GUARD-10
  - GUARD-12
gap_closure: true

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 4
  confidence: medium

must_haves:
  truths:
    - "One durable GitHub Actions run is tied to the exact CI-tested candidate SHA; that SHA is an ancestor of the later documentation-only closure HEAD, and every descendant path is inside the explicit closure-doc allowlist"
    - "Exactly one terminal branch is recorded: normal-four-target or waiver-three-target"
    - "On the normal branch, the tested run executes the golden-fixture step successfully for exactly aarch64-apple-darwin, x86_64-apple-darwin, x86_64-pc-windows-msvc and x86_64-unknown-linux-gnu"
    - "On the waiver branch, the tested run has successful golden steps for the other three targets, while native and Rosetta x86_64 macOS execution failures and all five D-17 fields are recorded"
    - "Each target runs the committed fixture/hash comparison and same-process double encode; a green build-only or skipped golden step does not count"
    - "A Windows workflow-only change is made only if the exact candidate run reproduces the failure; any required Rust source or test change stops this plan for a focused follow-up gap plan and fresh code-review/fix gate before publication"
    - "If the native macOS x86_64 runner is unavailable, the x86_64 test binaries execute under Rosetta on an arm64 runner; if even that is unavailable, a D-17 waiver records all five mandatory fields rather than silently shrinking the matrix"
    - "CONFORMANCE-GATE.md records the tested candidate SHA, closure HEAD relationship, run URL/ID, branch-appropriate target evidence, golden-step conclusions and any target-specific remediation or waiver"
    - "Fresh Phase 1 verification consumes that durable evidence and reaches a closure verdict without treating an in-progress, queued, cancelled, skipped or stale job as proof"
  prohibitions:
    - statement: "MUST NOT push a branch, create a PR, rerun a remote workflow, or mutate GitHub state without explicit user authorization. Inspecting existing runs is read-only; if no exact-head run exists, stop at the authorization gate."
    - statement: "MUST NOT accept run 34226193383 as Phase 1 proof: it is tied to stale head `5f6d5f5db4c1b8a731281779d693ec5b12230970`, Windows skipped the golden step after a test failure, and macOS x86_64 remained queued."
    - statement: "MUST NOT replace execution with cross-compilation, silently remove a target, loosen the golden assertion, regenerate the golden around a platform difference, or add a tolerance/permutation path."
    - statement: "MUST NOT modify `src/**` or `tests/**` as an undeclared deviation in this plan. A proven Rust/test defect requires a focused follow-up gap plan and a fresh resolved code-review/fix gate before its candidate may be published."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "The intended four-target full-suite and named GUARD-09/GUARD-10 execution, with branch-appropriate evidence when both x86_64 macOS routes are externally unavailable"
    - path: "CONFORMANCE-GATE.md"
      provides: "Durable tested-candidate CI evidence ledger, closure-HEAD ancestry/allowlist proof, mutually exclusive outcome, jobs, steps and any D-17 fallback"
    - path: ".planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md"
      provides: "Fresh goal-backward Phase 1 verdict based on the reconciled contract and the selected normal-four-target or waiver-three-target evidence branch"
  key_links:
    - from: "CONFORMANCE-GATE.md"
      to: ".github/workflows/ci.yml"
      via: "the recorded job and step names correspond to the four matrix entries and named golden step in the committed workflow"
      pattern: "Golden fixture reproduces on this target"
    - from: "CONFORMANCE-GATE.md"
      to: "tests/fixtures/golden/"
      via: "each successful golden step runs `tests/golden.rs` and `tests/fixture.rs` against the committed fixture and hash"
      pattern: "GUARD-09|GUARD-10"
    - from: ".planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md"
      to: "CONFORMANCE-GATE.md"
      via: "the verifier independently checks the tested SHA, run state, branch-appropriate successful target rows, and any required native/Rosetta failure evidence before disposing ROADMAP criterion 4"
      pattern: "normal-four-target|waiver-three-target|GUARD-09"
---

<objective>
Close the final executable Phase 1 gap with exact, branch-appropriate target byte-identity evidence
for a CI-tested candidate, and make that remote evidence durable and independently verifiable from
the later documentation-only closure HEAD.

Purpose: local golden tests and static workflow wiring prove intent, not execution on four operating
systems. The only previous run is stale and incomplete. This plan first freezes and validates the
candidate SHA, then obtains an exact-candidate CI run without assuming push authority, remediates only
failures reproduced on that head, records exact evidence, and re-runs Phase 1 verification.

Output: exactly one terminal result — either a complete four-target run, or a visible D-17
three-target operational waiver only after both native and Rosetta execution prove unavailable — a
durable evidence section in `CONFORMANCE-GATE.md`, and a fresh `01-VERIFICATION.md` closure verdict.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
@~/.codex/get-shit-done/workflows/verify-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md
@.planning/phases/01-conformant-lpcm-bitstream/01-09-SUMMARY.md
@.planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md
@.planning/phases/01-conformant-lpcm-bitstream/01-REVIEW.md
@.github/workflows/ci.yml
@CONFORMANCE-GATE.md
@tests/golden.rs
@tests/fixture.rs
@tests/fixtures/golden/
@rust-toolchain.toml
@./.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze a locally green candidate and audit remote evidence for its exact SHA</name>
  <read_first>
    - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md — Observable Truth 4, Human/External Verification 1 and Critical Gap 2
    - .github/workflows/ci.yml — four matrix entries, named golden step and D-16 fallback comment
    - tests/golden.rs and tests/fixture.rs — the assertions the remote step must actually execute
    - rust-toolchain.toml — pinned toolchain and the four target triples
  </read_first>
  <files>CONFORMANCE-GATE.md</files>
  <action>
Run the full local offline baseline and record `git rev-parse HEAD` only after plan 01-09 is committed
and the worktree contains no uncommitted implementation or contract changes. This SHA is the first
candidate; do not call it final until every later remediation is committed.

Use read-only GitHub CLI calls to inspect runs and jobs. Query by commit SHA, not by branch name or
"latest": `gh run list --workflow ci.yml --commit "$sha"` followed by `gh run view RUN_ID --json
headSha,status,conclusion,url,jobs`. If an exact-SHA run is `queued` or `in_progress`, monitor that
same run to a terminal state with `gh run watch RUN_ID --exit-status` or bounded `gh run view`
polling; live state is neither success evidence nor grounds to create a duplicate run.

Route exactly by the terminal query result:

- A completed exact-SHA run with conclusion `success`, exactly the four required target jobs, and a
  completed/successful `Golden fixture reproduces on this target (GUARD-09, GUARD-10)` step in each
  job goes directly to **Task 4**.
- A terminal exact-SHA run that failed, was cancelled/timed out/action-required, or is otherwise
  incomplete as evidence goes to **Task 3**, after downloading job metadata/logs sufficient to name
  the first failed, skipped or unavailable target/step. Do not call an in-progress run incomplete.
- No exact-SHA run goes to the **Task 2** authorization checkpoint. Do not push, create a PR, or
  trigger anything before that checkpoint.

Explicitly reject run 34226193383 and record it only as superseded context if useful.
  </action>
  <verify>
    <automated>cargo test --locked --all-targets &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo test --locked --test golden --test fixture</automated>
    <fails_when>non-zero exit, warning promoted to error, or either focused target reports zero tests</fails_when>
    <automated>test -z "$(git status --porcelain)" &amp;&amp; git rev-parse HEAD</automated>
    <fails_when>the worktree is dirty, the SHA is missing, or the SHA does not include completed plan 01-09</fails_when>
  </verify>
  <done>A clean, locally green candidate SHA is known; any live exact-SHA run was monitored to terminal; and the outcome is routed unambiguously to Task 4 on complete success, Task 3 on terminal diagnostic failure/incompleteness, or Task 2 when no exact-SHA run exists.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize how the exact candidate head reaches GitHub CI, only if no run exists</name>
  <files>.github/workflows/ci.yml</files>
  <action>Pause before external mutation and obtain one of the explicit publication choices below. Do not edit the workflow merely to bypass this gate.</action>
  <decision>Choose the authorized path that will create a CI run for the exact clean candidate SHA. Skip this checkpoint entirely when Task 1 found a complete run for that SHA.</decision>
  <context>
The workflow has only `pull_request` and `push` triggers, so `gh workflow run` cannot create an
exact-head run. A push or PR is an external mutation and is not implied by plan execution. The
executor has already performed every safe local and read-only check. Whichever path is selected,
the resulting remote `headSha` must equal the local candidate exactly; testing a branch name while
local commits remain unpublished is not evidence.
  </context>
  <options>
    <option id="user-pushes">
      <name>User pushes the candidate (recommended)</name>
      <pros>Keeps repository publication under direct user control; the executor resumes by finding the resulting exact-SHA run.</pros>
      <cons>Requires one manual `git push` or PR update before execution can resume.</cons>
    </option>
    <option id="authorize-executor-push">
      <name>Authorize executor push</name>
      <pros>Lets the executor publish the already-reviewed candidate and monitor the run end to end.</pros>
      <cons>Explicitly grants a state-changing operation against the configured remote; branch protection may still require a PR.</cons>
    </option>
  </options>
  <verify>After the authorized publication path completes, `gh run list --workflow ci.yml --commit "$sha"` returns a run and `gh run view RUN_ID --json headSha` reports the exact local candidate SHA.</verify>
  <done>The exact candidate is visible to GitHub CI through a user-selected, explicitly authorized path.</done>
  <resume-signal>Reply `user-pushes` after the exact candidate SHA is visible on GitHub, or `authorize-executor-push` to authorize publishing that candidate through the configured remote.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Diagnose only reproduced failures and preserve four executed target paths</name>
  <precondition>An exact-candidate CI run exists and has completed or exposed a concrete target failure</precondition>
  <read_first>
    - the exact-candidate run's failed job log and step metadata — no stale-run diagnosis is sufficient
    - .github/workflows/ci.yml — the failing target entry and fallback order
    - .planning/phases/01-conformant-lpcm-bitstream/01-CONTEXT.md — D-16 and D-17
    - CONFORMANCE-GATE.md — waiver format and existing W-1 example
  </read_first>
  <files>.github/workflows/ci.yml, CONFORMANCE-GATE.md</files>
  <action>
If all four target golden steps pass, make no workflow or code change and proceed to Task 4.

If Windows fails on the exact candidate, download that job's log and identify the first causal
failure, not downstream skipped steps. Reproduce it with the narrowest available Windows-equivalent
test or compile target. A narrowly scoped `.github/workflows/ci.yml` correction may remain in this
plan: make it, run the full local baseline, commit it, and treat the new commit as a new candidate
requiring a wholly new matrix run. Never splice successful jobs from different SHAs.

If the proven cause requires any change under `src/**` or `tests/**`, **stop this plan without making
that change**. Record the diagnosis and exact affected files, then create a focused follow-up Phase 1
gap plan through `/gsd-plan-phase 1 --gaps`. Execute that plan and run `/gsd-code-review 1`; if the
review finds issues, run `/gsd-code-review 1 --fix` and repeat review until its gate is resolved.
Only then resume this plan, freeze the reviewed commit as a new candidate, cross the publication
authorization gate again, and start a wholly new matrix. The existing resolved `01-REVIEW.md`
cannot silently cover newly changed Rust or tests.

If `x86_64-apple-darwin` is queued because `macos-13` is unavailable or retired, first retain the
exact target but move its execution to an available arm64 macOS runner: install/verify Rosetta 2,
install the `x86_64-apple-darwin` Rust target, build the target, and execute the produced tests under
`arch -x86_64`. A compile-only result does not count. Commit the workflow change and obtain a fresh
run for its new SHA.

Only if both the native x86_64 runner and actual Rosetta execution are unavailable for an external
runner reason may D-17 be used. Add a `CONFORMANCE-GATE.md` waiver naming: (1) GUARD-09/ROADMAP
criterion 4, (2) both execution routes attempted with run/job URLs, (3) why neither can execute, (4)
the weaker three-target property actually asserted, and (5) the executable restoration condition.
Do not delete the x86 target from `rust-toolchain.toml`, rewrite GUARD-09 as three-target, or call a
cross-compile success byte identity.

After every allowed workflow change, or after a separately planned and freshly reviewed code/test
fix, repeat Task 2's authorization boundary before publishing and monitor the new run to terminal
state. Choose exactly one terminal branch:

- **normal-four-target:** one tested candidate SHA has four target jobs and four successful named
  golden steps; no D-17 runner waiver is active.
- **waiver-three-target:** one tested candidate SHA has three target jobs with three successful named
  golden steps, while native x86_64 macOS and Rosetta x86_64 macOS execution both have concrete
  failed/unavailable run evidence and the D-17 entry contains all five required fields. This branch
  must not claim four-target execution.
  </action>
  <verify>
    <automated>cargo test --locked --all-targets &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo test --locked --test golden --test fixture</automated>
    <fails_when>non-zero exit or a remediation weakens the local golden/double-encode gate</fails_when>
    <automated>git diff --check</automated>
    <fails_when>non-zero exit or whitespace errors</fails_when>
    <automated>rg -n 'aarch64-apple-darwin|x86_64-apple-darwin|x86_64-pc-windows-msvc|x86_64-unknown-linux-gnu|Golden fixture reproduces on this target' .github/workflows/ci.yml</automated>
    <fails_when>any target triple or the named execution step is absent; if Rosetta fallback is active, its command does not actually run x86_64 test binaries</fails_when>
  </verify>
  <done>Exactly one terminal branch is ready: either four successful target executions, or three successful targets plus fully evidenced native-and-Rosetta failure and the complete D-17 waiver; any Rust/test remediation occurred only through a focused follow-up plan and fresh resolved review.</done>
</task>

<task type="auto">
  <name>Task 4: Record durable CI proof and re-run goal-backward Phase 1 verification</name>
  <precondition>The CI-tested candidate has reached exactly one Task 3 terminal branch: normal-four-target or waiver-three-target</precondition>
  <read_first>
    - final CI run JSON and its branch-appropriate job step lists
    - CONFORMANCE-GATE.md — Phase 1 exit table and D-17 waiver conventions
    - .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md — full prior report, especially truth 4 and Requirements Coverage
    - .planning/ROADMAP.md and .planning/REQUIREMENTS.md — post-01-09 authoritative contract
    - .planning/phases/01-conformant-lpcm-bitstream/01-REVIEW.md — resolved code-review gate
    - .planning/phases/01-conformant-lpcm-bitstream/01-01-SUMMARY.md through 01-09-SUMMARY.md — completed implementation and contract evidence
    - .planning/phases/01-conformant-lpcm-bitstream/01-10-PLAN.md — the executing gap plan; 01-10-SUMMARY.md does not exist until after this task and is not verifier input
  </read_first>
  <files>CONFORMANCE-GATE.md, .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md</files>
  <action>
Append a durable cross-target evidence section to `CONFORMANCE-GATE.md`. Record the exact 40-character
**CI-tested candidate SHA** (not the later documentation-only closure HEAD), workflow run ID and URL,
completion timestamp, and the terminal outcome marker exactly as `**Outcome:** normal-four-target`
or `**Outcome:** waiver-three-target`; exactly one marker is allowed.

On `normal-four-target`, record one evidence row for each of the four required targets with job
ID/URL, runner route, overall conclusion, and successful named golden step. On
`waiver-three-target`, record three successful evidence rows for arm64 macOS, Windows x86_64 and
Linux x86_64, plus separate failed/unavailable attempt rows for native macOS x86_64 and Rosetta
macOS x86_64; record all five D-17 fields and do not show or claim a fourth successful target. Use a
consistent `target`, `job`, `route`, `job conclusion`, `golden status` and `golden conclusion` table
schema so the verifier can count the branch-appropriate rows mechanically.

Record the committed SHA-256
`3e53f10babd78b721d524c0e41fbb0806a5ad37d2f5b4dd247a4336e0be1282c`; explain that step success
means the target regenerated and matched that committed artifact and passed same-process double
encode. If a remediation occurred, link it to the failing predecessor run and state why old and new
jobs were not combined. Never paste secrets or ephemeral authenticated log URLs.

Then execute a fresh goal-backward Phase 1 verification using the verifier workflow, the reconciled
ROADMAP/REQUIREMENTS, summaries 01-01 through 01-09, the executing 01-10-PLAN.md, the resolved code
review and this final CI evidence. Do not read or require 01-10-SUMMARY.md: it is produced only after
this task succeeds. Independently query the recorded run again and require `headSha` to equal the
recorded CI-tested candidate SHA. Regenerate `01-VERIFICATION.md`; do not merely edit the old score.
Re-run the local full suite,
strict Clippy, cargo-deny, guard proofs, dependency graph, reference-manifest checks, both external
oracles when available, artifact/key-link checks and golden hash. Keep the known rustfmt drift as a
non-blocking warning and the legal/provenance attestation as human verification; neither may be
silently recast as machine proof.

The evidence and verification documents necessarily land after CI, so closure HEAD will differ from
the tested candidate. Prove rather than assume that this is documentation-only: the tested candidate
must be an ancestor of closure HEAD, and every committed or uncommitted path after it must be in this
explicit allowlist: `CONFORMANCE-GATE.md`, `01-VERIFICATION.md`, `01-10-SUMMARY.md`,
`.planning/STATE.md`, `.planning/state.json`, `.planning/HANDOFF.json`, `.planning/ROADMAP.md`, and
`.planning/config.json` (using their full repository-relative paths). Any `src/**`, `tests/**`,
workflow, toolchain, dependency, fixture or other path after the tested SHA invalidates the evidence
and requires a new candidate plus a new matrix. Re-run this ancestry/allowlist check after the
01-10 summary and final state documents are committed.
  </action>
  <verify>
    <automated>cargo test --locked --all-targets &amp;&amp; cargo clippy --locked --all-targets -- -D warnings &amp;&amp; cargo deny check &amp;&amp; bash tools/prove-guards.sh</automated>
    <fails_when>non-zero exit, any warning is emitted by strict Clippy, cargo-deny lacks a successful verdict, or fewer than 6/6 guard canaries pass</fails_when>
    <automated>test "$(cargo tree -e normal,no-proc-macro --prefix none | awk 'NF {print $1}' | sort -u)" = "$(printf 'iamf\nthiserror')"</automated>
    <fails_when>non-zero exit because the linked shipping graph changed</fails_when>
    <automated>ruby -EUTF-8:UTF-8 -e 's=File.read("CONFORMANCE-GATE.md",encoding:"UTF-8"); outcomes=s.scan(/^\*\*Outcome:\*\* (normal-four-target|waiver-three-target)$/).flatten; abort "need exactly one outcome" unless outcomes.length==1; required=%w[aarch64-apple-darwin x86_64-apple-darwin x86_64-pc-windows-msvc x86_64-unknown-linux-gnu]; successes=s.scan(/^\| (aarch64-apple-darwin|x86_64-apple-darwin|x86_64-pc-windows-msvc|x86_64-unknown-linux-gnu) \|.*\| success \| completed \| success \|$/).flatten; if outcomes[0]=="normal-four-target"; abort "normal branch needs four unique successes" unless successes.sort==required.sort; else; expected=required-["x86_64-apple-darwin"]; abort "waiver branch needs exactly three successes" unless successes.sort==expected.sort; abort "missing native attempt" unless s.match?(/native.*x86_64.*(failed|unavailable)/i); abort "missing Rosetta attempt" unless s.match?(/Rosetta.*x86_64.*(failed|unavailable)/i); end; abort "golden hash missing" unless s.include?("3e53f10babd78b721d524c0e41fbb0806a5ad37d2f5b4dd247a4336e0be1282c"); puts "#{outcomes[0]} evidence shape valid"'</automated>
    <fails_when>non-zero exit because the outcome marker is absent/duplicated, normal branch lacks exactly four unique successes, waiver branch lacks exactly three successes plus both failed routes, or the committed hash is absent</fails_when>
    <automated>tested_sha=$(sed -nE 's/^\*\*CI-tested candidate SHA:\*\* `([0-9a-f]{40})`$/\1/p' CONFORMANCE-GATE.md | tail -n1); test "${#tested_sha}" -eq 40 &amp;&amp; git merge-base --is-ancestor "$tested_sha" HEAD &amp;&amp; unexpected=$({ git diff --name-only "$tested_sha"..HEAD; git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard; } | sort -u | grep -Ev '^(CONFORMANCE-GATE\.md|\.planning/phases/01-conformant-lpcm-bitstream/(01-VERIFICATION\.md|01-10-SUMMARY\.md)|\.planning/(STATE\.md|state\.json|HANDOFF\.json|ROADMAP\.md|config\.json))$' || true); test -z "$unexpected"</automated>
    <fails_when>the tested SHA is missing, is not an ancestor of HEAD, or any committed/uncommitted descendant path falls outside the explicit documentation/state allowlist</fails_when>
    <automated>git diff --check -- CONFORMANCE-GATE.md .planning/phases/01-conformant-lpcm-bitstream/01-VERIFICATION.md</automated>
    <fails_when>non-zero exit or whitespace errors</fails_when>
  </verify>
  <verify>
    <human-check>For `normal-four-target`, open all four durable job URLs and confirm each displays the CI-tested candidate SHA and a green named golden step. For `waiver-three-target`, open the three success URLs plus the native and Rosetta attempt URLs, confirm only the three claimed targets are green, and confirm the D-17 entry includes all five fields rather than a generic runner-unavailable note. In either branch, inspect the descendant-path allowlist result before accepting the later closure HEAD.</human-check>
  </verify>
  <done>Exactly one terminal outcome has durable evidence for its CI-tested candidate, closure HEAD is a documentation-only descendant by explicit path proof, and fresh Phase 1 verification reflects the reconciled contract and branch-appropriate current evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local candidate → remote GitHub commit | A branch name can point to a different SHA than the locally verified tree. |
| GitHub job summary → byte-identity claim | A green workflow can contain skipped target jobs or a skipped golden step. |
| runner availability → waiver | Operational pressure can silently shrink a four-target contract. |
| external logs → committed evidence | Mutable or ephemeral logs are not durable unless their IDs, SHA and conclusions are captured. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-55 | Spoofing | stale CI run presented as current | high | mitigate | Query and record the exact 40-character CI-tested candidate `headSha`; reject branch-only and run-34226193383 evidence, then prove it is an ancestor of the docs-only closure HEAD. |
| T-01-56 | Tampering | skipped golden step under an otherwise green run | high | mitigate | Inspect every job's step list and require the named GUARD-09/GUARD-10 step completed successfully. |
| T-01-57 | Repudiation | ephemeral CI result | medium | mitigate | Commit run/job IDs, canonical URLs, completion time, target routes and conclusions to `CONFORMANCE-GATE.md`. |
| T-01-58 | Tampering | mixing successful jobs from different SHAs | high | mitigate | All successful target rows claimed by the selected branch must come from one run whose `headSha` equals the CI-tested candidate. Native/Rosetta failure attempts may link predecessor runs but can never be spliced in as successful evidence; any remediation starts the claimed-success matrix over. |
| T-01-59 | Denial of Service | native macOS x86_64 runner retirement | medium | mitigate | Execute x86_64 tests under Rosetta first; D-17 waiver only after both routes fail, with an executable restoration condition. |
| T-01-60 | Elevation of Privilege | unauthorized remote publication | high | mitigate | Read-only inspection first; explicit blocking authorization gate before push, PR creation or rerun. |
| T-01-61 | Tampering | code changes hidden after the tested SHA | high | mitigate | Every descendant path through closure must match the explicit documentation/state allowlist; anything else requires a new candidate and full matrix. |
| T-01-62 | Repudiation | new Rust behavior relying on the old resolved review | high | mitigate | `src/**` or `tests/**` remediation stops this plan for a focused gap plan and fresh code-review/fix gate before publication. |
| T-01-SC | Tampering | package-manager installs | low | accept | No new package is planned. Rosetta/rustup setup is runner tooling and must remain confined to CI. |
</threat_model>

<final_verification>
- The final GitHub run's `headSha` equals the recorded CI-tested candidate SHA, and that SHA is an ancestor of closure HEAD with only explicitly allowlisted documentation/state descendants.
- Exactly one outcome marker exists.
- On `normal-four-target`, exactly the four required target jobs execute the named golden step successfully in one run; no required step is skipped.
- On `waiver-three-target`, exactly the other three targets execute the named golden step successfully, both native and Rosetta x86_64 macOS routes have failed/unavailable execution evidence, and all five D-17 fields are present; no four-target claim is made.
- `CONFORMANCE-GATE.md` contains branch-appropriate durable run/job evidence and the committed golden SHA-256.
- Fresh local tests, strict Clippy, cargo-deny, guard proofs and dependency-graph checks pass.
- Fresh `01-VERIFICATION.md` re-evaluates all five ROADMAP truths and all 63 Phase 1 requirements from evidence.
</final_verification>

<success_criteria>
- ROADMAP criterion 4 / GUARD-09 has exactly one terminal disposition: four successful executions for the CI-tested candidate, or three successful executions plus a complete D-17 waiver showing why both x86_64 macOS routes were impossible.
- Windows evidence comes from the CI-tested candidate, not inference from a stale failure or another platform.
- The later closure HEAD descends from that candidate only through the explicit documentation/state allowlist.
- Any Rust/test remediation was implemented in a focused follow-up gap plan and passed a fresh code-review/fix gate before the candidate's matrix run.
- The final phase-verification report has no unresolved implementation or contract gap; the separate legal/provenance attestation remains explicitly human-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/01-conformant-lpcm-bitstream/01-10-SUMMARY.md` recording
the CI-tested candidate SHA, documentation-only closure HEAD and descendant-path check, outcome
branch, branch-appropriate run/job URLs and golden-step conclusions, any Windows/Rosetta remediation
or D-17 waiver, all local verification commands/results, and the final Phase 1 verification verdict.
After committing the summary and final state documents, rerun the ancestry/allowlist command from
Task 4; closure is not complete until it still passes.
</output>
