---
phase: 03-flac-and-opus-framing
plan: 03
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified: [src/error.rs, src/obu/codec_config.rs, src/obu/mod.rs, src/dump.rs, tests/descriptors.rs, tests/error_shape.rs, tests/round_trip.rs, tests/temporal.rs, tests/support/sequence_cases.rs]
autonomous: true
requirements: [CODEC-02, CODEC-03, CODEC-05, CODEC-07]
must_haves:
  truths:
    - "Opus config is exactly 11 big-endian IAMF bytes without the ASCII OpusHead marker."
    - "Roll is checked -ceil(3840/n) and every boundary case is tested."
    - "Fresh Opus accepts only 48 kHz; parsed wrong values remain byte-faithful Findings."
  prohibitions:
    - statement: "MUST NOT expose caller-supplied fresh roll, use Ogg layout, resample, or import a codec crate in src."
  artifacts:
    - path: src/obu/codec_config.rs
      provides: "Typed Opus layout and roll derivation"
      exports: [OpusDecoderConfig, "CodecConfig::opus", required_opus_audio_roll_distance]
---

# Phase 3 Plan 03: Typed Opus Config Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Add dependency-free typed IAMF Opus syntax, rate rejection, and derived roll.
**Architecture:** Production code only models 11 config bytes; roll depends on samples-per-frame and is derived by a fallible constructor.
**Tech Stack:** Rust 1.85, existing bit I/O.
**Spec:** `03-CONTEXT.md` and `03-RESEARCH.md`.

## Global Constraints

- Readers preserve; constructors derive; validation reports all contradictions.
- No Ogg marker, runtime Opus, DSP, tolerance, or resampling.
- Verify pinned sources via git object commands before transcribing them.

<objective>Implement typed Opus config and arithmetic only. Output `03-03-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-02-SUMMARY.md @src/obu/codec_config.rs @tests/descriptors.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pin exact Opus bytes and roll boundaries</name>
  <read_first>
    - Command: `test "$(git -C .reference/iamf-tools rev-parse HEAD)" = 848c6ff4968ff8cc6f728259892ab4f90cb83256 && git -C .reference/iamf-tools show HEAD:iamf/obu/decoder_config/opus_decoder_config.cc`
    - Command: `test "$(git -C .reference/libiamf rev-parse HEAD)" = f06e919e2ad5502a2adc4bdd4e146f2e7e7ffb63 && git -C .reference/libiamf show HEAD:code/src/iamf_dec/opus/IAMF_opus_decoder.c`
    - src/obu/codec_config.rs — FLAC/LPCM faithful pattern
  </read_first>
  <files>tests/descriptors.rs, tests/error_shape.rs, tests/round_trip.rs, tests/temporal.rs, tests/support/sequence_cases.rs</files>
  <interfaces><produces>Tests lock decoder bytes `01 02 01 38 00 00 bb 80 00 00 00`; full id=2/n=960 OBU `00 14 02 4f 70 75 73 c0 07 ff fc ...`; roll table 0/error, 1/-3840, 120/-32, 960/-4, 3839/-2, 3840/-1, 3841/-1, 96000/-1.</produces></interfaces>
  <action>
- [ ] Add RED tests for exact bytes/endian offsets, absence of ASCII marker, roll table, 10-byte Raw, trailing `aa bb`, 44.1-kHz error, fixed-field/pre-skip/rate/roll Findings, dump, and canonical/foreign round trips.
- [ ] Add trim vector `at_end=17, at_start=312` proving existing header writes END before START.
- [ ] Run tests and require missing-API failures.
  </action>
  <verify><automated>cargo test --locked --test descriptors --test error_shape --test round_trip --test temporal</automated><fails_when>RED tests pass before API exists or expected bytes/formula differ from pinned source</fails_when></verify>
  <done>Independent tests specify all Opus syntax/arithmetic.</done>
  <commit>git add tests/descriptors.rs tests/error_shape.rs tests/round_trip.rs tests/temporal.rs tests/support/sequence_cases.rs &amp;&amp; git commit -m "test(03-03): pin Opus config and roll"</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement faithful Opus config</name>
  <read_first>
    - tests/descriptors.rs, tests/error_shape.rs, tests/round_trip.rs, tests/temporal.rs — Task 1's exact RED contracts
    - src/error.rs — compact Error budget
  </read_first>
  <files>src/error.rs, src/obu/codec_config.rs, src/obu/mod.rs, src/dump.rs</files>
  <interfaces><produces>`OpusDecoderConfig { version:u8, output_channel_count:u8, pre_skip:u16, input_sample_rate:u32, output_gain:i16, mapping_family:u8 }`; `CodecConfig::opus(id:u32,n:u32,rate:u32,pre_skip:u16)-&gt;Result&lt;Self&gt;`; `opus_config()-&gt;Option&lt;&amp;OpusDecoderConfig&gt;`; `required_opus_audio_roll_distance(n:u32)-&gt;Result&lt;i16&gt;`.</produces></interfaces>
  <action>
- [ ] Implement 11-byte threshold dispatch and adjacent cited big-endian reader/writer; shorter remains Raw and remainder becomes trailing.
- [ ] Implement roll with `q=3840/n`, add one iff remainder nonzero, checked conversion/negation; reuse ZeroSamplesPerFrame. Constructor fixes version1/channels2/gain0/mapping0, rejects rate !=48000, accepts measured nonzero pre_skip, derives roll.
- [ ] Validate every fixed field, zero pre-skip, rate and recomputed roll without normalization; update exhaustive exports/dump/property matches.
  </action>
  <verify><automated>cargo test --locked --test descriptors --test error_shape --test round_trip --test temporal --test citations &amp;&amp; cargo clippy --locked --all-targets --all-features -- -D warnings</automated><fails_when>byte/roll case fails, fresh roll is caller-controlled, foreign bytes change, or codec dependency/import appears</fails_when></verify>
  <done>CODEC-02/03 and Opus CODEC-05 are exact and faithful.</done>
  <commit>git add src/error.rs src/obu/codec_config.rs src/obu/mod.rs src/dump.rs &amp;&amp; git commit -m "feat(03-03): add typed Opus config"</commit>
</task>
</tasks>
<final_verification>Focused tests, citations, Clippy, exact normal graph, and `git diff --check` pass.</final_verification>
<success_criteria>Typed Opus config/roll/rate behavior is complete without codec code.</success_criteria>
