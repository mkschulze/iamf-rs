---
phase: 03-flac-and-opus-framing
plan: 05
type: tdd
wave: 5
depends_on: ["03-04"]
files_modified: [tests/support/fixture.rs, tests/fixture.rs, tests/conformance.rs, tests/codec_fixtures.rs]
autonomous: true
requirements: [CODEC-02, CODEC-03, CODEC-04, CODEC-05]
must_haves:
  truths:
    - "Opus packets enter Audio Frames unchanged with first start trim L and final end trim E."
    - "Shared gate compares libiamf exactly to standalone decode of the same packets after trims."
    - "Every LPCM and FLAC clause remains unchanged and Opus gets no codec-specific shortcut."
  prohibitions:
    - statement: "MUST NOT compare Opus to source PCM, add tolerance, reorder packets/channels, duplicate trim representation, or skip a shared gate clause."
  artifacts:
    - path: tests/support/fixture.rs
      provides: "fixture::opus using shared adapter"
    - path: tests/conformance.rs
      provides: "Exact shared-gate Opus test"
  key_links:
    - from: tests/fixtures/codecs/opus/packet-000.bin
      to: src/sequence.rs
      via: "fixture::opus passes manifest-ordered packet bytes through FrameSource::PreEncoded into SequenceWriter"
      pattern: "PreEncoded|SequenceWriter"
    - from: tests/fixtures/codecs/opus/expected-s16le.pcm
      to: tests/conformance.rs
      via: "assert_conformant compares libiamf output exactly with standalone decoded-and-trimmed expected PCM"
      pattern: "expected_pcm|differing"
---

# Phase 3 Plan 05: Opus Shared-Gate Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Route the measured Opus corpus through the same SequenceWriter and conformance assertions as LPCM/FLAC.
**Architecture:** Fixture preparation supplies opaque units and independently decoded expected PCM; the gate remains codec-neutral.
**Tech Stack:** Root artifact-only tests, pinned external reference tools when available.
**Spec:** `03-CONTEXT.md` exact-oracle decision and `03-RESEARCH.md` trim flow.

## Global Constraints

- Consume Plan 04's L/P/E/S manifest; do not recalculate from guessed constants.
- Compare exact decoded samples only after frame/channel/rate/count checks.
- Keep END-before-START in existing `Trimming`; no new production trim field.

<objective>Make CODEC-04 observable end-to-end. Output `03-05-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-04-SUMMARY.md @tests/support/fixture.rs @tests/conformance.rs @tests/fixtures/codecs/opus/MANIFEST.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire fixture and trim assertions</name>
  <read_first>
    - tests/support/fixture.rs — Plan 02 adapter
    - tests/codec_fixtures.rs — Plan 04 loader
    - src/obu/header.rs — END-before-START writer
  </read_first>
  <files>tests/support/fixture.rs, tests/fixture.rs, tests/codec_fixtures.rs</files>
  <interfaces><consumes>`CodecConfig::opus`, loader `(L,P,E,S,units,expected_pcm)`, `FrameSource::PreEncoded`.</consumes><produces>`fixture::opus()-&gt;Fixture` with output 48k/16-bit/stereo, P units, first start=L, final end=E.</produces></interfaces>
  <action>
- [ ] RED: assert typed fields/roll, P ordered payloads, first start=L only, last end=E only, middle zero, L!=E, and encoded header bytes decode END then START.
- [ ] GREEN: construct the descriptor/element/presentation and expected PCM from committed loader; pass payloads unchanged through adapter.
- [ ] Parse produced IAMF and prove packet order/payload identity and trim equality per temporal unit.
  </action>
  <verify><automated>cargo test --locked --test fixture --test codec_fixtures --test obu_header --test sequence_parse</automated><fails_when>packet/order differs, trim moves/equalizes, rate/roll differs, or expected PCM comes from source artifact</fails_when></verify>
  <done>Opus fixture encodes exact packet and trim semantics.</done>
  <commit>git add tests/support/fixture.rs tests/fixture.rs tests/codec_fixtures.rs &amp;&amp; git commit -m "test(03-05): frame Opus corpus with priming"</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Exact shared conformance comparison</name>
  <read_first>
    - tests/conformance.rs — assert_conformant clause order and round_trip observables
    - tests/fixtures/codecs/opus/MANIFEST.md — authoritative L/P/E/S and packet order
  </read_first>
  <files>tests/conformance.rs</files>
  <interfaces><consumes>`assert_conformant(&amp;Fixture)` and `fixture::opus()`.</consumes><produces>Named Opus gate test with exact standalone-output comparison.</produces></interfaces>
  <action>
- [ ] RED: add Opus shared-gate test and message assertions naming standalone decode rather than source.
- [ ] GREEN: generalize trim reporting for explicit first/end trims and unit count P without changing clause order. CONF-05 checks file existence, P*960 pre-trim implications through expected S frame count, channels/rate, two decodes/limiter drift, then zero differing samples against committed standalone decode. CONF-06 keeps strict temporal-unit observable.
- [ ] Run serial gate with reference tools when present; otherwise confirm established explicit skips and all offline clauses.
  </action>
  <verify><automated>cargo test --locked --test conformance -- --nocapture --test-threads=1 &amp;&amp; cargo test --locked --all-targets</automated><fails_when>any clause is Opus-skipped/weakened, source/tolerance comparison appears, output count/rate/channels unchecked, or older codec test regresses</fails_when></verify>
  <done>Two independent decoders agree sample-for-sample on the trimmed Opus timeline.</done>
  <commit>git add tests/conformance.rs &amp;&amp; git commit -m "test(03-05): prove Opus through shared gate"</commit>
</task>
</tasks>
<final_verification>Fixture/parser tests, serial gate, full tests, strict Clippy, and `git diff --check` pass.</final_verification>
<success_criteria>CODEC-02/03/04/05 work end-to-end with exact oracle semantics.</success_criteria>
