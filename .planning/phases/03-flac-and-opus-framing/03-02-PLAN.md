---
phase: 03-flac-and-opus-framing
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified: [Cargo.toml, tests/support/fixture.rs, tests/fixture.rs, tests/conformance.rs, tests/codec_fixtures.rs, tools/codec-fixtures/Cargo.toml, tools/codec-fixtures/Cargo.lock, tools/codec-fixtures/tests/support/mod.rs, tools/codec-fixtures/tests/flac_fixtures.rs, tests/fixtures/codecs/flac/]
autonomous: true
requirements: [CODEC-01, CODEC-05, CODEC-07]
must_haves:
  truths:
    - "The root fixture adapter accepts opaque access units and preserves old LPCM bytes/clauses."
    - "FLAC generation pads S=300 to 384, emits exactly three separately serialized 128-frame packets, and trims E=84 after a 384-frame decode."
    - "Root tests load committed artifacts only; even --include-ignored cannot run generation."
  prohibitions:
    - statement: "MUST NOT place generation code or codec dependencies in the root test/package graph, put STREAMINFO/fLaC in Audio Frames, or weaken shared conformance clauses."
  artifacts:
    - path: tools/codec-fixtures/Cargo.toml
      provides: "Excluded generator workspace manifest"
    - path: tests/support/fixture.rs
      provides: "FrameSource/EncodedTemporalUnit shared contract"
    - path: tests/fixtures/codecs/flac/MANIFEST.md
      provides: "Three fixed-block packets and provenance"
  key_links:
    - from: src/obu/codec_config.rs
      to: tests/support/fixture.rs
      via: "DecoderConfig::Flac supplies rate, depth, and frame size to codec-neutral ElementSpec"
      pattern: "FlacDecoderConfig|FixtureCodec::Flac"
    - from: tests/fixtures/codecs/flac/packet-000.bin
      to: src/sequence.rs
      via: "FrameSource::PreEncoded passes each committed packet unchanged into AudioFrame and SequenceWriter"
      pattern: "PreEncoded|SequenceWriter"
---

# Phase 3 Plan 02: Fixture Adapter and FLAC Corpus Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Establish the shared opaque-packet adapter and prove a precisely padded FLAC corpus through Claxon and the Phase 1 gate.

**Architecture:** Generation is a separate Cargo integration-test package excluded from root. Root tests only read immutable packet/PCM/manifest files and pass them to `SequenceWriter`.

**Tech Stack:** Root Rust 1.85; excluded `flacenc = =0.5.1` (defaults off), `claxon = =0.4.3`, `sha2 = =0.10.9`.

**Spec:** `03-CONTEXT.md`; exact fixed-block contract: `03-RESEARCH.md` plus checker-approved S/P/E rules below.

## Global Constraints

- Root `[workspace].exclude` includes `tools/codec-fixtures`; root Cargo.lock and tests contain no codec crate.
- Generator owns `Cargo.toml`, `Cargo.lock`, `tests/support/mod.rs`, and `tests/flac_fixtures.rs`; codec crates are exact `[dev-dependencies]`. Generation and verification run only through explicitly named ignored integration tests.
- FLAC Audio Frames contain one serialized FLAC frame each, never stream marker/metadata.

**Ten-path scope:** The ten frontmatter entries form two deliberately separate coupled contracts:
Task 1 owns only the three root adapter/gate files; Task 2 owns the root workspace exclusion, the
root artifact loader, the four excluded-package manifest/lock/test paths, and the committed FLAC
directory. Task 2 touches adapter/gate files only to construct `fixture::flac()` and invoke the
already-established interface; it does not redesign Task 1, add Opus syntax/corpus, or own guards/CI.

<objective>Add codec-neutral test framing and exact FLAC artifacts/conformance. Output `03-02-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-01-SUMMARY.md @.planning/phases/03-flac-and-opus-framing/03-CONTEXT.md @.planning/phases/03-flac-and-opus-framing/03-RESEARCH.md @tests/support/fixture.rs @tests/conformance.rs</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Codec-neutral committed-access-unit adapter</name>
  <read_first>
    - tests/support/fixture.rs — current ElementSpec/Fixture/packing/SequenceWriter path
    - tests/conformance.rs — clause order and observables
    - src/obu/audio_frame.rs — opaque payload and Trimming
  </read_first>
  <files>tests/support/fixture.rs, tests/fixture.rs, tests/conformance.rs</files>
  <interfaces><produces>`EncodedTemporalUnit { trimming: Option&lt;Trimming&gt;, substream_payloads: Vec&lt;Vec&lt;u8&gt;&gt; }`; `FrameSource::{LpcmInterleaved(Vec&lt;i32&gt;),PreEncoded(Vec&lt;EncodedTemporalUnit&gt;)}`; `FixtureCodec::{Lpcm{big_endian:bool},Flac,Opus}`; `Fixture { expected_pcm, frame_sources }`; `assert_conformant(&amp;Fixture)`.</produces></interfaces>
  <action>
- [x] RED: prove LPCM bytes unchanged; pre-encoded bytes copied exactly; mismatched element/source/unit/substream counts error; every substream in a unit receives identical trim.
- [x] GREEN: refactor ElementSpec/Fixture exactly to the produced types, retain `single_pcm()` as first expected buffer, preserve LPCM packing byte-for-byte, and let only `PreEncoded` bypass sample packing. Generalize gate inputs without changing clause 0→CONF-06 order, exact comparison, limiter checks, or skip reasons.
- [x] Run old fixture/golden/conformance tests before commit.
  </action>
  <verify><automated>cargo test --locked --test fixture --test golden &amp;&amp; cargo test --locked --test conformance -- --nocapture --test-threads=1</automated><fails_when>LPCM bytes/clauses drift, packet bytes are inspected/transformed, shape errors pass, or a clause weakens</fails_when></verify>
  <done>The adapter is codec-neutral and all prior proofs remain intact.</done>
  <commit>git add tests/support/fixture.rs tests/fixture.rs tests/conformance.rs &amp;&amp; git commit -m "refactor(03-02): accept committed access units"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Excluded FLAC generator, exact three-packet corpus, and conformance</name>
  <read_first>
    - CODEC-DEPENDENCY-PREFLIGHT.md — accepted versions/licences
    - Command: `test "$(git -C .reference/iamf-tools rev-parse HEAD)" = 848c6ff4968ff8cc6f728259892ab4f90cb83256 && git -C .reference/iamf-tools show HEAD:iamf/obu/decoder_config/flac_decoder_config.cc`
    - Local crate API: `$CARGO_HOME/registry/src/*/flacenc-0.5.1/src/component/datatype.rs` — `Stream::frame`, `frame_count`, `Frame::header`, `BitRepr::write`
  </read_first>
  <files>Cargo.toml, tools/codec-fixtures/Cargo.toml, tools/codec-fixtures/Cargo.lock, tools/codec-fixtures/tests/support/mod.rs, tools/codec-fixtures/tests/flac_fixtures.rs, tests/fixtures/codecs/flac/, tests/codec_fixtures.rs, tests/support/fixture.rs, tests/conformance.rs</files>
  <interfaces><produces>Generation command `CODEC_FIXTURE_OUTPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact generate_flac_corpus`; verification command `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact verify_flac_corpus`; root `fixture::flac()` consumes exactly three packets plus expected PCM.</produces></interfaces>
  <action>
- [x] Add `tools/codec-fixtures` to root workspace `exclude`. Create the independent package with `[workspace] members=["."]`, edition 2024, rust-version 1.85, publish=false, licence MIT OR Apache-2.0, no library/binary target, and exact `[dev-dependencies]`: `flacenc = { version = "=0.5.1", default-features = false }`, `claxon = "=0.4.3"`, `opus = "=0.4.0"`, and `sha2 = "=0.10.9"`. Add explicit `[[test]] name="flac_fixtures" path="tests/flac_fixtures.rs"`; Plan 04 adds the Opus test entry and path atomically. Commit the package lock. Do not create its deny.toml until Plan 06; Plans 02/04 run cargo-deny with repository `deny.toml` explicitly.
- [x] RED root tests: require manifest digests, exactly packets 000..002, no `fLaC`, three decoded header block sizes of 128, 384 decoded frames before trim, E=84, and exact 300-frame expected output.
- [x] Define `#[test] #[ignore = "explicit codec fixture command only"] fn generate_flac_corpus()`; require `CODEC_FIXTURE_OUTPUT`; author S=300 stereo frames, append exactly E=84 zero frames (384 total), call flacenc fixed-block encoding with block size 128, require `stream.frame_count()==3`, for i=0..2 require `stream.frame(i).header().block_size()==128`, and serialize each frame separately with `BitRepr::write` into one packet file. Write source/expected s16le PCM and manifest versions/settings/order/S=300/padded=384/E=84/digests.
- [x] Define `#[test] #[ignore = "explicit codec fixture command only"] fn verify_flac_corpus()`; require `CODEC_FIXTURE_INPUT`, reconstruct a temporary `fLaC`+STREAMINFO+three-frame stream, decode 384 with Claxon, remove final 84 frames, and assert exactly 300 frames/sample equality. Root artifact test independently parses/digests committed files; `fixture::flac()` sends the three frames with final `trim_at_end=84` through shared gate.
  </action>
  <verify><automated>CODEC_FIXTURE_INPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact verify_flac_corpus &amp;&amp; cargo deny --manifest-path tools/codec-fixtures/Cargo.toml --config deny.toml check &amp;&amp; cargo test --locked --test codec_fixtures --test fixture &amp;&amp; cargo test --locked --test conformance -- --nocapture --test-threads=1</automated><fails_when>not exactly 3×128 decoded frames, trim not exactly 84→300, frame serialization includes metadata, root tests generate, or exact PCM/conformance differs</fails_when></verify>
  <done>FLAC has mathematically fixed packetization and exact lossless conformance.</done>
  <commit>git add Cargo.toml tools/codec-fixtures tests/fixtures/codecs/flac tests/codec_fixtures.rs tests/support/fixture.rs tests/conformance.rs &amp;&amp; git commit -m "test(03-02): add fixed-block FLAC corpus"</commit>
</task>
</tasks>

<final_verification>Root all-target tests (including `--include-ignored`) never invoke generation; tool verify/deny, exact normal graph, strict Clippy, and serial conformance pass.</final_verification>
<success_criteria>CODEC-01/05/07 have fixed-block artifact, independent decode, and shared-gate evidence.</success_criteria>
