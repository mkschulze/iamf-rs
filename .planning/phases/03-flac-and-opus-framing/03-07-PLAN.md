---
phase: 03-flac-and-opus-framing
plan: 07
type: tdd
wave: 7
depends_on: ["03-06"]
files_modified: [tests/descriptors.rs, tests/error_shape.rs, tests/round_trip.rs, tests/temporal.rs, tests/sequence_parse.rs, tests/fuzz_regression.rs, tests/codec_fixtures.rs, tests/fixtures/codecs/README.md]
autonomous: true
requirements: [CODEC-01, CODEC-02, CODEC-03, CODEC-04, CODEC-05, CODEC-06, CODEC-07]
must_haves:
  truths:
    - "Every typed/raw/trailing/rate/roll/trim/parser/property/artifact case has a named offline regression."
    - "Root tests replay but never mutate immutable packets, PCM, or manifests."
    - "Regeneration is an explicit excluded-workspace command with review and verification steps."
  prohibitions:
    - statement: "MUST NOT regenerate in root tests, omit truncation boundaries, mutate artifacts, or treat README as an immutable generated artifact."
  artifacts:
    - path: tests/fixtures/codecs/README.md
      provides: "Requirement matrix and explicit regeneration procedure"
    - path: tests/fuzz_regression.rs
      provides: "Stable codec-shape replay"
---

# Phase 3 Plan 07: Offline Codec Regression Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Close every Phase 3 behavior with deterministic offline tests and prove tests do not rewrite committed artifacts.
**Architecture:** Named test matrix spans syntax through corpus; immutable-file SHA-256 inventories bracket test execution.
**Tech Stack:** Existing integration/property/fuzz-regression tests and sha2 0.10.9 test helper.
**Spec:** Validation Architecture table in `03-RESEARCH.md` and decisions in `03-CONTEXT.md`.

## Global Constraints

- Root tests use only committed artifacts; generator commands live only in documentation/excluded tool.
- Immutable inventory includes only `packet-*.bin`, `*.pcm`, and `MANIFEST.md`; exclude README and generated scratch output.
- Parser truncation must remain bounded/non-panicking and byte-faithful.

<objective>Produce complete offline coverage and immutable replay proof. Output `03-07-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-06-SUMMARY.md @.planning/phases/03-flac-and-opus-framing/03-CONTEXT.md @.planning/phases/03-flac-and-opus-framing/03-RESEARCH.md @tests/codec_fixtures.rs @tests/fuzz_regression.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Requirement matrix and missing hostile/property cases</name>
  <read_first>
    - tests/descriptors.rs — exact config vectors
    - tests/round_trip.rs — model/byte properties
    - tests/sequence_parse.rs — bounded parser/error-offset patterns
    - tests/fuzz_regression.rs — stable corpus replay
  </read_first>
  <files>tests/descriptors.rs, tests/error_shape.rs, tests/round_trip.rs, tests/temporal.rs, tests/sequence_parse.rs, tests/fuzz_regression.rs, tests/codec_fixtures.rs, tests/fixtures/codecs/README.md</files>
  <interfaces><produces>README table maps CODEC-01..07 to exact test names; canonical generators include legal FLAC/Opus/trailing cases.</produces></interfaces>
  <action>
- [ ] Inventory `cargo test -- --list`; write rows for FLAC38, Opus11, both short Raw, both trailing, own typed and foreign round trip, fixed-field/rate/roll preservation, FLAC three typed errors, Opus rate error, roll table, L/E trims, all digests/order, both independent decodes, dependency guard, sequence and fuzz replay.
- [ ] Add only missing named tests. For each codec, parse whole sequences and truncate at every byte of its fixed prefix: bounded config shorter than 38/11 stays Raw; enclosing OBU truncation remains a positioned structural error; no panic/read-through.
- [ ] Extend canonical property/fuzz generation with bounded legal typed configs and trailing bytes, preserving existing corpus behavior.
- [ ] Document all four commands verbatim: `CODEC_FIXTURE_OUTPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact generate_flac_corpus`; `CODEC_FIXTURE_OUTPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact generate_opus_corpus`; `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact verify_flac_corpus`; and `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact verify_opus_corpus`. Include exact tool versions, artifact review/digest steps, `git diff --binary`, and the fact root/`--include-ignored` tests never reach the excluded package.
  </action>
  <verify><automated>cargo test --locked --test descriptors --test error_shape --test round_trip --test temporal --test sequence_parse --test codec_fixtures --test codec_dependency_boundary &amp;&amp; cargo test --locked --features fuzzing --test fuzz_regression</automated><fails_when>matrix row lacks a named test, truncation panics/misclassifies, property omits a variant, or docs allow implicit generation</fails_when></verify>
  <done>All seven requirements have named offline coverage.</done>
  <commit>git add tests/descriptors.rs tests/error_shape.rs tests/round_trip.rs tests/temporal.rs tests/sequence_parse.rs tests/fuzz_regression.rs tests/codec_fixtures.rs tests/fixtures/codecs/README.md &amp;&amp; git commit -m "test(03-07): close codec framing regressions"</commit>
</task>
<task type="auto" tdd="false">
  <name>Task 2: SHA-256 immutability proof around full root tests</name>
  <read_first>
    - tests/codec_fixtures.rs — SHA helper and artifact loader
    - tests/fixtures/codecs/README.md — immutable file definition
  </read_first>
  <files>tests/codec_fixtures.rs</files>
  <interfaces><produces>`codec_artifact_sha256_inventory()` sorted by relative path, filtering exactly packet-*.bin/*.pcm/MANIFEST.md; named test writes it only when optional `CODEC_SHA256_INVENTORY` is set.</produces></interfaces>
  <action>
- [ ] Add a root test/helper that produces sorted `(relative_path, SHA-256)` inventory for immutable artifacts only; explicitly exclude README.
- [ ] Make the named inventory test always validate a non-empty sorted inventory and write it only when optional `CODEC_SHA256_INVENTORY` is set, so ordinary and `--include-ignored` runs stay green. Before the full command, set it to `target/conformance/codec-artifacts.before.sha256`; run tests; set it to the after path; compare byte-for-byte. Use Rust/sha2 for cross-platform hashing, not platform `sha256sum`.
- [ ] Repeat with `cargo test --locked -- --include-ignored` to prove no hidden root generator mutates artifacts.
  </action>
  <verify><automated>CODEC_SHA256_INVENTORY=target/conformance/codec-artifacts.before.sha256 cargo test --locked --test codec_fixtures write_sha256_inventory &amp;&amp; cargo test --locked --all-targets &amp;&amp; cargo test --locked -- --include-ignored &amp;&amp; CODEC_SHA256_INVENTORY=target/conformance/codec-artifacts.after.sha256 cargo test --locked --test codec_fixtures write_sha256_inventory &amp;&amp; cmp target/conformance/codec-artifacts.before.sha256 target/conformance/codec-artifacts.after.sha256</automated><fails_when>inventory includes README/scratch, excludes packet/PCM/manifest, tests mutate an artifact, or hidden ignored generation runs</fails_when></verify>
  <done>Root test execution is proven artifact-only and immutable by SHA-256.</done>
  <commit>git add tests/codec_fixtures.rs &amp;&amp; git commit -m "test(03-07): prove codec artifacts immutable"</commit>
</task>
</tasks>
<final_verification>Targeted/full/ignored/fuzz tests pass and pre/post SHA-256 inventories match; strict Clippy, deny, guard scripts, exact graph, and `git diff --check` pass.</final_verification>
<success_criteria>All CODEC requirements are covered offline without fixture mutation.</success_criteria>
