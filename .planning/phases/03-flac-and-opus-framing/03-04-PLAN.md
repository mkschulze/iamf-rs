---
phase: 03-flac-and-opus-framing
plan: 04
type: tdd
wave: 4
depends_on: ["03-03"]
files_modified: [tools/codec-fixtures/Cargo.toml, tools/codec-fixtures/Cargo.lock, tools/codec-fixtures/tests/support/mod.rs, tools/codec-fixtures/tests/opus_fixtures.rs, tests/fixtures/codecs/opus/, tests/codec_fixtures.rs]
autonomous: true
requirements: [CODEC-02, CODEC-04, CODEC-06, CODEC-07]
must_haves:
  truths:
    - "Opus corpus obeys L=query-lookahead, P=ceil((S+L)/960), E=P*960-L-S with P>=2, 0<E<960, and E!=L."
    - "Generator feeds exactly P*960 zero-padded frames, emits P packets, decodes P*960, removes L then E, and yields exactly S frames."
    - "Root tests replay artifacts only; generation remains in an excluded integration-test package."
  prohibitions:
    - statement: "MUST NOT hard-code 312 as protocol behavior, compare lossy output to source PCM, add tolerance/Ogg wrapping, or run generation from root tests."
  artifacts:
    - path: tools/codec-fixtures/tests/opus_fixtures.rs
      provides: "Explicit Opus corpus generation/verification"
    - path: tests/fixtures/codecs/opus/MANIFEST.md
      provides: "L/P/E, packet order/settings/digests and exact expected PCM"
---

# Phase 3 Plan 04: Exact Opus Corpus Generation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Generate committed Opus packets and standalone expected PCM with explicit priming/padding arithmetic.
**Architecture:** Explicit ignored integration tests in the excluded package query libopus and create/verify artifacts; root tests only parse/digest/replay them.
**Tech Stack:** excluded package `[dev-dependencies]` with `opus = =0.4.0`, opusic-sys 0.7.5, and SHA-256.
**Spec:** `03-CONTEXT.md`, `03-RESEARCH.md`, formulas below.

## Global Constraints

- Frame size N=960, stereo 48 kHz; source deterministic/distinguishable and below limiter threshold.
- L is queried for this encoder instance; choose S only after L is known so constraints hold.
- Expected PCM is standalone decode of exact packets after trims, never source PCM.

<objective>Create mathematically specified Opus artifacts. Output `03-04-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-03-SUMMARY.md @tools/codec-fixtures/Cargo.toml @tools/codec-fixtures/tests/support/mod.rs @tests/codec_fixtures.rs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED artifact contract</name>
  <read_first>
    - tests/fixtures/codecs/flac/MANIFEST.md — manifest schema
    - Command: `test "$(git -C .reference/iamf-tools rev-parse HEAD)" = 848c6ff4968ff8cc6f728259892ab4f90cb83256 && git -C .reference/iamf-tools show HEAD:iamf/cli/codec/opus_encoder.cc`
    - Command: `test "$(git -C .reference/iamf-tools rev-parse HEAD)" = 848c6ff4968ff8cc6f728259892ab4f90cb83256 &amp;&amp; git -C .reference/iamf-tools show HEAD:iamf/cli/proto_conversion/proto_to_obu/audio_frame_generator.cc`
  </read_first>
  <files>tests/codec_fixtures.rs</files>
  <interfaces><produces>Root loader returns manifest L/P/E/S, `Vec&lt;EncodedTemporalUnit&gt;`, and exact `Vec&lt;i32&gt;`; it checks all digests/order and trim positions.</produces></interfaces>
  <action>
- [x] RED: require L&gt;0; P=(S+L).div_ceil(960)&gt;=2; E=P*960-L-S; 0&lt;E&lt;960; E!=L; exactly P nonempty packets; first start trim=L; final end trim=E; middle trims zero; expected length S stereo frames; no OpusHead.
- [x] Run root artifact test and confirm absence failure.
  </action>
  <verify><automated>cargo test --locked --test codec_fixtures opus -- --nocapture</automated><fails_when>test passes without corpus or omits any formula/order/digest assertion</fails_when></verify>
  <done>Artifact contract rejects wrong priming/padding or packet count.</done>
  <commit>git add tests/codec_fixtures.rs &amp;&amp; git commit -m "test(03-04): specify Opus corpus arithmetic"</commit>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Generate and independently decode exact packets</name>
  <read_first>
    - CODEC-DEPENDENCY-PREFLIGHT.md — selected Opus version/licence/toolchain result
    - tests/codec_fixtures.rs — Task 1 loader and L/P/E/S contract
    - tools/codec-fixtures/tests/flac_fixtures.rs — ignored exact-test/error/output conventions
  </read_first>
  <files>tools/codec-fixtures/Cargo.toml, tools/codec-fixtures/Cargo.lock, tools/codec-fixtures/tests/support/mod.rs, tools/codec-fixtures/tests/opus_fixtures.rs, tests/fixtures/codecs/opus/</files>
  <interfaces><produces>Generation command `CODEC_FIXTURE_OUTPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact generate_opus_corpus`; verification command `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact verify_opus_corpus`.</produces></interfaces>
  <action>
- [x] Add `[[test]] name="opus_fixtures" path="tests/opus_fixtures.rs"` atomically with that path. Define `#[test] #[ignore = "explicit codec fixture command only"] fn generate_opus_corpus()`; require `CODEC_FIXTURE_OUTPUT`, query encoder lookahead L, select and record S satisfying all Task 1 constraints, compute P by quotient/remainder ceiling and E exactly, feed exactly P*960 stereo frames with zeros after S, and require exactly P encoded packets.
- [x] Define `#[test] #[ignore = "explicit codec fixture command only"] fn verify_opus_corpus()`; require `CODEC_FIXTURE_INPUT`; from a fresh decoder, decode the P packets in order and require exactly P*960 frames before trimming. Remove first L and last E frames; require exactly S remain. Generation serializes that result as expected-s16le; source-s16le is provenance only.
- [x] Write packet-NNN.bin and manifest with versions/settings/input hash/order/length/hash/L/S/P/E/output hash and explicit regeneration command. Never write Ogg/OpusHead.
- [x] Run excluded verify twice and root RED test; both decodes/digests must agree exactly.
  </action>
  <verify><automated>CODEC_FIXTURE_INPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact verify_opus_corpus &amp;&amp; cargo deny --manifest-path tools/codec-fixtures/Cargo.toml --config deny.toml check &amp;&amp; cargo test --locked --test codec_fixtures opus -- --nocapture</automated><fails_when>any formula, packet count, decoded-before-trim count, exact S result, digest, or fresh-decoder equality fails</fails_when></verify>
  <done>Opus packet corpus has measured, non-symmetric priming and exact standalone output.</done>
  <commit>git add tools/codec-fixtures/Cargo.toml tools/codec-fixtures/Cargo.lock tools/codec-fixtures/tests tests/fixtures/codecs/opus tests/codec_fixtures.rs &amp;&amp; git commit -m "test(03-04): generate exact Opus corpus"</commit>
</task>
</tasks>
<final_verification>Tool verify/deny and root artifact-only tests pass; `--include-ignored` cannot execute generator.</final_verification>
<success_criteria>CODEC-04 corpus arithmetic and exact oracle are permanently recorded.</success_criteria>
