---
phase: 03-flac-and-opus-framing
plan: 06
type: tdd
wave: 6
depends_on: ["03-05"]
files_modified: [tools/check-codec-dependency-boundary.sh, tests/codec_dependency_boundary.rs, tools/codec-fixtures/deny.toml]
autonomous: true
requirements: [CODEC-06, CODEC-07]
must_haves:
  truths:
    - "A demonstrated canary fails when a codec enters root dependencies/src; the real tree passes."
    - "Root normal graph remains exactly iamf+thiserror; codec tools live only in excluded tools/codec-fixtures."
    - "Root, fuzz, and generator graphs pass their scoped deny policies."
  prohibitions:
    - statement: "MUST NOT weaken deny/lints, add root codec dev-dependencies/default features, or scan harmless codec ID strings as imports."
  artifacts:
    - path: tools/check-codec-dependency-boundary.sh
      provides: "Executable CODEC-07 boundary"
    - path: tests/codec_dependency_boundary.rs
      provides: "Portable stable canary and real-tree assertions"
    - path: tools/codec-fixtures/deny.toml
      provides: "Generator-only licence/advisory/source policy finalized after its lock exists"
  key_links:
    - from: Cargo.toml
      to: tools/check-codec-dependency-boundary.sh
      via: "guard enforces root dependency sections, workspace exclusion, source imports, lock, and normal graph"
      pattern: "codec-fixtures|normal,no-proc-macro"
    - from: tools/codec-fixtures/Cargo.toml
      to: tools/codec-fixtures/deny.toml
      via: "cargo-deny checks exact generator dev-dependencies against the scoped tool policy"
      pattern: "dev-dependencies|licenses"
---

# Phase 3 Plan 06: Codec Dependency Guard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Turn the shipping codec-free claim into a guard proven to reject deliberate violations.
**Architecture:** Shell enforces Cargo/source/graph shape in Linux guard jobs; a shell-independent Rust integration test implements the same manifest/source checks and builds isolated canaries portably on every target.
**Tech Stack:** Cargo metadata/tree, cargo-deny, Rust filesystem/process APIs.
**Spec:** CODEC-06/07 in `REQUIREMENTS.md`; approved boundary in `03-CONTEXT.md`.

## Global Constraints

- Root codec dependencies are forbidden in both dependencies and dev-dependencies; excluded generator is the only codec graph.
- Root workspace exclude must name `tools/codec-fixtures`; root Cargo.lock must contain no codec packages.
- Scans target import syntax, not comments/constants such as `CODEC_ID_OPUS`.

<objective>Install and prove the dependency boundary. Output `03-06-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-05-SUMMARY.md @Cargo.toml @tools/prove-guards.sh @CODEC-DEPENDENCY-PREFLIGHT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED canaries then boundary script</name>
  <read_first>
    - Cargo.toml and Cargo.lock — root package/workspace/lock boundary
    - tools/prove-guards.sh — isolated safe-canary pattern
  </read_first>
  <files>tools/check-codec-dependency-boundary.sh, tests/codec_dependency_boundary.rs</files>
  <interfaces><produces>`check-codec-dependency-boundary.sh --root PATH`; the Rust test creates isolated manifest/source canaries for root dependency and src import without invoking bash.</produces></interfaces>
  <action>
- [ ] RED Rust test requires real tree pass and two canaries fail with messages naming `[dependencies]`/`opus` and `src` import respectively; it uses only std filesystem/process APIs and never invokes the shell script, so it executes on Windows MSVC.
- [ ] Implement script: validate explicit root; assert root manifest/lock exclude all selected codec/resampler packages, workspace exclude contains generator, scan `use`/`extern crate` syntax, and compare locked normal/no-proc graph sorted names to exactly iamf/thiserror.
- [ ] Give the shell script a `--self-test` that creates/removes only a validated temporary canary and proves its own checks bite. Ensure both Rust and shell canary cleanup target only their validated temporary directories; run both against the real tree.
  </action>
  <verify><automated>cargo test --locked --test codec_dependency_boundary -- --nocapture &amp;&amp; bash tools/check-codec-dependency-boundary.sh --self-test &amp;&amp; bash tools/check-codec-dependency-boundary.sh</automated><fails_when>canary passes, error does not name breach, harmless literal fails, real tree fails, Rust test requires bash, or graph is not exact</fails_when></verify>
  <done>CODEC-07 is demonstrated, not merely configured.</done>
  <commit>git add tools/check-codec-dependency-boundary.sh tests/codec_dependency_boundary.rs &amp;&amp; git commit -m "test(03-06): enforce codec dependency boundary"</commit>
</task>
<task type="auto" tdd="false">
  <name>Task 2: Close all scoped licence graphs</name>
  <read_first>
    - deny.toml — root policy
    - fuzz/deny.toml — fuzz-only policy
    - CODEC-DEPENDENCY-PREFLIGHT.md — approved resolved licences
  </read_first>
  <files>tools/codec-fixtures/deny.toml</files>
  <interfaces><produces>Three passing scoped deny commands without broadening root policy.</produces></interfaces>
  <action>
- [ ] Create `tools/codec-fixtures/deny.toml` now that the generator Cargo.lock exists: copy the repository's graph/advisory/ban/source settings and licence allow-list, document that it governs only the excluded integration-test package, and add only any already-preflighted package-scoped exception with its exact crate/version/reason.
- [ ] Run root licenses and advisories/bans/sources; run fuzz all-features check; run generator all checks from its manifest and newly committed scoped config. Never broaden root policy or ignore an advisory without stopping for a decision.
- [ ] Confirm root lock contains no claxon/flacenc/opus/opusic and generator lock pins exact selections.
  </action>
  <verify><automated>cargo deny check licenses &amp;&amp; cargo deny check advisories bans sources &amp;&amp; cargo deny --manifest-path fuzz/Cargo.toml --config fuzz/deny.toml --all-features check &amp;&amp; cargo deny --manifest-path tools/codec-fixtures/Cargo.toml --config tools/codec-fixtures/deny.toml check</automated><fails_when>any deny fails/is weakened or codec reaches root lock</fails_when></verify>
  <done>CODEC-06/07 graphs are separated and licence-clean.</done>
  <commit>git add tools/codec-fixtures/deny.toml &amp;&amp; git commit -m "chore(03-06): add scoped codec-tool policy"</commit>
</task>
</tasks>
<final_verification>Boundary/canaries, three deny scopes, exact normal graph, guard proof, and `git diff --check` pass.</final_verification>
<success_criteria>Codec dependency isolation is machine-enforced and licence-clean.</success_criteria>
