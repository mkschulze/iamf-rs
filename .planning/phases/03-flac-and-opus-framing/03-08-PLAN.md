---
phase: 03-flac-and-opus-framing
plan: 8
type: tdd
wave: 8
depends_on: ["03-07"]
files_modified: [.github/workflows/ci.yml, .github/workflows/reference.yml]
autonomous: true
requirements: [CODEC-01, CODEC-02, CODEC-03, CODEC-04, CODEC-05, CODEC-06, CODEC-07]
must_haves:
  truths:
    - "Four stable targets run config, parser, artifact, fuzz-regression, and dependency-boundary tests from the root codec-free graph."
    - "Pinned Linux reference CI runs serial shared conformance and uploads target/conformance evidence."
    - "Generator workspace is verified separately on Linux and never invoked by root tests/reference --include-ignored."
  prohibitions:
    - statement: "MUST NOT remove/skip a target, weaken Rosetta/Windows/MSRV/guard steps, infer reference success from exit code, or allow generator failure to alter shipping architecture."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "Four-target offline Phase 3 gates"
    - path: .github/workflows/reference.yml
      provides: "Pinned reference and generator verification with uploaded conformance evidence"
  key_links:
    - from: .github/workflows/ci.yml
      to: tools/check-codec-dependency-boundary.sh
      via: "Linux guardrail job runs boundary self-test/real-tree check and scoped generator deny"
      pattern: "check-codec-dependency-boundary|tools/codec-fixtures/Cargo.toml"
    - from: .github/workflows/ci.yml
      to: tools/codec-fixtures/tests/
      via: "Linux invokes only exact ignored verify_flac_corpus and verify_opus_corpus integration tests"
      pattern: "--ignored --exact verify_"
    - from: .github/workflows/reference.yml
      to: tests/conformance.rs
      via: "pinned manifest-first job runs serial shared conformance and uploads target/conformance"
      pattern: "test conformance|target/conformance/"
---

# Phase 3 Plan 08: CI and Reference Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Put the entire codec-free offline proof on four targets and external conformance on pinned Linux.
**Architecture:** Root matrix never compiles codec tooling; separate Linux commands compile the excluded integration-test package and invoke only its exact ignored verification tests. Reference job retains manifest-first observable-based semantics and archives codec evidence.
**Tech Stack:** GitHub Actions, Rust 1.85, Rosetta, cargo-deny, pinned libiamf/iamf-tools.
**Spec:** `03-RESEARCH.md` final gate matrix and `03-CONTEXT.md` dependency/oracle policy.

## Global Constraints

- Preserve exactly four targets, Windows LF handling, Rosetta execution, toolchain assertion, golden and stable fuzz steps.
- Root tests are committed-artifact-only; excluded generator is separately checked/denied on Linux.
- Reference conformance stays serial and manifest identity precedes every verdict.

<objective>Close cross-target and external-reference risk. Output `03-08-SUMMARY.md`.</objective>
<execution_context>@~/.claude/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-flac-and-opus-framing/03-07-SUMMARY.md @.github/workflows/ci.yml @.github/workflows/reference.yml @tests/conformance.rs</context>
<tasks>
<task type="auto" tdd="false">
  <name>Task 1: Four-target root gates and Linux generator gate</name>
  <read_first>
    - .github/workflows/ci.yml — matrix/Rosetta/LF/MSRV/graph/fuzz structure
    - tools/check-codec-dependency-boundary.sh — root boundary command
    - tools/codec-fixtures/Cargo.toml — excluded integration-test package with exact codec dev-dependencies
    - tools/codec-fixtures/tests/flac_fixtures.rs and tools/codec-fixtures/tests/opus_fixtures.rs — exact ignored verification test names
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <interfaces><produces>Named matrix codec regression step plus Linux boundary and generator check/deny/verify steps.</produces></interfaces>
  <action>
- [ ] Add a matrix step for descriptors/error/round-trip/temporal/sequence_parse/codec_fixtures/codec_dependency_boundary using `--target ${{ matrix.target }}`; preserve all current matrix behavior.
- [ ] In Linux guardrails run the boundary script, `cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --no-run`, generator cargo-deny with `tools/codec-fixtures/deny.toml`, `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/flac cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test flac_fixtures -- --ignored --exact verify_flac_corpus`, and `CODEC_FIXTURE_INPUT=tests/fixtures/codecs/opus cargo test --locked --manifest-path tools/codec-fixtures/Cargo.toml --test opus_fixtures -- --ignored --exact verify_opus_corpus`. Never invoke either `generate_*` test or rewrite committed output.
- [ ] Fallback: if bundled Opus cannot build on a target, keep root matrix unchanged (it has no codec dependency) and keep generator verification Linux-only; committed expected PCM + SHA provenance + libiamf comparison remain the cross-target evidence. Never add a root/platform codec feature.
  </action>
  <verify><automated>test "$(rg -c '^          - os:' .github/workflows/ci.yml)" -eq 4 &amp;&amp; rg -n 'codec_fixtures|codec_dependency_boundary|check-codec-dependency-boundary|--test flac_fixtures.*--ignored --exact verify_flac_corpus|--test opus_fixtures.*--ignored --exact verify_opus_corpus' .github/workflows/ci.yml</automated><fails_when>target/legacy step removed, root matrix compiles generator, CI invokes generate_* or rewrites artifacts, an exact ignored verify command is absent, or fallback changes shipping graph</fails_when></verify>
  <done>Four targets replay codec evidence while Linux separately checks generator portability/policy.</done>
  <commit>git add .github/workflows/ci.yml &amp;&amp; git commit -m "ci(03-08): gate codec framing on four targets"</commit>
</task>
<task type="auto" tdd="false">
  <name>Task 2: Pinned serial reference closure and evidence upload</name>
  <read_first>
    - .github/workflows/reference.yml — manifest-first ordering, include-ignored, container and upload
    - tests/conformance.rs — named FLAC/Opus tests and observable checks
  </read_first>
  <files>.github/workflows/reference.yml</files>
  <interfaces><produces>Serial conformance after pinned libiamf and after iamf-tools image; upload includes `target/conformance/`.</produces></interfaces>
  <action>
- [ ] Keep reference manifest assertion before conformance. Broad `cargo test --locked -- --include-ignored` is now safe because root has no ignored generator; retain it for reference-only tests.
- [ ] Run `cargo test --locked --test conformance -- --nocapture --test-threads=1` after decoder setup and after container setup as current semantics require. Ensure logs include FLAC and Opus tags, exact PCM verdicts, temporal-unit observable and no exit-code-only success.
- [ ] Add `target/conformance/` to the existing always-uploaded reference artifact paths alongside manifest/log/output artifacts.
- [ ] Inspect workflow for pinned actions/SHAs, 90-minute bound, manifest-first order, serial command, explicit skip semantics, and upload-on-failure.
  </action>
  <verify><automated>rg -n 'test conformance.*nocapture.*test-threads=1' .github/workflows/reference.yml &amp;&amp; rg -n 'target/conformance/' .github/workflows/reference.yml &amp;&amp; cargo test --locked --all-targets &amp;&amp; cargo clippy --locked --all-targets --all-features -- -D warnings</automated><fails_when>manifest runs late, conformance parallelizes, codec tag/evidence absent, output inferred from exit code, upload misses target/conformance, or broad ignored tests can generate</fails_when></verify>
  <done>Pinned external tools prove both codec paths and retain failure evidence.</done>
  <commit>git add .github/workflows/reference.yml &amp;&amp; git commit -m "ci(03-08): close codec reference evidence"</commit>
</task>
</tasks>
<final_verification>Full root tests/ignored/fuzz, strict Clippy, three deny scopes, both guards, exact graph, excluded verify commands, serial conformance when available, workflow inspection, SHA inventory match, and `git diff --check` pass.</final_verification>
<success_criteria>CODEC-01..07 are continuously enforced on stable and pinned-reference paths.</success_criteria>
