---
phase: quick-260913-th8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/error.rs
  - src/encoder.rs
  - src/obu/mix_presentation.rs
  - tests/encoder_builder.rs
  - tests/error_shape.rs
  - tests/encoder_streaming.rs
  - tests/sequence_parse.rs
  - tests/support/reference_expectations.rs
  - HANDOFF.md
  - .planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-deferred-items.md
autonomous: true
requirements: [API-01, API-04, DESC-05, CONF-05, CONF-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "EncoderBuilder::build() rejects a Mix Presentation declaration that lists the same Audio Element handle twice (in one sub-mix or across sub-mixes) with ErrorKind::DuplicateMixPresentationAudioElement at Location::Field(\"mix_presentation.audio_elements\"). The check runs after SubMixCountNotOne and ReservedHeadphonesRenderingMode and before validate_findings, so those two kinds still win and a duplicate never surfaces as InvalidDescriptorReference"
    - "build() validates a clone of each presentation template whose audio_element_id values are lowered from the handles, so two distinct handles whose template ids are both 0 build Ok, and the Parallax delivery fixture (placeholder id 0) still builds"
    - "EncoderBuilder::build() with no Mix Presentation returns ErrorKind::NoMixPresentation at Location::Field(\"mix_presentations\"), checked last in validate_declarations so every earlier-error test keeps its kind. Both new kinds are payload-free, listed in tests/error_shape.rs, and size_of::<Error>() <= 32 still holds"
    - "MixPresentation::validate() reports, in this order: the num_sub_mixes finding at Field(\"num_sub_mixes\") for 0 or n > 1 sub-mixes; one finding at Field(\"sub_mix.audio_element_id\") per later repeat of an audio_element_id across all sub-mixes; then, inside the element loop right after rendering_config.reserved, a finding at Field(\"headphones_rendering_mode\") for Reserved(raw). Messages are byte-identical to research E.2. DescriptorSet::validate() and ParsedSequence::validate() carry them"
    - "Exactly one reference expectation changes: test_000124.iamf semantic_sha256 893a6103219259eb5863c01d649f97a8af298f0dcbda3e0514c5236264016ad6 -> 795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f, recorded with its reason in the commit body. The qk3 compliance message is unchanged"
    - "Golden fixtures, DIFF-LEDGER.md, src/bits, src/model, src/sequence.rs and tests/conformance.rs are unchanged against 36bf30a. fmt, clippy (with and without --features fuzzing), cargo test --locked, doc tests and fuzz replay pass. Conformance with IAMF_REF_DECODER set fails at most the two known pre-existing FLAC/Opus CONF-05 tests; conformance with it unset passes"
  artifacts:
    - path: "src/error.rs"
      provides: "payload-free ErrorKind::NoMixPresentation and ErrorKind::DuplicateMixPresentationAudioElement with research E.1 doc comments and refs"
      contains: "DuplicateMixPresentationAudioElement"
    - path: "src/encoder.rs"
      provides: "duplicate-handle rejection, handle-lowered validation clone, zero-Mix-Presentation rejection in validate_declarations"
      contains: "NoMixPresentation"
    - path: "src/obu/mix_presentation.rs"
      provides: "num_sub_mixes, duplicate audio_element_id and reserved headphones_rendering_mode findings in MixPresentation::validate()"
      contains: "sub_mix.audio_element_id"
    - path: "tests/encoder_streaming.rs"
      provides: "mono_builder declaring a mono Mix Presentation at the codec's parameter rate"
    - path: "tests/support/reference_expectations.rs"
      provides: "the updated test_000124 semantic_sha256"
      contains: "795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f"
    - path: ".planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-deferred-items.md"
      provides: "research E.5 deferred items with sources"
  key_links:
    - from: "src/encoder.rs validate_declarations (per-presentation loop)"
      to: "src/obu/mix_presentation.rs MixPresentation::validate"
      via: "validate_findings(lowered.validate()) on a clone whose audio_element_id values come from u32::try_from(handle.index)"
      pattern: "lowered\\.validate\\(\\)"
    - from: "src/model/mod.rs DescriptorSet::validate and src/sequence.rs ParsedSequence::validate"
      to: "MixPresentation::validate"
      via: "existing presentation.validate() calls (mod.rs:131, sequence.rs:238); no edit needed"
      pattern: "presentation\\.validate\\(\\)"
    - from: "tests/parse_reference.rs every_positive_matches_its_complete_modeled_field_ledger"
      to: "tests/support/reference_expectations.rs test_000124 entry"
      via: "Debug projection plus ParsedSequence::validate() findings hashed with sha256"
      pattern: "795ef2885684954d"
---

<objective>
Close qk3 deferred items 1-3 (`260913-qk3-deferred-items.md`): `build()` rejects an IA Sequence with
no Mix Presentation and a Mix Presentation that references the same Audio Element twice, and
`MixPresentation::validate()` reports sub-mix-count, duplicate-element and reserved-headphones-mode
findings for foreign input without pre-empting `build()`'s dedicated kinds.

Locked decisions (orchestrator; non-negotiable):
- D-01 Implement research E.1-E.4 as-is. Check order in the per-presentation loop: handle count,
  handle validity, `SubMixCountNotOne`, `ReservedHeadphonesRenderingMode`, NEW
  `DuplicateMixPresentationAudioElement` (on handles), CHANGED `validate_findings(lowered.validate())`
  on a handle-lowered clone, existing layout checks. `NoMixPresentation` is checked LAST in
  `validate_declarations`, after the `write_descriptors` exercise.
- D-02 New findings in `MixPresentation::validate()` with the exact messages, `Field` paths and order
  of research E.2 (they are hash-bearing). Keep the qk3 compliance finding alongside them.
- D-03 Error kinds are payload-free; the 32-byte `Error` assertion stays. `// ref:` citations name
  IAMF v1.1.0 index.bs and iamf-tools@v2.1.0 `mix_presentation.cc` / `obu_processor.cc` (plus libiamf@v1.1.0
  where research B cites it).
- D-04 Do NOT change the qk3 compliance message for the zero-Mix-Presentation case (research open
  question 1: leave it).
- D-05 The scratch diff
  `/private/tmp/claude-501/-Users-cell-local-iamf-rs/ffcacbe6-b061-4445-a28e-fe0219356ad0/scratchpad/th8/th8-proposal.diff`
  may be used as a starting point; its error doc comments are placeholders, use E.1.
- D-06 Reference expectations: ONLY `test_000124.iamf` `semantic_sha256` may change, to
  `795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f` (iamf-tools marks it
  `is_valid: false`). Any other change: stop and report. Record old/new in the commit message.
- D-07 Fix `tests/encoder_streaming.rs::mono_builder` with a mono presentation whose parameter rate
  matches the codec (16 kHz LPCM/FLAC, 48 kHz Opus).
- D-08 Write `260913-th8-deferred-items.md` with research E.5 items.
- D-09 Verification: fmt, clippy with and without `--features fuzzing`, full `cargo test --locked`,
  doc tests, fuzz replay, and the conformance suite with
  `IAMF_REF_DECODER=/Users/cell/local/iamf-rs/.reference/libiamf/code/test/tools/iamfdec/iamfdec` and
  docker `iamf-tools:v2.1.0` under a perl alarm. Exactly `the_flac_fixture_is_conformant` and
  `the_opus_fixture_is_conformant_to_the_pinned_libiamf_decode` are known-failing when
  `IAMF_REF_DECODER` is set (qk3 deferred item 5). CONF-06 runs separately with the variable unset.
  Any OTHER failure is a real failure. Golden fixtures and DIFF-LEDGER.md unchanged.
- D-10 Foreign uncommitted changes (`.claude/CLAUDE.md`, `.gitignore`, `.planning/PROJECT.md`,
  `.planning/codebase/CONVENTIONS.md`, `README.md`, `REFERENCES.md`, `.claude/hooks/`, the untracked
  `CLAUDE.md` files in root/fuzz/tests/tools/tools/codec-fixtures, `docs/`) are never touched or
  staged. Commit by explicit path only; never `git add -A`, `git add .` or `git stash`. Rust 1.85: no
  let-chains.

Claude's discretion (documented):
- Task split: Task 1 is the tracer for the duplicate-element rule because it forces the one real
  architectural change (the lowered clone); Task 2 adds the zero-Mix-Presentation rejection; Task 3
  adds the remaining findings, the single hash update and the full gate.
- Test filter helper in tests/sequence_parse.rs selects findings by the three new `Field` paths so the
  new tests are immune to unrelated pre-existing findings.
- HANDOFF.md step 2's rejection sentence is extended with the two new `build()` rejections (it already
  lists the qk3 ones and is the consumer-facing contract).

Purpose: Core Value. A file with zero Mix Presentations never configures libiamf and fails iamf-tools'
decoder; a duplicate element reference is rejected by iamf-tools on read and write. `build()` must not
emit either. Foreign files get findings, not rejections.

Output: two error kinds, two builder rejections, a handle-lowered validation clone, three
`MixPresentation::validate()` findings, tests, one explained hash update, a HANDOFF.md sentence and a
deferred-items file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-RESEARCH.md
@.planning/quick/260913-qk3-enforce-iamf-v1-1-0-profile-restrictions/260913-qk3-CONTEXT.md
@.planning/quick/260913-qk3-enforce-iamf-v1-1-0-profile-restrictions/260913-qk3-deferred-items.md

<interfaces>
Extracted at planning time from HEAD 36bf30a (verified against the working tree). Use directly.

src/error.rs
- qk3 kinds at :151-158: `SubMixCountNotOne` then `ReservedHeadphonesRenderingMode` (doc comment +
  `// ref:` lines + `#[error(..)]`), followed by `ParameterRateMismatch`.
- `const _: () = assert!(size_of::<Error>() <= 32);` present; `Finding { at: Location, message: String }`.

src/encoder.rs
- `validate_declarations` per-presentation loop starts at :1068 (`for declaration in &self.mix_presentations`):
  expected_references check (:1069-1080), handle loop (:1081-1083), qk3 comment + `SubMixCountNotOne`
  (:1084-1096), `ReservedHeadphonesRenderingMode` via `iter().flat_map(..).any(matches!)` (:1097-1116),
  `validate_findings(declaration.presentation.validate())?;` (:1117), layout checks (:1118-1140).
- End of `validate_declarations`: `crate::model::write_descriptors(&mut crate::bits::BitWriter::new(), &descriptors)?;`
  at :1183 then `Ok(())` at :1184.
- `fn validate_findings(findings: Vec<Finding>) -> Result<()>` at :1271 maps any finding to
  InvalidDescriptorReference at `Field("descriptors")`. `fn allocation_error(_: core::num::TryFromIntError) -> Error` at :1292.
- `declaration.audio_elements: Vec<AudioElementHandle>`; handles compare with `==`; `handle.index: usize`.
  `build()` lowers ids later with `u32::try_from(handle.index)`.

src/obu/mix_presentation.rs `MixPresentation::validate()` (:378-425)
- Order: `localized_presentation_annotations` count check (:381-390), then `for sub_mix in &self.sub_mixes`
  { `findings.extend(sub_mix.validate())`; per element: `push_reserved_finding(&mut findings,
  element.rendering_config.reserved, "rendering_config.reserved", 6);` then
  `element.element_mix_gain.definition.validate()`, annotation count }, output gain, layout reserved.
- `HeadphonesRenderingMode { Stereo, Binaural, Reserved(u8) }` is defined in this file.
  `self.mix_presentation_id: u32`, `element.audio_element_id: u32`.

tests/encoder_builder.rs
- qk3 tests at :184-262: `build_rejects_two_sub_mixes` (two `add_fresh_element(&mut builder, codec, stereo_element())`
  handles, `presentation_for_elements(&[99], 100, 110)` plus the single sub-mix of
  `presentation_for_elements(&[100], 200, 210)` via `.sub_mixes.into_iter().next()`),
  `build_rejects_zero_sub_mixes`, `build_rejects_reserved_headphones_rendering_modes` (sets
  `.sub_mixes.first_mut().and_then(|s| s.elements.first_mut())...headphones_rendering_mode`),
  `build_accepts_binaural_headphones_rendering_mode`, helper `build_presentation(presentation)`.
- Helpers: `lpcm_config()` (:640, 16 kHz), `stereo_element()` (:673), `stereo_presentation()` (:918),
  `add_fresh_element(builder, codec, element)` (:1383), `presentation_for_elements(element_ids,
  element_parameter_id, output_parameter_id)` (:1469, one sub-mix, mix_presentation_id 7, stereo
  A0_2_0 layout, parameter ids counted up from element_parameter_id).
  `presentation_for_elements(&[99, 100], 100, 110)` is already used by qk3 tests with two handles.
- `HeadphonesRenderingMode`, `ErrorKind`, `Location`, `Profile` already imported.

tests/error_shape.rs `static_builder_errors_are_compact_typed_kinds` (:166-187) lists builder kinds,
ending with `ErrorKind::SubMixCountNotOne, ErrorKind::ReservedHeadphonesRenderingMode,`.

tests/encoder_streaming.rs
- `mono_builder(config: CodecConfig)` (:735-767): adds codec, substream, and
  `builder.add_audio_element_with_substreams(codec, vec![substream], AudioElement::channel_based(0, 0, vec![0], ScalableChannelLayoutConfig::single_layer(ChannelAudioLayerConfig::new(LoudspeakerLayout::Mono, 1, 0))));`
  with its handle discarded, then `let input = match config.decoder_config { Lpcm(_) => .., Flac(_) => .., Opus(_) => .., Raw { .. } | _ => unreachable!() };`
  then `builder.build()`. NO Mix Presentation. Called at :145-155 with LPCM, `CodecConfig::flac(0, 128, 16_000, 16)?`
  and `CodecConfig::opus(0, 960, 48_000, 312)?`.
- `fn presentation() -> MixPresentation` (:909): one sub-mix, one element id 0,
  `MixGainParamDefinition::mode_1(0, 16_000)` element gain, `mode_1(1, 16_000)` output gain, stereo
  A0_2_0 layout. `definition.parameter_rate` is a public field.

tests/sequence_parse.rs
- Imports include `Finding`, `Location`, `DescriptorSet`, `HeadphonesRenderingMode`, `IaSequenceHeader`,
  `MixPresentation`, `Obu`, `ObuHeader`, `ObuType`, `ParsedSequence`, `SequenceObu`, `SubMixAudioElement`;
  `#[path = "support/test_000003.rs"] mod support;`.
- Helpers (:600-640): `profile_findings(Vec<Finding>)`, `reference_to(audio_element_id) -> Option<SubMixAudioElement>`
  (clone of MP 42's first element reference), `element_301()`, `presentation_for(mp_id, element_id)`,
  plus `published_parsed_sequence()` (a `Vec<SequenceObu>`) used at :778.
- Support: `published_sequence_header()`, `published_codec_config()` (id 200), `published_audio_element()`
  (Obu, id 300), `published_mix_presentation()` (Obu, MP 42, one sub-mix referencing 300),
  `published_descriptor_set()`.
- Construction pattern for a ParsedSequence: `parsed_sequence_reports_two_sub_mixes_under_a_base_header` (:757-773).
- qk3 tests clone MP 42's sub-mix (so element 300 repeats); they filter with `profile_findings`, so
  the new findings do not disturb them.

tests/support/reference_expectations.rs: the `positive!("test_000124.iamf", "893a61...6ad6")` literal
is at :171-174. The Opus `OpusCodecExpectation { path: "test_000124.iamf", .. }` at :238 must NOT change.

HANDOFF.md :39-40: "`build()` rejects a Presentation whose `num_sub_mixes != 1` or whose rendering
config carries a reserved headphones mode."

Lint rules: `indexing_slicing` and `arithmetic_side_effects` apply to src and tests; `unwrap`/`expect`
allowed only inside `#[test]` fns. No let-chains (Rust 1.85).

Environment: iamfdec at `/Users/cell/local/iamf-rs/.reference/libiamf/code/test/tools/iamfdec/iamfdec`;
docker at `/usr/local/bin/docker`; no `timeout` binary; perl at `/usr/bin/perl`. qk3's conformance run
with IAMF_REF_DECODER set ended `test result: FAILED. 26 passed; 2 failed` listing exactly the two known
tests in the final `failures:` block; the unset run ended `test result: ok. 28 passed`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a Mix Presentation may not reference the same Audio Element twice" — build() rejection, lowered validation clone, validate() finding</name>
  <files>src/error.rs, src/encoder.rs, src/obu/mix_presentation.rs, tests/encoder_builder.rs, tests/sequence_parse.rs</files>
  <behavior>
    tests/encoder_builder.rs:
    - `build_rejects_the_same_audio_element_twice_in_one_presentation`: one `add_fresh_element` handle `a`,
      `presentation_for_elements(&[99, 100], 100, 110)`, handles `vec![a, a]` gives Err
      DuplicateMixPresentationAudioElement at `Location::Field("mix_presentation.audio_elements")`.
      On HEAD this is Ok (research probe: Ok(Base), wire ids [0, 0]), so it fails first.
    - `build_rejects_the_same_audio_element_across_two_sub_mixes_as_sub_mix_count_first`: the
      `build_rejects_two_sub_mixes` shape, but handles `vec![a, a]`, and the second sub-mix's element set to
      `HeadphonesRenderingMode::Reserved(2)` gives SubMixCountNotOne at `Field("num_sub_mixes")`.
    - `reserved_headphones_mode_wins_over_a_duplicate_handle`: one sub-mix with two element references
      (`presentation_for_elements(&[99, 100], 100, 110)`), first element `Reserved(3)`, handles `vec![a, a]`
      gives ReservedHeadphonesRenderingMode at `Field("headphones_rendering_mode")`.
    - `a_duplicate_handle_wins_over_a_presentation_finding`: `presentation_for_elements(&[99, 100], 100, 110)`
      with its sub-mix `layouts` cleared (SubMix::validate's stereo-layout finding), handles `vec![a, a]`
      gives DuplicateMixPresentationAudioElement at `Field("mix_presentation.audio_elements")`. Control in the
      same test: the same presentation with two distinct handles gives InvalidDescriptorReference at
      `Field("descriptors")`.
    - `distinct_elements_with_equal_template_ids_build`: two distinct handles, template
      `presentation_for_elements(&[0, 0], 100, 110)` builds Ok with `manifest.sequence_profile() == Profile::Base`.
      This locks the lowering: without it the duplicate-id finding would fire on the placeholder ids.
    tests/sequence_parse.rs (new helper `th8_findings(Vec<Finding>) -> Vec<Finding>` keeping only findings
    whose `at` is `Location::Field("num_sub_mixes")`, `Location::Field("sub_mix.audio_element_id")` or
    `Location::Field("headphones_rendering_mode")`, in returned order):
    - `mix_presentation_validate_reports_each_later_duplicate_audio_element_id`: MP 42's payload with
      `reference_to(300)` pushed into its first sub-mix gives exactly `[dup(42, 300)]`; with references
      [300, 301, 300, 300] in one sub-mix gives `[dup(42, 300), dup(42, 300)]`; with [300, 301] gives `[]`.
    - `mix_presentation_duplicate_scope_spans_sub_mixes`: MP 42 with a second sub-mix cloned from the first
      (both reference 300) filtered to `Field("sub_mix.audio_element_id")` gives exactly `[dup(42, 300)]`
      (presentation scope, as iamf-tools reads index.bs:1280).
    - `both_validators_carry_the_duplicate_audio_element_finding`: `published_descriptor_set()` with
      `reference_to(300)` pushed into MP 42's sub-mix, and a ParsedSequence (header, codec 200, element 300,
      that MP) built like the :757 test: `th8_findings(..validate())` is `[dup(42, 300)]` for both.
    - Exact message (hash-relevant from Task 3 on): `dup(mp, id)` = "mix presentation {mp} references
      audio_element_id {id} more than once; there SHALL be no duplicate audio_element_id within one Mix
      Presentation (IAMF v1.1.0 index.bs:1280)" at `Location::Field("sub_mix.audio_element_id")`.
  </behavior>
  <action>
    RED first: add the tests above (non-test helpers panic-free; `.first_mut()`, `.get()`, `into_iter().next()`,
    no indexing or bare arithmetic). Run `cargo test --locked --test encoder_builder build_rejects_the_same_audio_element`
    and record in the SUMMARY that it fails on HEAD (confirms qk3 deferred item 2).

    GREEN (per D-01, D-02, D-03; D-05 scratch diff may be applied as a base):
    - src/error.rs: directly after `ReservedHeadphonesRenderingMode`, add payload-free
      `DuplicateMixPresentationAudioElement` with doc comment "A Mix Presentation references the same Audio
      Element more than once.", the lines `// ref: IAMF v1.1.0 index.bs:1280; iamf-tools@v2.1.0 iamf/obu/mix_presentation.cc:41-54 ValidateUniqueAudioElementIds`
      and `// DISAGREEMENT: libiamf@v1.1.0 has no check; spec and iamf-tools are stricter and win`, and
      `#[error("a Mix Presentation references the same Audio Element more than once")]`. Leave the size assertion alone.
    - src/encoder.rs per-presentation loop: directly after the `ReservedHeadphonesRenderingMode` check and
      before the generic findings, add the duplicate-HANDLE check: `declaration.audio_elements.iter().enumerate().any(|(position, handle)| declaration.audio_elements.iter().take(position).any(|other| other == handle))`
      returns `Error::new(ErrorKind::DuplicateMixPresentationAudioElement, Location::Field("mix_presentation.audio_elements"))`.
      Cite `// ref: IAMF v1.1.0 index.bs:1280 (no duplicate audio_element_id within one Mix Presentation)` and
      `// ref: iamf-tools@v2.1.0 iamf/obu/mix_presentation.cc:41-54 ValidateUniqueAudioElementIds (read :550, write :496)`.
    - Replace `validate_findings(declaration.presentation.validate())?;` with: clone the template into
      `lowered`; for each `(element, handle)` in `lowered.sub_mixes.iter_mut().flat_map(|sub_mix| sub_mix.elements.iter_mut()).zip(&declaration.audio_elements)`
      set `element.audio_element_id = u32::try_from(handle.index).map_err(allocation_error)?`; then
      `validate_findings(lowered.validate())?;`. Comment: templates carry placeholder ids
      (tests/support/parallax_contract.rs uses 0 for every element), so presentation findings are checked
      on the ids `build()` will write; distinct handles give distinct ids, so the duplicate-id finding cannot
      fire here and the dedicated kind above stays the only duplicate signal. The subsequent layout checks
      keep reading `declaration.presentation`.
    - src/obu/mix_presentation.rs `MixPresentation::validate()`: immediately after the
      `localized_presentation_annotations` check and before `for sub_mix in &self.sub_mixes`, collect
      `references: Vec<u32>` from all sub-mixes' elements in wire order; for each `(position, id)` where
      `references.iter().take(position).any(|earlier| earlier == id)`, push a Finding at
      `Location::Field("sub_mix.audio_element_id")` with the exact `dup` message from behavior (use
      `self.mix_presentation_id`). Above it: `// ref: IAMF v1.1.0 index.bs:1280`,
      `// ref: iamf-tools@v2.1.0 iamf/obu/mix_presentation.cc:41-54 ValidateUniqueAudioElementIds (read :550, write :496)`,
      `// DISAGREEMENT: libiamf@v1.1.0 code/src/iamf_dec/IAMF_decoder.c:1284-1337 does not check; spec and iamf-tools are stricter and win`.
      Leave space for Task 3's `num_sub_mixes` block, which goes BETWEEN the annotations check and this block.
    - No indexing, no bare arithmetic, no unwrap in src. `Field("audio_element_id")` is NOT used (the
      reference-resolution finding owns it).

    Guard: run `cargo test --locked --test parse_reference` — it must pass UNCHANGED (research: no reference
    fixture has a duplicate element id). If it fails, STOP and report; do not touch reference_expectations.rs
    in this task.

    Commit by explicit path (src/error.rs src/encoder.rs src/obu/mix_presentation.rs tests/encoder_builder.rs tests/sequence_parse.rs):
    `feat(quick-260913-th8): reject duplicate Audio Element references within one Mix Presentation`.
  </action>
  <verify>
    <automated>cd /Users/cell/local/iamf-rs && grep -q 'DuplicateMixPresentationAudioElement' src/error.rs && grep -q 'lowered.validate()' src/encoder.rs && grep -q 'sub_mix.audio_element_id' src/obu/mix_presentation.rs && grep -q 'mix_presentation.cc:41-54' src/obu/mix_presentation.rs && cargo test --locked --test encoder_builder && cargo test --locked --test sequence_parse && cargo test --locked --test parallax_contract && cargo test --locked --test parse_reference && cargo test --locked --test descriptors</automated>
  </verify>
  <done>The duplicate-handle test failed on HEAD and now passes; the three order tests return SubMixCountNotOne, ReservedHeadphonesRenderingMode and DuplicateMixPresentationAudioElement respectively; equal template ids on distinct handles build Base; the duplicate finding appears in MixPresentation, DescriptorSet and ParsedSequence validation with the exact message; parse_reference, parallax_contract and descriptors pass unchanged. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build() rejects an IA Sequence with no Mix Presentation, and the streaming mono fixture declares one</name>
  <files>src/error.rs, src/encoder.rs, tests/encoder_builder.rs, tests/error_shape.rs, tests/encoder_streaming.rs</files>
  <behavior>
    - `build_rejects_zero_mix_presentations`: `EncoderBuilder::new()`, `add_codec_config(lpcm_config())`,
      one `add_fresh_element(&mut builder, codec, stereo_element())`, no `add_mix_presentation`: `build()`
      gives Err NoMixPresentation at `Location::Field("mix_presentations")`. On HEAD it is Ok(Simple), so it fails first.
    - Every existing encoder_builder test that builds an element-only declaration to reach an earlier error
      (for example UnsupportedParameterData, ScalableChannelLayers) keeps its kind unchanged: the new check is last.
    - `static_builder_errors_are_compact_typed_kinds` lists `ErrorKind::NoMixPresentation` and
      `ErrorKind::DuplicateMixPresentationAudioElement`; `size_of::<Error>() <= 32` holds.
    - tests/encoder_streaming.rs: every `mono_builder` caller (LPCM, FLAC 16 kHz, Opus 48 kHz) builds and its
      test passes, because `mono_builder` now declares a mono Mix Presentation whose element and output
      `parameter_rate` equal the codec output rate.
  </behavior>
  <action>
    RED: add `build_rejects_zero_mix_presentations` to tests/encoder_builder.rs near the qk3 rejection tests;
    run `cargo test --locked --test encoder_builder build_rejects_zero_mix_presentations` and record that it
    fails on HEAD (confirms qk3 deferred item 1).

    GREEN (per D-01, D-03, D-04, D-07):
    - src/error.rs: insert payload-free `NoMixPresentation` between `ReservedHeadphonesRenderingMode` and
      `DuplicateMixPresentationAudioElement` (research E.1 order), with doc comment "The builder declares no
      Mix Presentation.", the three lines
      `// ref: IAMF v1.1.0 index.bs:1929 (at least one Mix Presentation SHALL comply with primary_profile)`,
      `// ref: iamf-tools@v2.1.0 iamf/cli/obu_processor.cc:531-533 ("No mix presentation OBUs found.")`,
      `// ref: libiamf@v1.1.0 code/src/iamf_dec/IAMF_decoder.c:3157-3160 (IAMF_FLAG_CONFIG needs a Mix Presentation)`,
      and `#[error("an IA sequence must carry at least one Mix Presentation")]`.
    - src/encoder.rs `validate_declarations`: after the `crate::model::write_descriptors(..)?;` exercise and
      before the final `Ok(())`, add `if self.mix_presentations.is_empty()` returning
      `Error::new(ErrorKind::NoMixPresentation, Location::Field("mix_presentations"))`, with the same three
      `// ref:` lines and a one-line comment that it runs last so earlier, more specific declaration errors
      keep their kinds. Do not touch `crate::model::profile` or the qk3 compliance finding text (D-04).
    - tests/error_shape.rs: append both new kinds to the `static_builder_errors_are_compact_typed_kinds` list.
    - tests/encoder_streaming.rs `mono_builder`: bind the element handle returned by
      `add_audio_element_with_substreams`. Fold the rate into the existing `match config.decoder_config` so no
      new wildcard match is introduced: it yields `(input, rate)` with 16_000 for Lpcm and Flac, 48_000 for
      Opus, and keeps the existing `Raw { .. } | _ => unreachable!()` arm. Then take `presentation()`, set
      `output_mix_gain.definition.parameter_rate` on every sub-mix and `element_mix_gain.definition.parameter_rate`
      on every sub-mix element to `rate` (iterate with `iter_mut`, no indexing), and call
      `builder.add_mix_presentation(vec![element], mono)` before `builder.build()`. The rate must match the
      codec per the ParameterRateMismatch rule.

    If any test other than the expected RED test and the mono_builder callers fails (run
    `cargo test --locked --no-fail-fast` for the encoder_builder, error_shape, encoder_streaming, parallax_contract,
    temporal and fixture binaries), STOP and report which one: research verified that only mono_builder builds
    without a Mix Presentation.

    Commit by explicit path (src/error.rs src/encoder.rs tests/encoder_builder.rs tests/error_shape.rs tests/encoder_streaming.rs):
    `feat(quick-260913-th8): reject an IA sequence without a Mix Presentation in build()`.
  </action>
  <verify>
    <automated>cd /Users/cell/local/iamf-rs && grep -q 'NoMixPresentation' src/error.rs && grep -q 'ErrorKind::NoMixPresentation' src/encoder.rs && grep -q 'obu_processor.cc:531-533' src/encoder.rs && grep -q 'ErrorKind::NoMixPresentation' tests/error_shape.rs && grep -q 'ErrorKind::DuplicateMixPresentationAudioElement' tests/error_shape.rs && grep -q 'add_mix_presentation' tests/encoder_streaming.rs && cargo test --locked --test encoder_builder && cargo test --locked --test error_shape && cargo test --locked --test encoder_streaming && cargo test --locked --test parallax_contract && cargo test --locked --test temporal && cargo test --locked --test fixture</automated>
  </verify>
  <done>The zero-Mix-Presentation test failed on HEAD and now passes with NoMixPresentation at Field("mix_presentations"); earlier-error tests keep their kinds; both new kinds are in error_shape and the 32-byte assertion holds; mono_builder declares a rate-matched mono presentation and encoder_streaming is green. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sub-mix-count and reserved-headphones findings, the single explained hash update, deferred items and the full gate</name>
  <files>src/obu/mix_presentation.rs, tests/sequence_parse.rs, tests/support/reference_expectations.rs, HANDOFF.md, .planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-deferred-items.md</files>
  <behavior>
    Exact messages (hash-bearing; byte-identical to research E.2):
    - `sub0` at `Field("num_sub_mixes")`: "num_sub_mixes is 0; it SHALL NOT be 0 (IAMF v1.1.0 index.bs:1278)"
    - `subn(n)` at `Field("num_sub_mixes")`: "num_sub_mixes is {n}; it SHOULD be 1 and parsers SHOULD ignore a Mix Presentation with more (IAMF v1.1.0 index.bs:1920); libiamf fails its parse"
    - `hp(id, raw)` at `Field("headphones_rendering_mode")`: "audio element {id} carries reserved headphones_rendering_mode {raw}; parsers SHALL ignore this Mix Presentation (IAMF v1.1.0 index.bs:1341)"
    tests/sequence_parse.rs (using Task 1's `th8_findings`):
    - `published_mix_presentation_has_no_findings`: `support::published_mix_presentation().payload.validate()` is exactly `vec![]`.
    - `mix_presentation_validate_reports_zero_sub_mixes`: MP 42 with `sub_mixes` cleared gives `[sub0]`.
    - `mix_presentation_validate_reports_sub_mix_count_before_duplicates`: MP 42 with its sub-mix cloned once
      gives `[subn(2), dup(42, 300)]` (order locked).
    - `mix_presentation_validate_reports_reserved_headphones_modes`: for raw 2 and 3 on element 300 gives `[hp(300, raw)]`;
      `Binaural` gives `[]`.
    - `mix_presentation_findings_follow_the_research_order`: two sub-mixes (clone), the second sub-mix's
      element set to `Reserved(3)` gives `[subn(2), dup(42, 300), hp(300, 3)]`.
    - `both_validators_carry_the_sub_mix_and_headphones_findings`: `published_descriptor_set()` with MP 42's
      sub-mix cloned, and the test_000124-shaped ParsedSequence (as in :757), both give
      `th8_findings(..) == [subn(2), dup(42, 300)]`; the existing qk3 profile tests still pass (compliance
      finding kept, D-02, D-04).
    - tests/encoder_builder.rs order tests from Task 1 and the qk3 `build_rejects_two_sub_mixes`,
      `_zero_sub_mixes`, `_reserved_headphones_rendering_modes`, `build_accepts_binaural_headphones_rendering_mode`
      still return their dedicated kinds (no pre-emption), unchanged.
    - parse_reference passes with exactly one expectation literal changed: test_000124 -> 795ef288...9e2f.
  </behavior>
  <action>
    RED: add the sequence_parse tests; run `cargo test --locked --test sequence_parse` and see the new ones fail.

    GREEN, src/obu/mix_presentation.rs `MixPresentation::validate()` (per D-02; D-05 scratch diff is a base):
    - BETWEEN the `localized_presentation_annotations` check and Task 1's duplicate-id block, add a
      `match self.sub_mixes.len()`: `1` pushes nothing; `0` pushes `sub0`; any other `count` pushes `subn(count)`
      via `format!`. Cite `// ref: IAMF v1.1.0 index.bs:1278 (SHALL NOT be 0), :1920 (SHOULD be 1; > 1 SHOULD be ignored)`,
      `// ref: iamf-tools@v2.1.0 iamf/obu/mix_presentation.cc:195-199 ValidateNumSubMixes`,
      `// ref: libiamf@v1.1.0 code/src/iamf_dec/IAMF_OBU.c:760-782 (num_sub_mixes != 1 fails the parse)`.
    - Inside the element loop, immediately after the `push_reserved_finding(.., "rendering_config.reserved", 6)`
      call and before `element_mix_gain.definition.validate()`, add `if let HeadphonesRenderingMode::Reserved(raw) = element.rendering_config.headphones_rendering_mode`
      pushing `hp(element.audio_element_id, raw)`. Single `if let`, no let-chain. Cite
      `// ref: IAMF v1.1.0 index.bs:1337-1341 (parsers SHALL ignore the Mix Presentation)`,
      `// ref: iamf-tools@v2.1.0 iamf/cli/profile_filter.cc:240-271`,
      `// ref: libiamf@v1.1.0 code/src/iamf_dec/IAMF_decoder.c:1309-1315`.
    - Add a short comment on `validate()` that `EncoderBuilder::build()` checks `SubMixCountNotOne`,
      `ReservedHeadphonesRenderingMode` and duplicate handles before calling it, so these findings never
      pre-empt those kinds.
    - When writing the two string literals, beware Rust's trailing-backslash continuation: the resulting text
      must equal the behavior strings exactly (single spaces, no newline).

    Hash update (D-06), in this order:
    1. Run `cargo test --locked --test parse_reference 2>&1 | tee target/th8-parse-reference-before.log`. It is
       expected to fail on `test_000124.iamf`. If it fails naming ANY other fixture, or on the negative
       ValidationFinding fixture, STOP and report without editing expectations. If the log prints the computed
       hash for test_000124 and it is not `795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f`,
       compare the three message templates and `Field` paths character by character with behavior, fix any
       deviation in src, and rerun; never adopt a different hash.
    2. Replace only the `positive!("test_000124.iamf", ..)` literal
       `893a6103219259eb5863c01d649f97a8af298f0dcbda3e0514c5236264016ad6` with
       `795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f`. Do not touch the Opus codec
       expectation entry.
    3. Rerun `cargo test --locked --test parse_reference`; it must pass. Because that test panics on the first
       mismatch across all positives, a pass proves no other expectation changed. If it now fails on another
       fixture, STOP and report (revert step 2's edit is not needed; just report).

    HANDOFF.md step 2: extend the sentence at :39-40 so it reads that `build()` rejects a sequence with no Mix
    Presentation, a Presentation whose `num_sub_mixes != 1`, whose rendering config carries a reserved
    headphones mode, or that lists the same Audio Element handle more than once. Use Edit, not Write.

    Deferred items (D-08): write `260913-th8-deferred-items.md` in the qk3 style: heading; "Resolved by this
    task" (qk3 deferred items 1, 2 and 3, one line each naming NoMixPresentation,
    DuplicateMixPresentationAudioElement, and the three validate() findings); a note that the qk3 compliance
    message for zero Mix Presentations was deliberately left unchanged (research open question 1, orchestrator
    decision); then one section per research E.5 item with **Source**, **Current state**, **Why out of scope**:
    1. duplicate `annotations_language` (v1.1.0 index.bs:1272; iamf-tools mix_presentation.cc:528-530 read,
       :471-473 write; build() acceptance unverified);
    2. BCP-47 conformance of `annotations_language` (index.bs:1272);
    3. `num_layouts >= num_layers` for a single scalable channel element, parse-side (index.bs:1305-1310);
    4. `MixPresentationTags` presence rule (index.bs:1313; modelled as `trailing`);
    5. iamf-tools' unique anchored-loudness elements (mix_presentation.cc:56-66);
    6. carried: qk3 item 4 (Binaural `decoder_main` abort) and qk3 item 5 (CONF-05 FLAC/Opus iamfdec failure).
    Close with a note that `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md` was deliberately not edited.

    Commit by explicit path (src/obu/mix_presentation.rs tests/sequence_parse.rs tests/support/reference_expectations.rs HANDOFF.md .planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-deferred-items.md):
    `feat(quick-260913-th8): report sub-mix count and reserved headphones mode in MixPresentation validation`.
    The body contains the line
    `test_000124.iamf semantic_sha256: 893a6103219259eb5863c01d649f97a8af298f0dcbda3e0514c5236264016ad6 -> 795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f`
    and the reason: its one Mix Presentation has 2 sub-mixes; the new num_sub_mixes finding (IAMF v1.1.0
    index.bs:1920) is inserted before the sequence-level findings; iamf-tools@v2.1.0
    iamf/cli/testdata/test_000124.textproto:19-20 marks it `is_valid: false`, `is_valid_to_decode: false`.

    Full gate (D-09), in order; fix clippy findings at the source, never with `#[allow]`:
    1. `cargo fmt --all -- --check`
    2. `cargo clippy --locked --all-targets -- -D warnings`
    3. `cargo clippy --locked --all-targets --features fuzzing -- -D warnings`
    4. `cargo test --locked`
    5. `cargo test --locked --doc`
    6. `cargo test --locked --features fuzzing --test fuzz_regression`
    If a formatting or clippy fix is needed after the Task 3 commit, amend nothing: make a follow-up commit by
    explicit path `style(quick-260913-th8): ...`.

    Conformance, bounded (no timeout binary). Wrapper W, passed as one argument list:
    `perl -e 'my $s=shift; my $p=fork; if(!$p){setpgrp(0,0); exec @ARGV; exit 127} $SIG{ALRM}=sub{kill "KILL", -$p; print STDERR "TIMED OUT after $s s\n"; exit 124}; alarm $s; waitpid($p,0); exit($? >> 8)' SECONDS CMD...`
    1. `cargo test --locked --test conformance --no-run`.
    2. Probe `docker image inspect iamf-tools:v2.1.0` under W with 60 s. Exit 0 = present; 124 = timed out;
       other = absent. If timed out, create `target/th8-docker-shim/docker` (`#!/bin/sh` then `exit 1`),
       `chmod +x`, prepend its directory to PATH for both runs below only, and make the first line of each log
       `NOTE: docker unresponsive (60 s probe timed out); container clauses SKIPPED via shim`.
    3. Run A: under W with 1200 s,
       `IAMF_REF_DECODER=/Users/cell/local/iamf-rs/.reference/libiamf/code/test/tools/iamfdec/iamfdec cargo test --locked --test conformance -- --nocapture`,
       stdout+stderr to `target/th8-conformance.log`, then append `conf_exit=<code>`. Acceptance: the log has a
       `test result:` line, no `TIMED OUT`, and every name in the final `failures:` list is one of
       `the_flac_fixture_is_conformant` or `the_opus_fixture_is_conformant_to_the_pinned_libiamf_decode`
       (known pre-existing, qk3 deferred item 5). Any other failing test is a REAL failure: stop, check without disturbing the
       working tree (e.g. a `git worktree add` of 36bf30a in the scratchpad with `.reference-manifest.json`
       and `.reference/` supplied) whether it predates th8, and report.
    4. Run B (CONF-06 separately): under W with 1200 s,
       `env -u IAMF_REF_DECODER cargo test --locked --test conformance -- --nocapture` to
       `target/th8-conformance-conf06.log`, then append `conf_exit=<code>`. Acceptance: `test result: ok`.
    5. Boundary: `git diff --quiet 36bf30a -- src/bits src/model src/sequence.rs src/lib.rs src/fuzzing.rs src/packing.rs src/dump.rs DIFF-LEDGER.md tests/golden.rs tests/fixtures tests/conformance.rs tests/parallax_contract.rs tests/support/fixture.rs tests/support/parallax_contract.rs tests/support/sequence_cases.rs tests/support/test_000003.rs fuzz/fuzz_targets`
       exits 0, and `git diff --name-only 36bf30a -- src/obu` prints only `src/obu/mix_presentation.rs`.

    In the SUMMARY: quote the RED failures from Tasks 1-2; the `test result:` lines for encoder_builder,
    error_shape, encoder_streaming, sequence_parse, parse_reference and both conformance runs; every `CONF-05`
    and `CONF-06: decoder_main reported` line from the two logs (or SKIPPED/TIMED OUT stated explicitly, never
    "passed"); the two known failures named as pre-existing, not passed; the hash change; the boundary result.
  </action>
  <verify>
    <automated>cd /Users/cell/local/iamf-rs && grep -q 'IAMF_OBU.c:760-782' src/obu/mix_presentation.rs && grep -q 'IAMF_decoder.c:1309-1315' src/obu/mix_presentation.rs && mkdir -p target && git diff -U0 36bf30a -- tests/support/reference_expectations.rs > target/th8-expectations.diff && D=$(grep -E '^[-+][^-+]' target/th8-expectations.diff) && [ "$(printf '%s\n' "$D" | wc -l | tr -d ' ')" = 2 ] && printf '%s\n' "$D" | grep -q '^-.*893a6103219259eb5863c01d649f97a8af298f0dcbda3e0514c5236264016ad6' && printf '%s\n' "$D" | grep -q '^+.*795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f' && F=.planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-deferred-items.md && test -f $F && grep -q 'annotations_language' $F && grep -q 'Binaural' $F && grep -q 'NoMixPresentation' HANDOFF.md && C=$(git log -n 1 --format=%H -- tests/support/reference_expectations.rs) && git log -1 --format=%B "$C" > target/th8-hash-commit.txt && grep -q '893a6103219259eb5863c01d649f97a8af298f0dcbda3e0514c5236264016ad6 -> 795ef2885684954d4e5eda8e26c52e0a1328488844d4de54e82eb1e69c8a9e2f' target/th8-hash-commit.txt && git diff --name-only 36bf30a HEAD > target/th8-committed.txt && [ -z "$(grep -vE '^(src/error\.rs|src/encoder\.rs|src/obu/mix_presentation\.rs|tests/encoder_builder\.rs|tests/error_shape\.rs|tests/encoder_streaming\.rs|tests/sequence_parse\.rs|tests/support/reference_expectations\.rs|HANDOFF\.md|\.planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/.*)$' target/th8-committed.txt)" ] && cargo fmt --all -- --check && cargo clippy --locked --all-targets -- -D warnings && cargo clippy --locked --all-targets --features fuzzing -- -D warnings && cargo test --locked && cargo test --locked --doc && cargo test --locked --features fuzzing --test fuzz_regression && test -f target/th8-conformance.log && grep -q '^test result: ' target/th8-conformance.log && ! grep -q 'TIMED OUT' target/th8-conformance.log && [ -z "$(grep -E '^    [a-z0-9_]+$' target/th8-conformance.log | sed 's/^ *//' | sort -u | grep -vxE 'the_flac_fixture_is_conformant|the_opus_fixture_is_conformant_to_the_pinned_libiamf_decode')" ] && grep -q '^test result: ok' target/th8-conformance-conf06.log && git diff --quiet 36bf30a -- src/bits src/model src/sequence.rs src/lib.rs src/fuzzing.rs src/packing.rs src/dump.rs DIFF-LEDGER.md tests/golden.rs tests/fixtures tests/conformance.rs tests/parallax_contract.rs tests/support/fixture.rs tests/support/parallax_contract.rs tests/support/sequence_cases.rs tests/support/test_000003.rs fuzz/fuzz_targets && [ "$(git diff --name-only 36bf30a -- src/obu)" = "src/obu/mix_presentation.rs" ]</automated>
  </verify>
  <done>MixPresentation::validate() reports sub0/subn, dup and hp findings in research E.2 order with exact texts, and both validators carry them; build() dedicated kinds are not pre-empted; only test_000124's hash changed, recorded old -> new with reason in the commit body; HANDOFF.md and the deferred-items file are written; fmt, both clippy runs, full tests, doc tests and fuzz replay pass; conformance run A fails at most the two known FLAC/Opus CONF-05 tests, run B passes; boundary paths unchanged; only allowed paths committed; foreign changes untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> EncoderBuilder::build() | public declaration API lets a caller omit Mix Presentations or repeat an Audio Element handle, and passes templates with placeholder ids |
| crate output -> reference decoders | zero Mix Presentations never configure libiamf and fail iamf-tools' decoder; duplicate element ids fail iamf-tools' read |
| foreign bytes -> parse_sequence -> validate() | hostile Mix Presentation content (sub-mix counts, element id lists, reserved modes) reaches the new findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-th8-01 | Tampering | src/encoder.rs validate_declarations | high | mitigate | `NoMixPresentation` (checked last) and `DuplicateMixPresentationAudioElement` (on handles, after the qk3 kinds) stop `build()` from writing a sequence the references reject; RED tests fail on HEAD first |
| T-th8-02 | Denial of Service | src/encoder.rs lowered clone | medium | mitigate | validation runs on handle-derived ids via `u32::try_from(..).map_err(allocation_error)?`, so placeholder template ids (Parallax fixture) cannot cause false rejections; locked by `distinct_elements_with_equal_template_ids_build` and parallax_contract/conformance tests |
| T-th8-03 | Denial of Service | MixPresentation::validate on foreign input | medium | mitigate | no indexing, no bare arithmetic, no unwrap (`indexing_slicing`, `arithmetic_side_effects` clippy with and without `--features fuzzing`); fuzz replay gate |
| T-th8-04 | Denial of Service | O(n^2) duplicate scan | low | accept | `num_audio_elements` is already bounded at read by `read_bounded_count` against `bytes_remaining()` (mix_presentation.rs:516); the id Vec holds only parsed elements |
| T-th8-05 | Repudiation | tests/support/reference_expectations.rs | medium | mitigate | only test_000124 may change, to a pre-measured hash; parse_reference panics on any other mismatch; old -> new plus reason in the commit body; any other change stops execution |
| T-th8-06 | Tampering | golden / foreign round trip | medium | mitigate | writers untouched; `git diff --quiet 36bf30a` gate on src/bits, src/model, src/sequence.rs, golden fixtures, DIFF-LEDGER.md, conformance.rs; src/obu limited to mix_presentation.rs |
</threat_model>

<verification>
- Task 1: duplicate-handle RED on HEAD then green; order tests lock SubMixCountNotOne > ReservedHeadphonesRenderingMode > DuplicateMixPresentationAudioElement > generic findings; lowering proven by equal template ids; parse_reference unchanged.
- Task 2: zero-Mix-Presentation RED on HEAD then green; error_shape lists both kinds; encoder_streaming green with the rate-matched mono presentation.
- Task 3: exact E.2 findings and order in all three validators; exactly one hash literal changed with provenance; full gate (fmt, clippy x2, cargo test, doc, fuzz replay); conformance A fails only the two known tests, B passes; boundary diff clean; only allowed paths committed.
</verification>

<success_criteria>
- `build()` cannot emit an IA Sequence without a Mix Presentation or a Mix Presentation with a repeated Audio Element, and never rejects valid Parallax-style templates with placeholder ids.
- Foreign Mix Presentations with a wrong sub-mix count, repeated element ids or a reserved headphones mode are reported by `MixPresentation::validate()`, `DescriptorSet::validate()` and `ParsedSequence::validate()`, alongside the unchanged qk3 compliance finding.
- Only `test_000124.iamf`'s `semantic_sha256` changed, to `795ef288...9e2f`, with recorded provenance.
- Golden fixtures and DIFF-LEDGER.md are byte-identical; conformance evidence is quoted honestly, the two known FLAC/Opus CONF-05 failures named as pre-existing.
- `260913-th8-deferred-items.md` records research E.5.
</success_criteria>

<output>
Create `.planning/quick/260913-th8-reject-empty-mix-presentations-and-dupli/260913-th8-SUMMARY.md` when done
</output>
