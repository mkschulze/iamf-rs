---
phase: quick-260914-1mq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/error.rs
  - src/obu/parameter_block.rs
  - tests/temporal.rs
  - tests/allocation_bounds.rs
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [TIME-05, PARSE-01, GUARD-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "read_parameter_block refuses every Demixing or Recon Gain block whose subblock count is not exactly 1 with ErrorKind::SubblockCountNotOne at Location::InputOffset(0) (the payload start), before usize conversion, the bytes_remaining() rule or bounded_vec: 2 MiB `01 fb ff 7f 01`+zeros with ReconGain [false;7]; `01 ff ff ff 7f 01` ReconGain [false;2]; `01 02 00 02 01 01` ReconGain [false]; `01 00 00` under a mode-0 definition {duration 2, csd 1} ReconGain [false;2]; `05 02 01 bb bb` Demixing; `05 00 00 00` Demixing (count 0)"
    - "A legal single Recon Gain subblock with every layer absent (`01 c0 07 c0 07`, ReconGain [false;2]) now parses to one subblock with layers [None, None], consumes all 5 bytes and re-serialises byte-identically; on HEAD it failed with UnexpectedEndOfInput at 0"
    - "write_parameter_block refuses a Demixing block with 2 subblocks (block duration 2, csd 1) with SubblockCountNotOne at Location::Field(\"subblocks\")"
    - "Mix Gain and extension (Raw) blocks are unaffected: `01 02 01 00 00 01 00 ff ff` MixGain still yields 2 Step subblocks and round-trips, and the existing hostile MixGain count still reports UnexpectedEndOfInput"
    - "No semantic_sha256, golden artifact, fixture, HANDOFF.md, DIFF-LEDGER.md or existing ErrorKind variant/message changes; size_of::<Error>() <= 32 still holds; fmt, clippy (with and without --features fuzzing), cargo test --locked, fuzz replay and tests/citations.rs pass; `rg -U -c 'allow\\(\\s*clippy::disallowed_types' src/` is exactly `src/model/loudness.rs:1`"
  artifacts:
    - path: "src/error.rs"
      provides: "appended unit variant SubblockCountNotOne with doc, ref and DISAGREEMENT comments"
      contains: "SubblockCountNotOne,"
    - path: "src/obu/parameter_block.rs"
      provides: "private check_single_subblock(context, count, at) called in read_parameter_block before the byte rule and in validate_parameter_block; byte rule exempts only ParameterDataContext::ReconGain"
      contains: "fn check_single_subblock"
    - path: "tests/temporal.rs"
      provides: "RED-first reader/writer vectors for the single-subblock rule plus Mix Gain control"
      contains: "a_single_recon_gain_subblock_with_every_layer_absent_reads_and_round_trips"
    - path: "tests/allocation_bounds.rs"
      provides: "2 MiB zero-byte Recon Gain subblock vector"
      contains: "hostile_recon_gain_subblock_count_in_two_mebibyte_parameter_block_errors_at_offset_0"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "zero-byte subblock class recorded as closed; remaining linear residuals named"
      contains: "260914-1mq"
  key_links:
    - from: "src/obu/parameter_block.rs read_parameter_block"
      to: "check_single_subblock"
      via: "called once num_subblocks is known (explicit or derived, both modes) and before usize::try_from / bytes_remaining() / bounded_vec"
      pattern: "check_single_subblock\\(context, num_subblocks"
    - from: "src/obu/parameter_block.rs validate_parameter_block (used by write_parameter_block)"
      to: "check_single_subblock"
      via: "called on the actual subblock count before the expected-count comparison, at Field(\"subblocks\")"
      pattern: "check_single_subblock\\(context, actual_count"
    - from: "src/sequence.rs parse_sequence"
      to: "read_parameter_block"
      via: "with_input_base(payload_base) makes the InputOffset(0) absolute; no change to sequence.rs"
      pattern: "read_parameter_block"
---

<objective>
Close the cross-AI review finding [MEDIUM] "Zero-byte Parameter Block subblocks amplify memory about 50×"
(`.planning/reviews/2026-09-13-CODEBASE-REVIEWS.md:124-131`).

Today `read_parameter_block` (`src/obu/parameter_block.rs:365-490`) bounds `num_subblocks` only by
`r.bytes_remaining()` (`:445`). A Recon Gain subblock whose `recon_gain_is_present` flags are all false consumes
0 wire bytes (`:733-739`) but costs 40 bytes of model plus 20 bytes per layer slot, so a 2 MiB OBU reaches about
204× model bytes per input byte (research Q1). The same byte rule also wrongly rejects the legal single zero-byte
Recon Gain block `01 c0 07 c0 07` with `UnexpectedEndOfInput@0`.

Conformance rule (spec AND references, stricter wins): IAMF v1.1.0 `index.bs:776-782` (DemixingParamDefinition)
and `:786-792` (ReconGainParamDefinition) say `num_subblocks` SHALL be set to 1. iamf-tools@v2.1.0
`ValidateSpecificParamDefinition` (`iamf/obu/param_definitions.cc:37-60`) requires mode 0 and
`constant_subblock_duration == duration != 0` on every Parameter Block read (`parameter_block.cc:376`) and write
(`:350`). libiamf@v1.1.0 `iamf_parameter_new` (`IAMF_OBU.c:1103-1129`) does not check (DISAGREEMENT). Rejecting a
count other than 1 cannot reject a file the spec or iamf-tools accepts. The fuller iamf-tools mode/csd rule is
deferred (see output).

Purpose: parser hardening plus one false-rejection fix. Research Q5: 110 of 110 committed fixture/corpus inputs give
identical parse results, `semantic_sha256` projections and dumps before and after, so no reference hash and no golden
change is predicted.

Output: one new appended `ErrorKind`, one private check used by reader and writer, 8 new tests (RED first), a CONCERNS
update, and a deferred-items file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/.continue-here.md
@.claude/CLAUDE.md
@tests/CLAUDE.md
@.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-RESEARCH.md

Base commit: `64d2eb3`. The working tree has untracked `.DS_Store`, `docs/` clones, `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md`,
`docs/superpowers/` and this quick-task directory. Leave all of them alone and never stage them. Never use `git add -A`,
`git add .` or `git commit -a`; stage explicit paths and check `git diff --cached --name-only` before every commit.
Work sequentially on `main`, no worktrees.

Interfaces verified this session (do not re-explore):

- `src/obu/parameter_block.rs:356-369`: `// ref:` lines then `pub fn read_parameter_block(r: &mut BitCursor<'_>, registry: &ParamDefinitionRegistry) -> Result<ParameterBlock>`; `let start = r.byte_position();`, peek, `let def = &registered.definition; let context = &registered.context;` (`:378-379`).
- `:395-432`: `let (duration_fields, num_subblocks) = if def.param_definition_mode() { .. } else { .. };` where `num_subblocks: u32` is explicit (mode 1, csd 0), `subblocks_implied_by(..)` (mode 1 csd != 0, or mode 0 csd != 0), or `fields.subblock_durations.len()` (mode 0, csd 0).
- `:434-437`: `include_subblock_duration`. `:439-450`: the `T-01-32` comment, `let num_subblocks = usize::try_from(num_subblocks)` mapped to `ObuTooLarge`, then `if num_subblocks > r.bytes_remaining()` returning `UnexpectedEndOfInput` at `InputOffset(start)`. `:452` `bounded_vec(num_subblocks)`.
- `:492-527` `write_parameter_block` calls `validate_parameter_block(def, context, block)?` first. `:529-620` `pub(crate) fn validate_parameter_block(def, context: &ParameterDataContext, block)`; at `:585-592` `let actual_count = u32::try_from(block.subblocks.len())` mapped to `ObuTooLarge` at `Field("subblocks")`, then `if actual_count != expected_count` returns `SubblockDurationMismatch` at `Field("subblocks")`.
- `:622-657` `validate_parameter_data_kind`, then `:659-683` `subblocks_implied_by` with its doc and `// ref: ... GetNumSubblocks`. `ParameterDataContext` variants used: `MixGain`, `Demixing`, `ReconGain { recon_gain_is_present: Vec<bool> }`, `Reserved(u8)`.
- `src/error.rs:170-219`: `ErrorKind` ends with `OpusStartTrimExceedsPreSkip`, `OpusStartTrimShortOfPreSkip,` then `}`. Style example with doc + ref + DISAGREEMENT: `DuplicateAnchorElement` (`:179-184`). `size_of::<Error>() <= 32` const assertion lives in `src/error.rs`; a unit variant does not affect it.
- `tests/temporal.rs:14-26`: already imports `hex`, `BitCursor`, `BitWriter`, `ErrorKind`, `Location`, `BlockDurationFields`, `DemixingInfoParameterData`, `DurationFields`, `MixGainParameterData`, `ParamDefinition`, `ParamDefinitionRegistry`, `ParameterBlock`, `ParameterData`, `ParameterDataContext`, `ParameterSubblock`, `ReconGainInfoParameterData`, `read_parameter_block`, `write_parameter_block`. `:802-819` test `a_subblock_count_larger_than_the_bytes_remaining_is_refused_before_reserving` (MixGain, kind only). `:822-829` helper `fn registry_with(definition, context) -> ParamDefinitionRegistry`. `:832` `demixing_data_uses_exactly_three_mode_bits_and_five_reserved_bits`, then `test_000059_two_layer_recon_gain_shape_round_trips_exact_bytes`. `ParamDefinition::mode_1(id, rate)` exists; `ParamDefinition` has pub fields `parameter_id`, `parameter_rate`, `reserved`, `duration_fields: Option<DurationFields>`; `DurationFields { duration, constant_subblock_duration, subblock_durations: Vec<u32> }`; `BlockDurationFields { duration, constant_subblock_duration }`; `ParameterSubblock { subblock_duration: Option<u32>, data }`; `DemixingInfoParameterData { dmixp_mode, reserved }`; `ReconGainInfoParameterData { layers: Vec<Option<ReconGainElement>> }`; `MixGainParameterData::Step { start_point_value: i16 }`.
- `tests/allocation_bounds.rs`: module doc `:1-31` lists what the file proves; imports `iamf::bits::BitCursor`, `iamf::obu::{read_audio_element, read_mix_presentation}`, `iamf::{ErrorKind, Location}`; `const TWO_MEBIBYTES: usize = 1 << 21;` and helper `two_mebibyte_buffer(prefix) -> Vec<u8>`; the last hostile test is `hostile_parameter_count_in_two_mebibyte_audio_element_errors_at_offset_12` (`:100-115`), followed by the source-scan gate.
- `.planning/codebase/CONCERNS.md:50-55`: "Allocation amplification from count fields" entry; its "Residuals:" bullet (1) mentions ~24 bytes per empty annotation string.
- Consumer impact (research Q4): Parallax does not match on `iamf::ErrorKind`; the encoder cannot emit Demixing/Recon Gain blocks because `EncoderBuilder::build` rejects any Audio Element params (`src/encoder.rs:1314-1322`, `UnsupportedParameterData` at `Field("audio_element_params")`); `HANDOFF.md:52-62` already covers "the underlying parameter writer's typed validation errors". So `HANDOFF.md` needs NO change.
- Pinned references were verified in research at `git -C docs/iamf show v1.1.0:index.bs`, `git -C docs/iamf-tools show 848c6ff4:...`, `git -C docs/libiamf show f06e919e:...`. Do not cite the unpinned working trees.

Project constraints: Rust 1.85 (no let-chains; `matches!` is fine). In `src/`: no `unwrap`/`expect`/indexing/bare arithmetic, no new `#[allow]`. In `tests/*.rs`: `expect`/`expect_err`/`unwrap` only inside `#[test]` fn bodies; helper fns must be panic-free and index-free (use `.get()`, `.first()`, `.err().map(..)`). Do not change any existing `ErrorKind` variant or message; append only. The scratch prototype (research `scratchpad/1mq.diff`, `scratchpad/1mq/tests/zz_1mq_fix.rs`) may be consulted, but re-implement in the repo.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Demixing and Recon Gain Parameter Blocks carry exactly one subblock (read and write), RED first</name>
  <files>src/error.rs, tests/temporal.rs, tests/allocation_bounds.rs, src/obu/parameter_block.rs</files>
  <behavior>
    - H1 (allocation_bounds) ReconGain [false;7], mode_1(1, 48_000): `two_mebibyte_buffer(&[0x01, 0xFB, 0xFF, 0x7F, 0x01])` (duration 2_097_147, csd 1, implied count == bytes left) -> Err SubblockCountNotOne at InputOffset(0)
    - H2 ReconGain [false;2]: `01 ff ff ff 7f 01` (duration 0x0FFFFFFF, csd 1) -> SubblockCountNotOne at InputOffset(0) (new kind wins over the byte rule)
    - H3 ReconGain [false]: `01 02 00 02 01 01` (duration 2, csd 0, num_subblocks 2, durations 1+1) -> SubblockCountNotOne at InputOffset(0)
    - H4 ReconGain [false;2] under a mode-0 definition {parameter_id 1, parameter_rate 48_000, reserved 0, duration_fields Some(duration 2, csd 1, [])}: `01 00 00` -> SubblockCountNotOne at InputOffset(0)
    - H5 Demixing, mode_1(5, 48_000): `05 02 01 bb bb` (duration 2, csd 1) -> SubblockCountNotOne at InputOffset(0)
    - H6 Demixing, mode_1(5, 48_000): `05 00 00 00` (duration 0, csd 0, num_subblocks 0) -> SubblockCountNotOne at InputOffset(0) (the rule is "not 1", not "more than 1")
    - P1 ReconGain [false;2], mode_1(1, 48_000): `01 c0 07 c0 07` (duration 960, csd 960) -> Ok, subblocks.len() == 1, first data == ReconGain { layers: [None, None] }, cursor byte_position 5; write_parameter_block of it reproduces the 5 bytes
    - C1 (control) MixGain, mode_1(1, 48_000): `01 02 01 00 00 01 00 ff ff` -> Ok, 2 subblocks, second data Step { start_point_value: -1 }; writes back the 9 bytes
    - W1 write_parameter_block, Demixing, mode_1(5, 48_000), block {parameter_id 5, duration_fields Some(duration 2, csd 1), two subblocks each subblock_duration None, Demixing { dmixp_mode 0, reserved 0 }} -> Err SubblockCountNotOne at Field("subblocks")
  </behavior>
  <action>
    Step 1 (variant first, so the RED tests compile; no behaviour change). In `src/error.rs` append after
    `OpusStartTrimShortOfPreSkip,` a unit variant `SubblockCountNotOne` in the `DuplicateAnchorElement` comment style:
    doc line `/// A Demixing or Recon Gain Parameter Block does not carry exactly one subblock.`, then
    `// ref: IAMF v1.1.0 index.bs:776-782 and :786-792 (Demixing / Recon Gain: num_subblocks SHALL be set to 1)`,
    `// ref: iamf-tools@v2.1.0 iamf/obu/param_definitions.cc:37-60 ValidateSpecificParamDefinition (via parameter_block.cc:350 write, :376 read)`,
    `// DISAGREEMENT: libiamf@v1.1.0 code/src/iamf_dec/IAMF_OBU.c:1103-1129 iamf_parameter_new allocates nb_segments unchecked`,
    and `#[error("a Demixing or Recon Gain Parameter Block does not have exactly one subblock")]`. Change nothing else in
    `ErrorKind`.

    Step 2 (tests). In `tests/temporal.rs`, directly after `a_subblock_count_larger_than_the_bytes_remaining_is_refused_before_reserving`
    (before `registry_with`), add a comment header "Demixing / Recon Gain carry exactly one subblock (review [MEDIUM], 2026-09-13; quick 260914-1mq)"
    and these `#[test]`s, each with a one-line hand decode of its bytes (D-25 style) and assertions only inside the test
    body. Assert kind AND location for every error, e.g.
    `assert_eq!(read_parameter_block(&mut r, &registry).err().map(|e| (e.kind().clone(), e.at())), Some((ErrorKind::SubblockCountNotOne, Location::InputOffset(0))));`.
    Use `registry_with` for registries. Names exactly:
    `a_recon_gain_count_implied_by_a_hostile_duration_is_refused_before_the_byte_rule` (H2),
    `an_explicit_recon_gain_subblock_count_of_two_is_refused` (H3),
    `a_mode_0_recon_gain_definition_implying_two_subblocks_is_refused` (H4),
    `two_demixing_subblocks_are_refused_at_the_block_start` (H5),
    `a_demixing_block_with_zero_subblocks_is_refused` (H6),
    `a_single_recon_gain_subblock_with_every_layer_absent_reads_and_round_trips` (P1; comment that this legal block
    failed before because the byte rule saw 1 subblock and 0 bytes left),
    `a_multi_subblock_mix_gain_block_is_still_accepted_and_round_trips` (C1),
    `the_writer_refuses_two_demixing_subblocks` (W1).
    In `tests/allocation_bounds.rs`, add `ParamDefinition, ParamDefinitionRegistry, ParameterDataContext, read_parameter_block`
    to the `iamf::obu` import and, after `hostile_parameter_count_in_two_mebibyte_audio_element_errors_at_offset_12`, add
    `hostile_recon_gain_subblock_count_in_two_mebibyte_parameter_block_errors_at_offset_0` (H1) with an offset-by-offset
    comment in that file's style (offset 0 parameter_id 1; 1..4 duration FB FF 7F = 2_097_147 = bytes left; 4 csd 1;
    implied count 2_097_147 zero-byte subblocks with ReconGain [false; 7]; before the fix this parsed Ok and built
    2_097_147 subblocks). Build the registry inline in the test body. Add one bullet to the module doc's "What this file
    proves" list: a Demixing/Recon Gain subblock count other than 1, which can be derived rather than paid for in wire
    bytes, is refused before any subblock is reserved (quick 260914-1mq).

    Step 3 (RED). From `/Users/cell/local/iamf-rs` run
    `mkdir -p target && cargo test --locked --no-fail-fast --test temporal --test allocation_bounds > target/1mq-red.log 2>&1 || true`
    (`--no-fail-fast` so the second binary runs after the first fails; the H1 RED run on HEAD allocates a few hundred MB
    once, which is expected). The log must show `FAILED` for H1, H2, H3, H4, H5, H6, P1 and W1, and `ok` for C1,
    `a_subblock_count_larger_than_the_bytes_remaining_is_refused_before_reserving`,
    `demixing_data_uses_exactly_three_mode_bits_and_five_reserved_bits`,
    `test_000059_two_layer_recon_gain_shape_round_trips_exact_bytes` and
    `hostile_parameter_count_in_two_mebibyte_audio_element_errors_at_offset_12`. If any differ, stop and report the log
    excerpt; do not adjust expectations to match HEAD.

    Step 4 (GREEN) in `src/obu/parameter_block.rs` (research Q3 option (a)):
    1. Add a private `fn check_single_subblock(context: &ParameterDataContext, count: u32, at: Location) -> Result<()>`
       directly before the `subblocks_implied_by` doc comment. It returns `Err(Error::new(ErrorKind::SubblockCountNotOne, at))`
       when `context` matches `ParameterDataContext::Demixing | ParameterDataContext::ReconGain { .. }` and `count != 1`,
       else `Ok(())`. Plain-comment block above it:
       `// ref: IAMF v1.1.0 index.bs:776-782 DemixingParamDefinition (num_subblocks SHALL be set to 1)`,
       `// ref: IAMF v1.1.0 index.bs:786-792 ReconGainParamDefinition (num_subblocks SHALL be set to 1)`,
       `// ref: iamf-tools@v2.1.0 iamf/obu/param_definitions.cc ValidateSpecificParamDefinition`,
       a `// NOTE:` that the reference is stricter (mode 0 and constant_subblock_duration == duration != 0, checked on
       every block read and write) and only the count consequence is enforced here, the mode/csd rule being deferred
       (quick 260914-1mq deferred item 2), and
       `// DISAGREEMENT: libiamf@v1.1.0 code/src/iamf_dec/IAMF_OBU.c iamf_parameter_new allocates nb_segments without checking; spec and iamf-tools are stricter and win.`
       Add a `///` doc: Demixing and Recon Gain blocks carry exactly one subblock; the check runs before the count reaches
       an allocation because a Recon Gain subblock with every layer absent is zero wire bytes, so no byte rule bounds it.
    2. In `read_parameter_block`, after `include_subblock_duration` and BEFORE `usize::try_from(num_subblocks)`, call
       `check_single_subblock(context, num_subblocks, Location::InputOffset(start))?;` with a comment explaining the
       ordering (research pitfall 1: after it, a small hostile Recon Gain vector would report UnexpectedEndOfInput).
    3. Rewrite the `T-01-32` comment: every subblock except a Recon Gain one is at least one byte, so more subblocks than
       bytes left is unreadable by construction; a Recon Gain subblock can be zero bytes and its count is already exactly
       one. Exempt only Recon Gain from the byte rule: `let zero_byte_subblock_possible = matches!(context, ParameterDataContext::ReconGain { .. });`
       and `if !zero_byte_subblock_possible && num_subblocks > r.bytes_remaining()`. Keep the rule, its kind and offset
       unchanged for Demixing, Mix Gain and Reserved/Raw (research pitfall 2).
    4. In `validate_parameter_block`, right after `actual_count` is computed and before `if actual_count != expected_count`,
       call `check_single_subblock(context, actual_count, Location::Field("subblocks"))?;`.
    5. Add one sentence to the `read_parameter_block` `///` doc: a Demixing or Recon Gain block whose subblock count,
       explicit or derived, is not exactly 1 is `SubblockCountNotOne` at the payload start, before any subblock is reserved.

    Run `cargo fmt --all`. Commit by explicit path only:
    `git add src/error.rs src/obu/parameter_block.rs tests/temporal.rs tests/allocation_bounds.rs`, confirm
    `git diff --cached --name-only` lists exactly those four, then commit with message
    `fix(quick-260914-1mq): require exactly one Demixing or Recon Gain Parameter Block subblock`.
    Body: the review finding reference (`.planning/reviews/2026-09-13-CODEBASE-REVIEWS.md:124`); the measured
    amplification (zero-byte Recon Gain subblocks, up to about 204× model bytes per input byte at 2 MiB); the
    spec/iamf-tools/libiamf paragraph; the accepted-set changes (Demixing/Recon Gain blocks with a count other than 1 now
    fail with the new appended kind `SubblockCountNotOne`; the legal single all-absent Recon Gain block `01 c0 07 c0 07`
    changes from UnexpectedEndOfInput@0 to Ok); and the line `No semantic_sha256, golden artifact or fixture changed.`
  </action>
  <verify>
    <automated>cd /Users/cell/local/iamf-rs && for t in hostile_recon_gain_subblock_count_in_two_mebibyte_parameter_block_errors_at_offset_0 a_recon_gain_count_implied_by_a_hostile_duration_is_refused_before_the_byte_rule an_explicit_recon_gain_subblock_count_of_two_is_refused a_mode_0_recon_gain_definition_implying_two_subblocks_is_refused two_demixing_subblocks_are_refused_at_the_block_start a_demixing_block_with_zero_subblocks_is_refused a_single_recon_gain_subblock_with_every_layer_absent_reads_and_round_trips the_writer_refuses_two_demixing_subblocks; do grep -q "^test $t \.\.\. FAILED" target/1mq-red.log || { echo "RED missing: $t"; exit 1; }; done && for t in a_multi_subblock_mix_gain_block_is_still_accepted_and_round_trips a_subblock_count_larger_than_the_bytes_remaining_is_refused_before_reserving demixing_data_uses_exactly_three_mode_bits_and_five_reserved_bits test_000059_two_layer_recon_gain_shape_round_trips_exact_bytes hostile_parameter_count_in_two_mebibyte_audio_element_errors_at_offset_12; do grep -q "^test $t \.\.\. ok" target/1mq-red.log || { echo "control not green on HEAD: $t"; exit 1; }; done && cargo test --locked --test temporal && cargo test --locked --test allocation_bounds && cargo test --locked --test citations && cargo test --locked --test error_shape && cargo test --locked --test parse_reference && cargo test --locked --test golden && cargo test --locked --lib && grep -q 'fn check_single_subblock' src/obu/parameter_block.rs && grep -q 'check_single_subblock(context, num_subblocks' src/obu/parameter_block.rs && grep -q 'check_single_subblock(context, actual_count' src/obu/parameter_block.rs && grep -q 'ValidateSpecificParamDefinition' src/obu/parameter_block.rs && grep -q 'IAMF_OBU.c iamf_parameter_new' src/obu/parameter_block.rs && grep -qF 'SubblockCountNotOne,' src/error.rs && grep -qF '#[error("a Demixing or Recon Gain Parameter Block does not have exactly one subblock")]' src/error.rs && git diff --quiet HEAD -- src tests && cargo clippy --locked --all-targets -- -D warnings</automated>
  </verify>
  <done>H1-H6, P1 and W1 failed on HEAD and now pass; the controls stayed green throughout. Demixing and Recon Gain blocks with a subblock count other than 1 are refused with SubblockCountNotOne at the payload start before any reservation, and at Field("subblocks") on write. The legal single all-absent Recon Gain block reads and round-trips. Mix Gain and Raw behaviour and the existing error kinds/offsets are unchanged. Citations, error_shape, parse_reference and golden pass, clippy is clean, and exactly the four files are committed by explicit path.</done>
</task>

<task type="auto">
  <name>Task 2: Record the closed class and the residuals, write deferred items, run every release gate</name>
  <files>.planning/codebase/CONCERNS.md, .planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-deferred-items.md</files>
  <action>
    `.planning/codebase/CONCERNS.md`, "Allocation amplification from count fields" entry (`:50-55`): add a bullet after
    "Current mitigation" titled `Zero-byte Parameter Block subblocks — Resolved 2026-09-14 (quick task 260914-1mq).` It
    states: the review finding [MEDIUM] (`.planning/reviews/2026-09-13-CODEBASE-REVIEWS.md:124`) is closed; a Recon Gain
    subblock with every layer absent is zero wire bytes, so `bytes_remaining()` did not bound its derived count (about
    204× model bytes per input byte at 2 MiB); Demixing and Recon Gain blocks now require exactly one subblock
    (`SubblockCountNotOne`, spec `index.bs:776-792`, iamf-tools `ValidateSpecificParamDefinition`), checked before
    allocation, and `tests/allocation_bounds.rs` pins the 2 MiB vector. Extend "Residuals" (1) so it names the remaining
    linear classes, all at least one wire byte per element and bounded by the 2 MiB OBU cap: extension (Raw,
    `param_definition_type` >= 3) subblocks about 40× exact / 64× with Vec growth slack, Mix Gain Step subblocks about
    13×/16×, `read_strings` NUL strings about 24-32×. State that the review's 38 MB -> 1.94 GB RSS scenario is still
    reachable through extension-type blocks, and that a per-parse memory budget is the only hard bound and an open user
    decision (260914-1mq deferred item 1). Edit only this entry.

    Commit by explicit path: `git add .planning/codebase/CONCERNS.md`, confirm `git diff --cached --name-only` is exactly
    that file, message `docs(quick-260914-1mq): record the zero-byte subblock class as closed and name the linear residuals`.

    Create (do NOT commit) `.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-deferred-items.md`
    headed `# 260914-1mq deferred items`, following the numbered style of `260914-0sp-deferred-items.md`:
    1. **Linear parse-memory residuals and a per-parse memory budget.** Raw extension subblocks (1 byte -> 40×/64×), Mix
       Gain Step (3 bytes -> 13×/16×), `read_strings` (`src/obu/mix_presentation.rs`, about 24-32×) remain; the review
       RSS scenario stays reachable via `param_definition_type` >= 3. Option (d) from research Q3: a per-parse budget
       such as `parse_sequence_with_limits`, which is an API change for Parallax and needs a user decision.
    2. **Optional `validate()` finding for the full iamf-tools Demixing/Recon Gain definition rule** (`param_definition_mode`
       = 0, `constant_subblock_duration` = `duration` != 0; `iamf/obu/param_definitions.cc:37-60`). Churn: mode-1 models
       in `src/fuzzing.rs:103-110`, `tests/support/sequence_cases.rs:119-123` and `tests/temporal.rs` (around the Demixing
       and Recon Gain tests). Include the research question verbatim in both languages:
       EN: "Should the full iamf-tools rule for Demixing/Recon Gain definitions (`param_definition_mode` = 0, `constant_subblock_duration` = `duration` ≠ 0) become a `validate()` finding? That means test churn in the fuzz model and `sequence_cases`, which use mode 1."
       DE: "Soll die vollständige iamf-tools-Regel für Demixing-/Recon-Gain-Definitionen (Modus 0, `constant_subblock_duration` = `duration` ≠ 0) zusätzlich als `validate()`-Finding gemeldet werden? Diese Aufgabe erzwingt nur die Folge daraus (genau 1 Subblock). Option A: jetzt nicht (empfohlen, eigener Quick-Task). Option B: als Finding im selben Task."
       Add a note that 260914-1mq took Option A, so Option B now means a follow-up quick task, and that a parse-side check
       would also need the reference-hash constraint checked first.
    3. **Assumption A1 not run against a binary.** That iamf-tools and libiamf accept a single all-absent Recon Gain block
       comes from reading the pinned source; no reference binary decoded such a file. Low risk (the change only turns an
       error into Ok for count 1).

    Gates, from `/Users/cell/local/iamf-rs`, logs under `target/`:
    1. `cargo fmt --all -- --check`
    2. `cargo build --locked --all-targets`
    3. `cargo clippy --locked --all-targets -- -D warnings`
    4. `cargo clippy --locked --all-targets --features fuzzing -- -D warnings`
    5. `cargo test --locked > target/1mq-full.log 2>&1; echo "full_exit=$?" >> target/1mq-full.log`
    6. `cargo test --locked --features fuzzing --test fuzz_regression`
    7. Float census: `rg -U -c 'allow\(\s*clippy::disallowed_types' src/` prints exactly `src/model/loudness.rs:1` (the
       single-line census pattern is known stale, see 260914-0sp deferred item 1).
    8. Protected paths unchanged since the base commit:
       `git diff --quiet 64d2eb3 -- tests/support/reference_expectations.rs tests/fixtures tests/golden.rs DIFF-LEDGER.md HANDOFF.md fuzz tools src/sequence.rs src/encoder.rs src/fuzzing.rs src/bits`.
    9. Committed scope: write `git diff --name-only 64d2eb3 HEAD -- src tests` to `target/1mq-src-tests.txt` and
       `git diff --name-only 64d2eb3 HEAD` to `target/1mq-committed.txt` before inspecting them.
    If fmt or clippy needs a fix, make a follow-up commit by explicit path titled `style(quick-260914-1mq): ...`; amend
    nothing. If `tests/parse_reference.rs` reports any `semantic_sha256` mismatch, or the golden test fails, stop and
    report: research predicted none and the reference-hash constraint forbids adopting one here. If a gate fails for an
    unrelated reason, stop and report the log excerpt. Do not edit fixtures, goldens, reference expectations,
    DIFF-LEDGER.md, HANDOFF.md or conformance code. Do not commit PLAN, SUMMARY, STATE, RESEARCH or the deferred-items file.
  </action>
  <verify>
    <automated>cd /Users/cell/local/iamf-rs && D=.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-deferred-items.md && test -f "$D" && grep -q 'parse_sequence_with_limits' "$D" && grep -q 'Soll die vollständige iamf-tools-Regel' "$D" && grep -q 'Assumption A1' "$D" && grep -q '260914-1mq' .planning/codebase/CONCERNS.md && grep -q 'SubblockCountNotOne' .planning/codebase/CONCERNS.md && git diff --quiet HEAD -- .planning/codebase/CONCERNS.md && cargo fmt --all -- --check && cargo build --locked --all-targets && cargo clippy --locked --all-targets -- -D warnings && cargo clippy --locked --all-targets --features fuzzing -- -D warnings && grep -q '^full_exit=0$' target/1mq-full.log && [ -z "$(grep '^test result: ' target/1mq-full.log | grep -v '^test result: ok')" ] && cargo test --locked --features fuzzing --test fuzz_regression && [ "$(rg -U -c 'allow\(\s*clippy::disallowed_types' src/)" = "src/model/loudness.rs:1" ] && git diff --quiet 64d2eb3 -- tests/support/reference_expectations.rs tests/fixtures tests/golden.rs DIFF-LEDGER.md HANDOFF.md fuzz tools src/sequence.rs src/encoder.rs src/fuzzing.rs src/bits && git diff --quiet HEAD -- src tests && git diff --name-only 64d2eb3 HEAD -- src tests > target/1mq-src-tests.txt && [ "$(tr '\n' ' ' < target/1mq-src-tests.txt)" = "src/error.rs src/obu/parameter_block.rs tests/allocation_bounds.rs tests/temporal.rs " ] && git diff --name-only 64d2eb3 HEAD > target/1mq-committed.txt && [ -z "$(grep -vE '^(src/error\.rs|src/obu/parameter_block\.rs|tests/allocation_bounds\.rs|tests/temporal\.rs|\.planning/codebase/CONCERNS\.md)$' target/1mq-committed.txt)" ]</automated>
  </verify>
  <done>CONCERNS.md records the zero-byte subblock class as closed and names the remaining linear residuals and the open per-parse budget decision, committed by explicit path. The deferred-items file exists with the three items, including the EN/DE question verbatim, and is not committed. fmt, build, both clippy runs, the full offline suite (every binary ok, including golden, parse_reference, citations and error_shape), fuzz replay and the float census pass. Reference expectations, fixtures, goldens, DIFF-LEDGER.md, HANDOFF.md, sequence/encoder/fuzzing code and the bit layer are untouched, and only the five planned files are committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| foreign .iamf bytes -> parse_sequence / read_parameter_block | Hostile `duration`, `constant_subblock_duration`, `num_subblocks` and a descriptor-owned Recon Gain context with every layer absent reach the subblock allocation |
| model -> write_parameter_block / write_parsed_sequence | A caller-built or parsed model with a non-conformant subblock count reaches the writer |
| iamf -> Parallax | Error kinds and offsets for malformed input are part of the consumer contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1mq-01 | Denial of Service (memory amplification, CWE-789) | `read_parameter_block` subblock loop for Recon Gain with all layers absent | high | mitigate | Task 1 calls `check_single_subblock` before `usize::try_from`, the byte rule and `bounded_vec`, so a Demixing/Recon Gain count other than 1 allocates nothing. H1 proves the 2 MiB vector (2_097_147 derived subblocks on HEAD) is refused at offset 0 |
| T-1mq-02 | Denial of Service (residual linear amplification, CWE-400) | extension Raw subblocks, Mix Gain Step subblocks, `read_strings` | medium | accept | Each costs at least 1 wire byte per element and is bounded by the 2 MiB OBU cap; recorded in CONCERNS.md residuals and deferred item 1 (per-parse budget needs a user decision and an API change) |
| T-1mq-03 | Tampering (non-conformant output) | `write_parameter_block` | medium | mitigate | Task 1 mirrors the check in `validate_parameter_block` at `Field("subblocks")` (W1), so the writer cannot emit a multi-subblock Demixing/Recon Gain block that iamf-tools would reject |
| T-1mq-04 | Denial of Service (panic) | new check and byte-rule condition | medium | mitigate | `matches!` and a `u32` comparison only; no indexing, bare arithmetic or unwrap. Enforced by clippy `indexing_slicing`/`arithmetic_side_effects` with and without `fuzzing`, and by fuzz replay in Task 2 |
| T-1mq-05 | Repudiation (silent validation drift) | reference `semantic_sha256`, golden | low | mitigate | Task 1 runs parse_reference and golden; Task 2 gates `git diff --quiet 64d2eb3` on expectations, fixtures and golden and runs the full suite. Any hash mismatch is stop-and-report |
| T-1mq-06 | Information disclosure / contract drift | `ErrorKind` seen by Parallax | low | accept | Append-only unit variant on a `#[non_exhaustive]` enum, `size_of::<Error>()` unchanged; the encoder cannot reach it (Audio Element params rejected in `build()`), Parallax does not match on `iamf::ErrorKind`, and HANDOFF.md already covers parameter-writer validation kinds. The accepted-set changes are documented in the commit body |
</threat_model>

<verification>
- `target/1mq-red.log` shows H1-H6, P1 and W1 FAILED on HEAD and the five controls ok.
- `cargo fmt --all -- --check`; `cargo build --locked --all-targets`; `cargo clippy --locked --all-targets -- -D warnings` with and without `--features fuzzing`.
- `cargo test --locked` passes in every binary (including `temporal`, `allocation_bounds`, `citations`, `error_shape`, `golden`, `parse_reference`); `cargo test --locked --features fuzzing --test fuzz_regression` passes.
- `rg -U -c 'allow\(\s*clippy::disallowed_types' src/` prints exactly `src/model/loudness.rs:1`.
- `git diff --quiet 64d2eb3 -- tests/support/reference_expectations.rs tests/fixtures tests/golden.rs DIFF-LEDGER.md HANDOFF.md fuzz tools`.
</verification>

<success_criteria>
- No Demixing or Recon Gain Parameter Block with a subblock count other than 1 is read or written; the read rejection happens before any subblock is reserved, at the payload start, with the appended `SubblockCountNotOne`.
- The legal single all-absent Recon Gain block is accepted and round-trips byte-identically.
- Mix Gain and extension blocks, all existing error kinds/offsets, reference hashes, goldens and fixtures are unchanged.
- The spec/iamf-tools/libiamf disagreement is recorded in `// ref:` / `// NOTE:` / `// DISAGREEMENT:` comments; CONCERNS.md names the closed class and the remaining linear residuals; the deferred decisions are written down.
</success_criteria>

<output>
Create, and do NOT commit (the orchestrator handles them):
- `.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-SUMMARY.md`
- `.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-deferred-items.md` (content specified in Task 2)
</output>
