---
context: default
phase: none (v1 phases 01-04 complete; post-v1 conformance hardening via quick tasks)
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-14T00:51:52.010Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through a real failure or a near-miss in the 2026-09-13/14 sessions.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: Conformance rule.** Satisfy BOTH the IAMF v1.1.0 spec AND the pinned references
  (`iamf-tools` v2.1.0 `848c6ff`, `libiamf` v1.1.0 `f06e919`).
  - Where the spec is stricter, the spec wins. Where a reference is stricter, keep that rule too.
  - User decision 2026-09-13. Record every spec/reference disagreement in a `// ref:` comment.
- [ ] **CONSTRAINT: Reference hashes.** A `semantic_sha256` in `tests/support/reference_expectations.rs`
  may change ONLY for a fixture that the pinned `iamf-tools` testdata marks `is_valid: false`.
  Predict the new hash in research, verify it exactly; any other hash change → stop and report.
- [ ] **CONSTRAINT: Pinned trees only.** Read references only at the pinned commits:
  `git -C docs/iamf-tools show 848c6ff4:<path>`, `git -C docs/iamf show v1.1.0:index.bs`,
  `git -C docs/libiamf show f06e919e:<path>`. The `docs/` working trees are newer drafts.
- [ ] **CONSTRAINT: Staging discipline.** `docs/` holds ~8.6 GB of untracked upstream clones and is NOT in
  `.gitignore` (now also `docs/iamf-tests/` and `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md`).
  Never `git add -A` / `git add .` / `git commit -a`. Stage explicit paths; check
  `git diff --cached --name-only` before every commit.
- [ ] **CONSTRAINT: Licence — FFmpeg source.** `docs/iamf-tests/` (github.com/iDestyKK/iamf-tests) holds
  shell scripts that *invoke* the `ffmpeg` CLI. Running the binary is allowed (`REFERENCES.md` §licence
  table); reading `libavformat` source is not. The repo has **no LICENSE file** → copy nothing from it
  into this repo (scripts, samples); use it for ideas only.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Placeholder template ids | `build()` validates the caller's template, where ids are placeholders. A naive duplicate-id check broke the Parallax delivery. | blocking | Check duplicate **handles** first, then validate a handle-lowered clone (pattern from `260913-th8`). |
| "Decoder accepts it" as proof | `decoder_main` ignores trim placement and `pre_skip`; `libiamf` silently decodes 0 frames under a wrong profile; `iamfdec` exits 0 on corrupt input. | blocking | Derive rules from pinned source and spec. Test a positive file and a control that must fail. Judge reference logs only by "0 of N samples differ". |
| A guard that is green because it sees nothing | The documented float census (`rg 'allow.*disallowed_types' src/`) returned 0 hits for months because rustfmt split the attribute — "0" looked like "clean". Fixed in `260914-38r`. | blocking | Every guard needs a `tools/prove-guards.sh` case that (1) passes on the clean copy and (2) fires with the exact exit code and hit count on an injected violation. |
| Ambiguous numbered replies | User answered "1" when two numbered lists were on screen. | advisory | When presenting options after another list, confirm with a one-question AskUserQuestion. |
| Stale IDE diagnostics | rust-analyzer reported non-existent errors after executor commits. | advisory | Trust `cargo check --all-targets` / `cargo test --locked`. |
| Hanging `docker images` / no `timeout` binary | macOS has no `timeout`/`gtimeout`. | advisory | Wrap docker-dependent runs in a perl alarm; report SKIPPED / TIMED OUT honestly. |
| Host-dependent FLAC/Opus CONF-05 | Local `iamfdec` on macOS has no FLAC/Opus (`dep_codecs_disabled: true`). | advisory | Real FLAC/Opus CONF-05 runs only in the `reference` workflow on x86_64 Linux. |
| Worktree base mismatch | GSD quick executors degrade to sequential on `main` whenever local HEAD is ahead of origin. | advisory | Expected; user approved sequential execution on `main`. Push before a task if worktrees are wanted. |

<current_state>
All v1 phases (01–04) are complete. Post-v1 conformance hardening continues as quick tasks.

- **Remote:** everything committed and pushed. `origin/main` = `d629570` (before this WIP handoff commit).
- **CI on `d629570`:** run 34793732084 green on all 4 targets + "licence gate + guardrail proof"
  (`guardrail proof: 9 passed, 0 failed (9 expected)`). The `reference` workflow was not triggered by this
  push (no `src/` change); last reference run 34790729874 on `375fcaf` green with `[phase3_flac]` 0/600 and
  `[phase3_opus]` 0/3214 differing samples, limiter delta 0.
- **Working tree:** clean except untracked, user-owned `docs/` clones, `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md`
  (not read yet this session) and `.DS_Store` files. Do not commit them.
- **A previous side session crashed** after 260914-1mq; this session picked up from its last report.
</current_state>

<completed_work>

Since the previous handoff (`2d61359`, after 260913-wlv):

| Task | What it did | Commit |
|---|---|---|
| 260914-0sp | OBU header after-size fields (trim, extension) bounded by `obu_size` | 1b76415 |
| 260914-1mq | Exactly one Demixing / Recon Gain Parameter Block subblock (`SubblockCountNotOne`); closes zero-byte subblock memory amplification; CONCERNS.md names remaining linear residuals | 16eca4a, 5708c33 |
| 260914-38r | Float escape census rebuilt as `tools/check-float-escape-census.sh` (perl, multi-line aware, also catches `expect`, `cfg_attr`, `clippy::all`/`clippy::style`); `prove-guards.sh` cases (g)(h)(i), 6 → 9 PASS; all docs point at the script | 35cd1ea, 2dc6990, 0ee6dc8, d629570 |

Also this session: evaluated `docs/iamf-tests/` (see decisions).
</completed_work>

<remaining_work>

Recommended order (the user's menu from the crashed session, minus the done float census):

1. **Unknown extension param definition aborts the whole parse** — conformance risk, confirmed in code:
   `src/obu/param_definition.rs:119-129` calls `read_param_definition(&mut reader)?` on the opaque bytes of
   `AudioElementParam::Extension`. The spec only guarantees `param_definition_size` + opaque bytes, so a legal
   0- or 2-byte extension definition errors the parse. Plan: `/gsd-quick` with TDD — red test with a
   spec-legal extension definition lacking the shared prefix; then don't register it (and/or emit a `Finding`)
   instead of `?`. Check the reference-hash constraint first. Bundle with the review item "low-level writers
   accept states that don't round-trip" (loudness extension bits, reserved enum aliases, extension param
   types 0–2, `DurationFields`, ambisonics lengths).
2. **Per-parse memory budget** (`260914-1mq-deferred-items.md` item 1) — needs `--discuss`; API change for
   Parallax (e.g. `parse_sequence_with_limits`). Residuals: Raw extension subblocks ~40×/64×, Mix Gain Step
   ~13×/16×, `read_strings` ~24–32×; 38 MB → 1.94 GB RSS still reachable via `param_definition_type >= 3`.
3. **BCP-47 `annotations_language`** (`260913-wlv-deferred-items.md` item 1) — `--discuss` on strictness.
4. **MixPresentationTags presence rule** (wlv item 2) — model change first (tags are raw `trailing` bytes).
5. **Optional:** full iamf-tools Demixing/Recon Gain definition rule as a `validate()` finding (1mq item 2,
   Option B); FFmpeg third-lineage interop sweep (see decisions).
6. Older deferred items are listed in the previous handoff (`git show 2d61359:.planning/.continue-here.md`):
   parse-side `validate()` parity (n56/o3k/lta/vvx), binaural 1/1 in `decoder_main`, scalable per-layer
   counts, Ambisonics projection coupling, demixing/recon-gain duration rules, separate FLAC/Opus Parallax
   deliveries, rustdoc link at `src/sequence.rs:669`, LOW review items (AAC roll distance, duplicate
   `parameter_id` binds to first definition).
7. **Hygiene:** absolute local path on `CLAUDE.md` line 7 in a public repo; `docs/` not in `.gitignore`.
</remaining_work>

<decisions_made>

All decisions from the previous handoff still hold (conformance rule, single Codec Config, `parameter_rate` ==
sample rate, Opus start trim == `pre_skip`, profile scopes per errata, ASCII-case-insensitive language tags,
`num_layouts >= num_layers` finding-only, low-level writers byte-exact, sequential execution on `main`).

New this session:
- **Float census uses perl, not `rg -U`:** no evidence `rg` exists on the Ubuntu CI runner; perl ships on
  Ubuntu and macOS. Single shared script so docs and proof cannot drift. Known limits (in script header):
  macro-generated attributes invisible; `/* */` and trailing `//` comments not stripped (false positive fails
  loudly). The line-based census test in `tests/profile.rs` stays (runs on Windows, no bash).
- **`docs/iamf-tests/` verdict:** useful as ideas only. 9 ffmpeg scripts muxing IAMF into MP4 to test YouTube
  (YouTube = v1.0 Base). Samples are silence, outputs not committed, no licence. Takeaways:
  - 9.1.6 ❌ on YouTube matches our rule: expanded layout (`loudspeaker_layout = 15`) needs Base-Enhanced.
  - 5.1+FOA ❌ is within Base limits → likely a YouTube renderer issue; worth a note in `HANDOFF.md` for Parallax.
  - Two scripts share `parameter_id=101` across elements → a `validate()` test idea.
  - Local `ffmpeg 9.0.1` has the raw `-f iamf` muxer → possible third-lineage writer: generate the 9 configs as
    raw `.iamf` in the scratchpad, run `parse_sequence` → `validate()` → `select_minimum_profile`. Not started;
    user has not chosen it. MP4/containers stay out of scope.
</decisions_made>

<blockers>
- **None.** No external jobs outstanding.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — "Quick Tasks Completed" table and Session Continuity.
2. `.planning/quick/260913-qk3-enforce-iamf-v1-1-0-profile-restrictions/260913-qk3-CONTEXT.md` — binding conformance rule.
3. `.planning/quick/260914-1mq-bound-parameter-block-subblock-memory-am/260914-1mq-deferred-items.md` — memory budget decision.
4. `.planning/quick/260913-wlv-enforce-unique-annotations-languages-sca/260913-wlv-deferred-items.md` — BCP-47, MixPresentationTags.
5. `.planning/reviews/2026-09-13-CODEBASE-REVIEWS.md` — still-open review findings (extension param, writers).
6. `REFERENCES.md` (licence table incl. FFmpeg) and `CONFORMANCE-GATE.md`.
7. `.planning/METHODOLOGY.md`, if it exists.

## Critical Anti-Patterns (do NOT repeat these)
- **Trusting decoder acceptance or exit code 0 as conformance** → pinned source + spec, positive and negative controls.
- **A guard without a firing proof** → add a `prove-guards.sh` case that also passes on the clean copy.
- **Naive id-based validation of builder templates** → validate a handle-lowered clone.
- **Adopting a changed reference hash without upstream `is_valid: false`** → stop and report.
- **Broad staging** → explicit paths only.

## Infrastructure State
- **Reference decoder:** `IAMF_REF_DECODER=/Users/cell/local/iamf-rs/.reference/libiamf/code/test/tools/iamfdec/iamfdec`
  (gitignored, no FLAC/Opus, `host_triple: x86_64-apple-darwin`).
- **Docker image:** `iamf-tools:v2.1.0` (sha256 `4a011a6d…`) present locally for CONF-06.
- **ffmpeg:** `/usr/local/bin/ffmpeg` 9.0.1 with raw `iamf` muxer (binary use only).
- **Toolchain:** Rust 1.85.0, pinned. No let-chains.
- **GitHub Actions:** `reference` workflow ~11 min; once hung 16 h in "Build the digest-pinned iamf-tools image".
  CI ~15 min; `guardrails` job now expects 9 PASS.

<context>
The user wants iamf-rs strictly IAMF-conformant. Method per quick task: `--research` when rules come from the
references (scratch prototype, predicted hash) → planner → sequential executor on `main` → orchestrator
re-verification (re-run the key commands, don't trust the executor's report) → STATE.md row + docs commit →
push on request → watch CI. The user communicates in German; present results in German with technical
identifiers in English. Explanatory output style: include short ★ Insight blocks.
</context>

<next_action>
Start with: `git status -sb` and `gh run list --limit 3` to confirm `origin/main` includes this handoff and CI is
green. Then ask the user whether to start item 1 (extension param definition aborts parse) as `/gsd-quick` with
TDD, or item 2 (memory budget) via `--discuss`.
</next_action>
