---
context: default
phase: none (v1 phases 01-04 complete; post-v1 conformance hardening via quick tasks; today also merged AAC-LC and tagged a release)
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-14T18:20:38.829Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through a real failure or a near-miss in this session
> (2026-09-14, continuing the 2026-09-13/14 series). Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: Conformance rule.** Satisfy BOTH the IAMF v1.1.0 spec AND the pinned references
  (`iamf-tools` v2.1.0 `848c6ff`, `libiamf` v1.1.0 `f06e919`).
  - Where the spec is stricter, the spec wins. Where a reference is stricter, keep that rule too.
  - User decision 2026-09-13. Record every spec/reference disagreement in a `// ref:` comment.
- [ ] **CONSTRAINT: Reference hashes.** A `semantic_sha256` in `tests/support/reference_expectations.rs`
  may change ONLY for a fixture that the pinned `iamf-tools` testdata marks `is_valid: false`.
  Predict the new hash in research, verify it exactly; any other hash change → stop and report.
- [ ] **CONSTRAINT: Pinned trees only.** Read references only at the pinned commits:
  `git -C docs/iamf-tools show 848c6ff4:<path>`, `git -C docs/iamf show v1.1.0:index.bs`,
  `git -C .reference/libiamf show f06e919e:<path>` (NOT `docs/libiamf`, which doesn't exist as a
  checkout on this machine — the pinned `libiamf` reference lives only in `.reference/libiamf`).
- [ ] **CONSTRAINT: Staging discipline.** `docs/` holds untracked upstream clones and is NOT in
  `.gitignore`. Never `git add -A` / `git add .` / `git commit -a`. Stage explicit paths; check
  `git diff --cached --name-only` before every commit.
- [ ] **CONSTRAINT: `git worktree add <path> <branch>` fails silently into a chained `cd` if the
  branch is already checked out elsewhere.** This session accidentally ran a trial `git merge` in
  the PRIMARY checkout because `git worktree add /tmp/x main` failed (branch `main` already checked
  out there) and the following `cd /tmp/x` in the same multi-line command also failed, leaving the
  shell in the primary checkout when `git merge` ran. Caught via `git status` before it did damage,
  aborted cleanly. **Prevention:** use a distinct branch name for scratch worktrees (`git worktree
  add <path> -b tmp-scratch-name <base>`), and always `git status`/`pwd`-check immediately after a
  worktree-add before running anything destructive in the "new" directory.
- [ ] **CONSTRAINT: A pre-existing worktree can hide real uncommitted work.** This session found
  `.worktrees/aac-lc-framing` — a leftover worktree from a crashed prior session — with substantial
  uncommitted, untested changes (a "delivery validator") that existed in NO commit and NO stash
  entry anywhere. It was one `git worktree remove --force` or orphan-reap sweep away from being lost
  permanently. **Prevention:** before removing or reaping ANY worktree, `git -C <worktree> status
  --porcelain` first; if non-empty, commit-and-branch it off before touching the worktree further.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Placeholder template ids | `build()` validates the caller's template, where ids are placeholders. A naive duplicate-id check broke the Parallax delivery. | blocking | Check duplicate **handles** first, then validate a handle-lowered clone (pattern from `260913-th8`). |
| "Decoder accepts it" as proof | `decoder_main` ignores trim placement and `pre_skip`; `libiamf` silently decodes 0 frames under a wrong profile; `iamfdec` exits 0 on corrupt input. | blocking | Derive rules from pinned source and spec. Test a positive file and a control that must fail. Judge reference logs only by "0 of N samples differ". |
| A guard that is green because it sees nothing | The documented float census (`rg 'allow.*disallowed_types' src/`) returned 0 hits for months because rustfmt split the attribute — "0" looked like "clean". Fixed in `260914-38r`. | blocking | Every guard needs a `tools/prove-guards.sh` case that (1) passes on the clean copy and (2) fires with the exact exit code and hit count on an injected violation. |
| Stale IDE diagnostics | rust-analyzer reported a non-existent `E0004` non-exhaustive-match error in `encoder.rs:1484` right after the AAC-LC merge commit landed; `cargo build` was clean. Recurs from the 2026-09-13 handoff. | advisory | Trust `cargo check --all-targets` / `cargo test --locked`, never the editor's live diagnostics, after any commit made by a subagent. |
| Ambiguous numbered replies | User answered "1" when two numbered lists were on screen. | advisory | When presenting options after another list, confirm with a one-question AskUserQuestion. |
| Hanging `docker images` / no `timeout` binary | macOS has no `timeout`/`gtimeout`. | advisory | Wrap docker-dependent runs in a perl alarm; report SKIPPED / TIMED OUT honestly. |
| Worktree base mismatch | GSD quick executors degrade to sequential on `main` whenever local HEAD is ahead of origin. | advisory | Expected; user approved sequential execution on `main` for the whole day. Push promptly if worktrees are wanted back. |

<current_state>
v1 phases (01–04) complete. Post-v1 conformance hardening continued as quick tasks all day
(2026-09-13/14). This session additionally: merged the long-lived `feature/aac-lc-framing` branch
into `main` (AAC-LC as a fourth, framing-only codec), tagged and pushed `v0.1.0-beta.2`, and updated
Parallax's portfolio-boundaries maturity row.

- **Remote (`iamf-rs`):** everything through `1d9c463` is on `origin/main`. Tag `v0.1.0-beta.2` is on
  `origin`. CI (`34875445083`) and `reference` (`34875446067`, with `IAMF_REFERENCE_ENABLE_AAC=1`,
  including `the_pinned_aac_lc_vector_round_trips_through_both_reference_oracles`) both green on
  `1d9c463`.
- **Remote (`Parallax`, separate repo at `/Volumes/lab/talea/Parallax`):** local commit `8fb7482e`
  ("docs: update iamf-rs maturity to v0.1.0-beta.2...") on branch `gsd/v1.0-milestone`, **NOT
  pushed**. That branch is 3 commits ahead of `origin/gsd/v1.0-milestone` (2 pre-existing + this
  one). The Parallax working tree also has unrelated pre-existing uncommitted changes this session
  did not touch (see `<context>`).
- **Working tree (`iamf-rs`):** clean except user-owned `docs/` clones, `.DS_Store`,
  `iamf-rs.code-workspace`, and one newly-noticed untracked planning doc — see
  `docs/superpowers/plans/2026-09-12-delivery-conformance-profile-matrix.md` below.
- **A separate, unreviewed WIP branch exists:** `origin/wip/delivery-validator-conformance`
  (commit `79dafb1`) — see `<remaining_work>` item 2.
</current_state>

<completed_work>

Since the previous handoff (`2d61359` → `.planning/.continue-here.md` as it stood at session start,
which itself listed the 2026-09-14 quick-task queue):

| Task | What it did | Commits |
|---|---|---|
| 260914-5c5 | Opaque extension param definitions (`param_definition_type` > 2) never registered instead of aborting parse; writers refuse 7 classes of non-round-tripping states (`ReservedAliasesDefinedValue`, `GatedFieldMismatch`) | `b164021`,`f4a4414`,`10c0077`; pushed as `f24a095` |
| 260914-hoa | Streaming `SequenceReader` (`parse_sequence` = `collect` over it); 32 MiB of minimal OBUs: 3.25 GB → 34 MB max RSS; no `ParseLimits` added | `c1fb3fa`,`0c34ed4`,`5fa7718`; pushed as `2b4c058` |
| 260914-kfs | `kMaxNumParameters` (iamf-tools' own 256-param resource limit) NOT adopted on read/write; `validate()` finding added instead | `98c80fe`,`36b4289`; pushed as `b70b35c` |
| 260914-m62 | BCP-47 (RFC 5646 §2.1) well-formedness check of `annotations_language`; `validate()` finding + `build()` `AnnotationsLanguageNotWellFormed`; parse stays tolerant; ASCII-case-insensitive duplicates kept | `269b4f9`,`40f71a4`,`d23a223`; pushed as `44221fe` |
| 260914-pyd | Merged `feature/aac-lc-framing` (8 commits, forked 2026-09-12) into `main` via `git merge --no-ff`; AAC-LC is now a fourth pre-encoded, framing-only codec; fixed one post-merge `E0004` (`output_sample_rate()` non-exhaustive) not among the 4 predicted textual conflicts; updated `PROJECT.md`/`HANDOFF.md`/`REFERENCES.md`/`REQUIREMENTS.md` scope wording | `474004a`(merge),`2ba6b33`,`1d9c463`(+`d12590e`); pushed as `1d9c463` |
| release | Tagged `v0.1.0-beta.2` (annotated) at `1d9c463`, pushed; updated Parallax's `iamf-rs` maturity row | tag `v0.1.0-beta.2`; Parallax `8fb7482e` (local only) |

Each quick task's CI + `reference` workflow run was independently verified green by the orchestrator
(not just trusted from the executor's own report) before proceeding to the next.

**Notable incident, handled:** while investigating `feature/aac-lc-framing` for the merge, found a
leftover worktree (`.worktrees/aac-lc-framing`) from an earlier crashed session containing
substantial **uncommitted, untested** work — a "delivery validator"
(`validate_delivery_conformance()`/`validate_delivery_timeline()`, matching what
`docs/IAMF-V1.1-COMPLETENESS-AUDIT.md` describes as already present, which it is NOT on any branch
that was ever committed). This was committed as a WIP commit, split onto its own branch
`wip/delivery-validator-conformance`, pushed for safekeeping, and the working `feature/aac-lc-framing`
worktree was reset to `origin`'s clean tip before the real merge proceeded. Nothing from that WIP
went into the `main` merge.

</completed_work>

<remaining_work>

User-approved / queued, not yet started this session:

1. **`libiamf` vector harness + test-scaffolding consolidation** — user said "ja gerne einbauen" (go
   ahead, build it in). Full scope is in
   `.planning/todos/pending/2026-09-14-libiamf-vector-harness-and-test-consolidation.md`: the pinned
   `.reference/libiamf` ships 221 test vectors (193 `is_valid: true`, 28 `is_valid: false`) that the
   suite barely exercises today (essentially just `test_000003`), plus ~1,000–1,400 reducible lines
   of test-helper duplication (`lpcm_config` ×4, `stereo_element` ×3, `presentation` ×3, etc.) across
   `tests/**/*.rs` (20,187 lines, 469 tests as of that todo's writing — now higher after today).
   **File-overlap caution:** this will likely touch `tests/conformance.rs`, `tests/support/`, and
   `tools/build-reference.sh` — all touched by today's `260914-pyd` AAC-LC merge — so route through
   `/gsd-quick --research` (or `--discuss --research` if the harness shape needs a user decision
   first) rather than assuming the todo's proposal is still current against `main`'s new state.
2. **Review `origin/wip/delivery-validator-conformance`** (commit `79dafb1`) — untested,
   uncommitted-until-rescued work implementing a delivery-completeness validator and a profile
   matrix. Pairs with two artifacts already on disk in this working tree (untracked, not lost, but
   not yet reviewed):
   - `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md` (238 lines, dated 2026-09-12, written against the
     *uncommitted* worktree state — its P1 findings describe `validate_delivery_conformance()` /
     `validate_delivery_timeline()` as if already shipped; they are not, anywhere on `main`)
   - `docs/superpowers/plans/2026-09-12-delivery-conformance-profile-matrix.md` (516 lines,
     newly noticed this session, same 2026-09-12 origin — the actual implementation plan for that WIP)
   Read all three together before deciding whether to resume/replan/discard the branch. Do NOT
   assume the branch compiles or that its approach still fits `main`'s current shape (today's
   `TemporalProgress`/cross-unit-trim work in `encoder.rs` post-dates this WIP's fork point).
3. **`MixPresentationTags` presence rule** (`260913-wlv` deferred item 2) — needs a model change
   first; tags are currently raw `trailing` bytes, not interpreted.
4. **Accumulated deferred items** across today's five quick tasks — see each
   `.planning/quick/260914-{5c5,hoa,kfs,m62,pyd}-*/*-deferred-items.md`. Highlights:
   - Reserved-alias problem likely also exists in `LoudspeakerLayout`, `SoundSystem`,
     `HeadphonesRenderingMode` (unprobed).
   - Audio Element param type 0: spec/reference disagreement noted but not resolved (spec: no size
     field; `iamf-tools`: rejects it; this crate and `libiamf`: read size + bytes).
   - Spec's 0–2 params per channel-based element / 0 for scene-based element rule
     (`index.bs:752-753`) has no finding yet.
   - `*.proptest-regressions` files aren't gitignored (will reappear untracked after any future
     proptest failure).
5. **Push Parallax's `8fb7482e`** (or fold it into a larger push) — currently local-only on
   `gsd/v1.0-milestone`, 3 ahead of `origin`. Not pushed because it was never asked for beyond the
   commit itself.

</remaining_work>

<decisions_made>

All decisions from the previous handoff still hold (conformance rule, single Codec Config,
`parameter_rate` == sample rate, Opus start trim == `pre_skip`, profile scopes per errata,
ASCII-case-insensitive language tags, `num_layouts >= num_layers` finding-only, low-level writers
byte-exact, sequential execution on `main`).

New this session:
- **`feature/aac-lc-framing` merged via `git merge --no-ff`**, not rebase/squash — user: "ja mergen
  würde ich sagen, ist doch ein normales feature". Preserves the branch's 8-commit history.
- **Delivery-validator WIP kept fully separate** from the AAC-LC merge and from `main` — user chose
  "separately review" over merging-together or discarding, after the orchestrator surfaced it
  mid-task rather than silently including or silently dropping it.
- **`REQUIREMENTS.md` (DECO-03, API-06, API-09) updated immediately**, not deferred — user: "ja,
  jetzt kurz nachziehen" — even though this was outside the AAC-LC merge quick task's originally
  locked doc scope (`PROJECT.md` + `HANDOFF.md` only).
- **`kMaxNumParameters` (256) NOT adopted as a read/write limit** — it's `iamf-tools`' own artificial
  resource-safety cap (introduced by its commit `feb873de`, "to fix cases of excessive fuzzer
  memory"), not a spec or interop requirement, and this crate's own memory amplification for that
  path is already bounded by other means. A `validate()` finding warns the consumer instead.
- **`v0.1.0-beta.2` tagged and pushed** — user: "tag mal ein neues release". Annotated tag,
  same convention as `v0.1.0-beta.1` (no `Cargo.toml` version bump; `publish = false`, consumed as a
  Parallax path dependency, so the tag is a milestone marker, not a crates.io release).
- **Parallax portfolio doc maturity row updated, "Owns" column left alone** — the AAC-LC merge
  doesn't move ownership or add a cross-library object (the portfolio doc already described
  `iamf-encode-rs` as producing AAC-LC access units for `iamf-rs` to frame; this merge just makes
  `iamf-rs` actually do what the doc already expected of it), so the heavier Change-Control path
  (HANDOFF.md + consumer pin together) was judged not to apply — only committed in Parallax's repo
  after explicit user "ok", and only the one intended file was staged (several unrelated pre-existing
  uncommitted files sit in that working tree, untouched).

</decisions_made>

<blockers>
- **None technical.** Three items queued for the next session with no explicit priority order chosen
  yet (see `<remaining_work>` 1–3); `human_actions_pending` in `.planning/HANDOFF.json` has the
  specifics.
</blockers>

## Required Reading (in order)
1. This file's `<current_state>` and `<remaining_work>` sections.
2. `.planning/todos/pending/2026-09-14-libiamf-vector-harness-and-test-consolidation.md` — if
   resuming item 1.
3. `docs/IAMF-V1.1-COMPLETENESS-AUDIT.md` + `docs/superpowers/plans/2026-09-12-delivery-conformance-profile-matrix.md`
   + `git show origin/wip/delivery-validator-conformance` — if resuming item 2. Read all three before
   touching any of them.
4. `.planning/quick/260914-{5c5,hoa,kfs,m62,pyd}-*/{RESEARCH,SUMMARY,deferred-items}.md` — full
   context for today's five completed tasks, if any deferred item needs picking up.
5. `REFERENCES.md` (licence table incl. the new FDK-AAC reference-tier note) and
   `CONFORMANCE-GATE.md`.
6. `.planning/METHODOLOGY.md`, if it exists.

## Critical Anti-Patterns (do NOT repeat these)
- **Trusting decoder acceptance or exit code 0 as conformance** → pinned source + spec, positive and
  negative controls.
- **A guard without a firing proof** → add a `prove-guards.sh` case that also passes on the clean copy.
- **Trusting the editor's live diagnostics over `cargo build`/`cargo test`** after any subagent commit.
- **A multi-line shell command where an earlier `cd` silently failed** can leave later commands
  running in the wrong directory with no error — check `pwd`/`git status` after any `worktree add`
  or `cd` whose success wasn't independently confirmed.
- **Removing or reaping a worktree without checking `git status --porcelain` in it first** — it may
  hold the only copy of real uncommitted work.
- **Adopting a changed reference hash without upstream `is_valid: false`** → stop and report.
- **Broad staging** → explicit paths only, in every repository touched (this session touched two:
  `iamf-rs` and `Parallax`).

## Infrastructure State
- **Reference decoder:** `.reference/libiamf` (gitignored, pinned `f06e919e`), built with
  `IAMF_REFERENCE_ENABLE_AAC=1` in CI as of today (`aac_reference_enabled: true` in the manifest);
  local dev builds still default AAC off (opt-in only) to keep ordinary local smoke runs fast.
- **Docker image:** `iamf-tools:v2.1.0` present locally for CONF-06/07.
- **`ffmpeg`:** `/usr/local/bin/ffmpeg` 9.0.1 with raw `iamf` muxer (binary use only, per licence
  policy).
- **Toolchain:** Rust 1.85.0, pinned. No let-chains.
- **GitHub Actions:** `reference` workflow now builds an extra FDK-AAC archive link step when
  `IAMF_REFERENCE_ENABLE_AAC=1`; still ~10–11 min typically, has hung once historically (16 h) on
  the Docker image build step — watch for that specifically, not just overall duration.
- **Git remotes touched this session:** `iamf-rs` (`origin/main` = `1d9c463`, tag
  `v0.1.0-beta.2` pushed, branch `wip/delivery-validator-conformance` pushed); `Parallax`
  (`gsd/v1.0-milestone`, local commit `8fb7482e` NOT pushed).

<context>
The user wants iamf-rs strictly IAMF-conformant, and today additionally wanted the long-idle
AAC-LC feature branch merged and a fresh release cut. Method per quick task, unchanged from prior
sessions: `--research` when rules come from the references (scratch prototype, predicted hash) →
planner → sequential executor on `main` → orchestrator re-verification (re-run the key commands,
don't trust the executor's report) → STATE.md row + docs commit → push on request → watch CI +
`reference` workflow, extracting the actual sample-diff/byte-diff lines from the log rather than
trusting exit codes. For the AAC-LC merge specifically, the orchestrator did its own disposable trial
merge in a scratch worktree first to produce an authoritative conflict-resolution guide before
spawning the planner — this proved valuable (caught the real conflict shape, including one
non-conflict-marker compile error the plan correctly anticipated fixing) and is worth repeating for
any future branch integration. The user communicates in German; present results in German with
technical identifiers in English. Explanatory output style: include short ★ Insight blocks.

Parallax's working tree (`/Volumes/lab/talea/Parallax`) has unrelated pre-existing uncommitted
changes this session did not create and did not touch: `.claude/skills/parallax-stack/SKILL.md`,
`.gsd/dispatch-isolation-sentinel.json`, `.planning/LICENCES.md`, `.planning/STATE.md`,
`.planning/state.json`, `CLAUDE.md`, `docs/CLAUDE.md`, plus untracked
`.planning/milestone.lock` and `tests/__pycache__/*.pyc`. These belong to whatever other work is in
progress there; do not stage or commit them as a side effect of any future Parallax edit in this
project's name.
</context>

<next_action>
Ask the user to pick from `<remaining_work>` items 1–3 (libiamf vector harness is pre-approved but
not yet scheduled; delivery-validator WIP review and MixPresentationTags are queued but undecided on
order). Start with `git status -sb` and `gh run list --limit 3` in `iamf-rs` to reconfirm nothing
changed remotely since this handoff, then route the chosen item through the appropriate `/gsd-quick`
variant per the file-overlap caution in item 1.
</next_action>
